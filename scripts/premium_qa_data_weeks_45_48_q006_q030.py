"""Hand-crafted premium Q006–Q030 for Weeks 45–48 — .NET, Cloud, System Design, Whiteboard Mocks."""

from premium_qa_data import q

WEEK_45_Q006_Q030 = [
    q("SOLID Single Responsibility", "SOLID", "Very Common",
      "Explain the Single Responsibility Principle. Give a .NET example of a violation and how you would refactor it.",
      "A class should have one reason to change — one job. Violation: `OrderService` that validates, persists, sends email, and generates PDF. Refactor into separate services orchestrated by a thin coordinator.",
      "**SRP:** Each module owns one axis of change — persistence, notification, or business rules — not all three.\n\n**Violation:**\n```csharp\npublic class OrderService {\n  public void PlaceOrder(Order o) {\n    Validate(o); _db.Save(o); _email.Send(o); _pdf.Generate(o);\n  }\n}\n```\n\n**Refactor:** `OrderValidator`, `IOrderRepository`, `INotificationService`, `IInvoiceGenerator` injected into a slim `PlaceOrderHandler`.\n\n**Architect lens:** SRP at service boundary — microservice 'does one thing' is SRP at deployable unit. Over-splitting creates distributed monolith — balance cohesion vs change isolation.",
      "Interviewers want refactor instinct, not textbook definition — show before/after with reasons to change.",
      "SRP vs microservices? — One bounded context per service — SRP at architecture level.",
      "When is a god class acceptable? — Prototype or script — never in production domain layer.",
      "Defining SRP as 'one method per class'",
      "Splitting into 20 one-liner classes with no cohesion",
      "Ignoring that 'reason to change' comes from stakeholders not code shape"),
    q("Open Closed Principle", "SOLID", "Very Common",
      "What is the Open/Closed Principle and how do you apply it in ASP.NET extensibility?",
      "Open for extension, closed for modification. Add behavior via new types (strategies, handlers) without editing existing stable code — e.g. `IAuthorizationHandler`, MediatR pipeline behaviors, or policy-based auth.",
      "**OCP in .NET:**\n- **Strategy injection** — new `IPricingStrategy` implementation without changing `CheckoutService`\n- **MediatR behaviors** — add logging/validation pipeline without touching handlers\n- **Policy-based authorization** — new `IAuthorizationRequirement` + handler\n\n**Anti-pattern:** Giant `switch` on `orderType` in one service — every new type edits core class.\n\n**Architect:** Plugin architecture (MEF, DI registration modules) enables third-party extensions. Document extension points in ADR — which assemblies are stable vs pluggable.",
      "OCP is how platforms scale feature velocity without regression risk on core paths.",
      "OCP vs YAGNI? — Introduce extension point when second variant is certain — not ten speculative interfaces.",
      "Decorator vs inheritance for OCP? — Prefer composition — decorators wrap without subclass explosion.",
      "switch/if-else chains for every new feature",
      "Abstracting before second use case exists",
      "Modifying shared library for one team's edge case"),
    q("Liskov Substitution", "SOLID", "Common",
      "Explain Liskov Substitution Principle with a .NET collections or inheritance example.",
      "Subtypes must be substitutable for base types without breaking callers. Classic violation: `Square : Rectangle` where setting width breaks height invariant. In .NET: don't throw `NotSupportedException` on base interface methods callers expect to work.",
      "**LSP:** Preconditions not strengthened, postconditions not weakened, invariants preserved.\n\n**.NET examples:**\n- `ReadOnlyCollection<T>` — callers expecting mutable list break\n- `Repository` subclass that throws on `Delete` when base allows it\n- EF `DbContext` override that skips `SaveChanges` side effects\n\n**Fix:** Smaller interfaces (`IReadRepository`, `IWriteRepository`) so read-only impl doesn't fake unsupported writes.\n\n**Architect:** Contract tests on interface implementations — CI verifies LSP across teams.",
      "LSP connects inheritance design to production bugs when mocks don't match real behavior.",
      "Interface segregation relation? — ISP prevents LSP violations from fat interfaces.",
      "Testing LSP? — Property-based tests — any impl satisfies same contract.",
      "Subclass that changes expected behavior silently",
      "Throwing NotSupportedException on half the interface",
      "Mock in tests that violates production impl contract"),
    q("Interface Segregation", "SOLID", "Common",
      "What is Interface Segregation and how does it show up in repository and API design?",
      "Clients should not depend on methods they don't use. Split `IUserRepository` into `IUserReader`, `IUserWriter`, `IUserAdmin` — consumers take only what they need.",
      "**Fat interface smell:**\n```csharp\ninterface IOrderRepo {\n  Get(); Save(); Delete(); BulkImport(); GenerateReport();\n}\n```\nReporting service forced to depend on `Delete`.\n\n**Segregated:**\n- `IOrderQueries` — read models\n- `IOrderCommands` — writes\n- `IOrderMaintenance` — admin batch jobs\n\n**API parallel:** Don't expose 40-field admin DTO on mobile BFF — separate read contracts.\n\n**Architect:** Segregation reduces blast radius of change and clarifies security boundaries (read-only service account gets `IReader` only).",
      "ISP is permission and dependency minimization — critical for zero-trust service accounts.",
      "ISP vs too many interfaces? — Segregate by client role — not every method gets its own file.",
      "CQRS connection? — Command/query split is ISP at architectural level.",
      "One IRepository<T> with 15 methods for all consumers",
      "Admin operations on same interface as public API",
      "Segregating into unusable one-method interfaces"),
    q("Dependency Inversion", "SOLID", "Very Common",
      "Explain Dependency Inversion and how DI containers implement it in ASP.NET Core.",
      "High-level modules depend on abstractions, not concretions. `OrderService` depends on `IOrderRepository`, not `SqlOrderRepository`. ASP.NET Core DI registers implementations at composition root.",
      "**DIP layers:**\n1. Domain defines `IOrderRepository` port\n2. Infrastructure implements `SqlOrderRepository` adapter\n3. `Program.cs` wires `AddScoped<IOrderRepository, SqlOrderRepository>`\n\n**Benefits:** Unit test with fakes, swap SQL for Cosmos without domain change, clear module boundaries.\n\n**Composition root:** Only `Program.cs`/startup references concrete types — rest of app sees interfaces.\n\n**Architect:** DIP enables hexagonal architecture — domain never imports EF or HTTP.",
      "DIP is the foundation of testable .NET microservices — interviewers probe composition root discipline.",
      "DI vs service locator? — Constructor injection only — service locator hides dependencies.",
      "Registering concrete types everywhere? — Bind to interfaces at boundaries.",
      "Domain project references Entity Framework",
      "new SqlConnection() inside business logic",
      "Service locator or static singletons in domain"),
    q("Repository Anti-Pattern When", "Data Access", "Very Common",
      "When is the repository pattern an anti-pattern in .NET?",
      "When it wraps EF `DbSet` with no added value — leaky CRUD pass-through, duplicate LINQ in repo and service, or hides `IQueryable` leading to N+1. Skip for simple CRUD; use when aggregating multiple sources, caching, or test seam needed.",
      "**Anti-pattern signals:**\n- `GetById`, `GetAll`, `Add`, `Update`, `Delete` only — EF already is a repository\n- Generic `IRepository<T>` used everywhere — anemic domain\n- Repository returns `IQueryable` — data access leaks to controllers\n\n**Good use:**\n- Aggregate root persistence with domain mapping\n- Multiple backing stores (SQL + search index)\n- Explicit test double boundary\n\n**Architect:** CQRS read side often skips repository — queries hit optimized projections directly.",
      "Senior answer: repository is a seam, not a mandatory layer — justify with testing and boundary needs.",
      "Specification pattern instead? — Composable query specs when complex filtering reused.",
      "DbContext as unit of work? — Often sufficient — don't wrap for ceremony.",
      "Mandatory IRepository<T> on every entity",
      "Repository per table in DDD aggregate design",
      "Returning IQueryable to API layer"),
    q("Unit of Work Interview", "Data Access", "Very Common",
      "Explain Unit of Work pattern and how EF Core DbContext implements it.",
      "Unit of Work tracks changes across multiple repositories in one transaction. EF `DbContext` is a UoW — `SaveChanges()` commits atomically. Explicit UoW wrapper useful when multiple DbContexts or non-EF stores participate.",
      "**EF Core UoW:**\n```csharp\nawait using var tx = await _db.Database.BeginTransactionAsync();\ntry {\n  _db.Orders.Add(order);\n  _db.Inventory.Update(stock);\n  await _db.SaveChangesAsync();\n  await tx.CommitAsync();\n} catch { await tx.RollbackAsync(); throw; }\n```\n\n**Explicit `IUnitOfWork`:** Wraps `DbContext` for domain services that shouldn't know EF — `Commit()`/`Rollback()`.\n\n**Distributed:** Cross-service UoW needs saga — not one SQL transaction.\n\n**Architect:** Scoped `DbContext` per HTTP request = one UoW per request — default ASP.NET pattern.",
      "UoW interview tests transaction boundary understanding — critical for consistency.",
      "Multiple SaveChanges in one request? — Usually one at end — intermediate saves break atomicity.",
      "UoW across microservices? — Saga/outbox — not shared DbContext.",
      "Calling SaveChanges after every single insert",
      "Singleton DbContext",
      "Expecting SQL transaction across HTTP calls"),
    q("EF Core N+1 Problem", "EF Core", "Very Common",
      "What is the N+1 query problem in EF Core and how do you fix it?",
      "N+1: load N parent rows, then one query per child in a loop — 1 + N round trips. Fix with `.Include()`, `.ThenInclude()`, projection `.Select()`, or explicit split query / batching.",
      "**Problem:**\n```csharp\nvar orders = await _db.Orders.ToListAsync();\nforeach (var o in orders)\n  var items = o.Items; // lazy load triggers query each time\n```\n\n**Fixes:**\n1. **Eager load** — `.Include(o => o.Items)`\n2. **Projection** — `.Select(o => new { o.Id, Items = o.Items.Select(...) })`\n3. **Split query** — `.AsSplitQuery()` for cartesian explosion\n4. **Explicit loading** — `Entry(o).Collection(x => x.Items).LoadAsync()` once batched\n\n**Detection:** EF logging, MiniProfiler, Application Insights dependency traces.\n\n**Architect:** API pagination + projection beats Include on large graphs.",
      "N+1 is the #1 EF production perf bug — show detection and fix, not just definition.",
      "AsNoTracking when? — Read-only lists — reduces memory and tracking overhead.",
      "Cartesian explosion? — Multiple Includes inflate rows — split query or projection.",
      "Lazy loading enabled globally in API",
      "Include entire object graph by default",
      "Not profiling SQL in staging"),
    q("EF Core Raw SQL When", "EF Core", "Common",
      "When should you use raw SQL with EF Core instead of LINQ?",
      "Raw SQL for: complex reports, bulk updates, table hints, database-specific features (JSON indexes, CTEs), or proven perf-critical paths where LINQ generates poor plans. Use `FromSqlRaw` with parameters — never string concat.",
      "**Use cases:**\n- Bulk `UPDATE`/`DELETE` — `ExecuteSqlRawAsync`\n- Window functions, recursive CTEs\n- Indexed view queries\n- Migration-time data fixes\n\n**Safe pattern:**\n```csharp\n_db.Orders.FromSqlInterpolated($\"SELECT * FROM Orders WHERE Status = {status}\")\n```\n\n**Risks:** Bypasses change tracker, DB coupling, harder to test.\n\n**Architect:** Isolate raw SQL in query classes — keep domain free of SQL strings. Document in ADR with benchmark evidence.",
      "Raw SQL is a scalpel — justify with execution plan or unsupported LINQ, not preference.",
      "FromSql vs ExecuteSql? — FromSql materializes entities; Execute for non-query.",
      "Stored procedures? — Valid for DBA-owned tuning — map with FromSqlRaw.",
      "String concatenation SQL — injection risk",
      "Raw SQL everywhere avoiding LINQ maintainability",
      "No parameterization on user input"),
    q("ASP.NET Middleware Order", "ASP.NET Core", "Very Common",
      "What is the correct middleware order in ASP.NET Core and why does it matter?",
      "Order matters because each middleware wraps the next. Typical: Exception handler → HTTPS → Static files → Routing → CORS → Authentication → Authorization → Endpoints. Correlation ID early; auth before authz.",
      "**Recommended order:**\n1. Exception handling / developer exception page\n2. HTTPS redirection\n3. Static files (if any)\n4. Routing\n5. CORS\n6. Authentication\n7. Authorization\n8. Custom (logging, correlation ID)\n9. Endpoints\n\n**Why:** Auth middleware must run before `[Authorize]` endpoints execute. CORS before auth for preflight. Exception handler outermost to catch all.\n\n**Architect:** Document middleware pipeline diagram per environment — dev vs prod differ (HSTS, exception detail).",
      "Wrong middleware order causes 'works locally' auth and CORS bugs — common senior trap question.",
      "Terminal middleware? — Run, Map, Use when short-circuit — nothing after runs.",
      "Request buffering? — Enable early if body read multiple times.",
      "Authorization before Authentication",
      "CORS after endpoints",
      "Exception handler innermost — errors escape unhandled"),
    q("DI Container Scopes Interview", "Dependency Injection", "Very Common",
      "Explain DI lifetimes in ASP.NET Core: Singleton, Scoped, Transient. Give a dangerous mismatch example.",
      "Singleton: one instance app-wide. Scoped: per request (DbContext). Transient: new each resolve. Danger: injecting Scoped `DbContext` into Singleton — captive dependency, stale context, thread-safety bugs.",
      "**Lifetime rules:**\n- `DbContext` → **Scoped**\n- Stateless services → Singleton OK\n- Lightweight helpers → Transient\n\n**Captive dependency:**\n```csharp\n// BAD: Singleton holds Scoped DbContext\nservices.AddSingleton<ICacheWarmup, CacheWarmup>(); // ctor takes DbContext\n```\n\n**Fix:** `IServiceScopeFactory` to create scope inside singleton background work.\n\n**Hosted services:** Create scope per iteration:\n```csharp\nusing var scope = _scopeFactory.CreateScope();\nvar db = scope.ServiceProvider.GetRequiredService<AppDbContext>();\n```\n\n**Architect:** Validate lifetimes in code review checklist — static analysis catches some mismatches.",
      "Scope mistakes cause production-only concurrency bugs — classic staff-level .NET question.",
      " IDisposable singleton? — Rare — ensure thread-safe or use scoped.",
      "HttpClient? — IHttpClientFactory — not new HttpClient per request.",
      "Singleton injecting DbContext",
      "Transient DbContext per resolve",
      "Background service without scope factory"),
    q("Minimal API Production", "ASP.NET Core", "Common",
      "When are Minimal APIs appropriate for production and what structure do you add at scale?",
      "Minimal APIs suit micro-endpoints, internal tools, and BFFs with low ceremony. At scale add: endpoint groups, filters, validation, OpenAPI, auth policies, and separate handlers — don't put business logic inline.",
      "**Production structure:**\n```csharp\nvar orders = app.MapGroup(\"/orders\").RequireAuthorization();\norders.MapPost(\"/\", CreateOrder.Handle)\n  .AddEndpointFilter<ValidationFilter<CreateOrderRequest>>();\n```\n\n**Add:**\n- Carter/vertical slices or MediatR handlers\n- `ProblemDetails` exception handling\n- API versioning\n- Rate limiting (.NET 7+)\n\n**When MVC instead:** Complex model binding, Razor, filter ecosystems teams already know.\n\n**Architect:** Minimal vs controllers is team consistency — not religion.",
      "Minimal APIs are production-ready with discipline — interview tests whether you know the guardrails.",
      "Endpoint filters vs middleware? — Filters per-route; middleware pipeline-wide.",
      "Testability? — WebApplicationFactory integration tests same as controllers.",
      "500-line lambda in Program.cs",
      "No auth on internal 'temp' endpoints",
      "Skipping OpenAPI on public APIs"),
    q("API Controller Fat vs Thin", "API Design", "Very Common",
      "Fat controller vs thin controller — where should logic live in a .NET API?",
      "Thin controllers: validate input, call application service/handler, map response. Fat controllers mix HTTP, business rules, and data access — untestable and duplicated across endpoints.",
      "**Thin:**\n```csharp\n[HttpPost]\npublic async Task<IActionResult> Create(CreateOrderDto dto)\n  => Ok(await _mediator.Send(new CreateOrderCommand(dto)));\n```\n\n**Layers:**\n- Controller — HTTP concerns\n- Application — use cases, orchestration\n- Domain — invariants, entities\n- Infrastructure — EF, messaging\n\n**Fat smell:** `if (user.IsPremium && order.Total > 100)` in controller.\n\n**Architect:** Vertical slice folders (`Features/Orders/Create`) colocate handler + validator + endpoint — thin surface, cohesive module.",
      "Thin controllers enable MediatR/CQRS and consistent cross-cutting — architecture not MVC trivia.",
      "MediatR worth it? — Valuable at 20+ endpoints with shared pipelines.",
      "Exception handling in controller? — Global middleware — not try/catch per action.",
      "Business rules in controller actions",
      "Direct DbContext in controller",
      "Duplicate validation in every action"),
    q("DTO vs Domain Model", "API Design", "Very Common",
      "When use DTOs vs domain entities in API contracts?",
      "Never expose domain entities directly on APIs — use DTOs/records to control shape, versioning, and prevent over-posting and lazy-load serialization leaks.",
      "**DTO benefits:**\n- Hide internal fields (`PasswordHash`, navigation props)\n- Stable contract when domain refactors\n- Input validation on dedicated request types\n- Prevent mass assignment attacks\n\n**Mapping:** Mapster, AutoMapper, or manual `ToDto()` in application layer.\n\n**Read models:** Separate query DTOs optimized for UI — not reused for writes.\n\n**Architect:** Version DTOs (`CreateOrderV2Request`) — domain can evolve independently. Document breaking change policy.",
      "DTO boundary is API security and evolution — entities over the wire is a red flag.",
      "Record DTOs? — Immutable request types — great for Minimal APIs.",
      "Bidirectional mapping leaks? — Map inbound DTO to command — don't round-trip entity.",
      "Return EF entities with circular references",
      "Same model for read and write on complex aggregates",
      "AutoMapper config untested — silent null mapping"),
    q("Validation Pipeline", "ASP.NET Core", "Common",
      "Design validation for ASP.NET Core APIs — where does it run?",
      "Use FluentValidation or DataAnnotations + `IValidatableObject`, executed via filter or MediatR pipeline behavior before handler. Domain validation stays in domain for invariants.",
      "**Layers:**\n1. **Request validation** — format, required fields (`FluentValidation`)\n2. **Domain validation** — business rules (`OrderCannotExceedCreditLimit`)\n3. **Database constraints** — last line of defense\n\n**Pipeline:**\n```csharp\npublic class ValidationBehavior<TReq,TRes> : IPipelineBehavior<TReq,TRes> {\n  // run validators before handler\n}\n```\n\n**ProblemDetails:** Return 400 with field errors — RFC 7807.\n\n**Architect:** Single validation path — not duplicated in controller and service.",
      "Validation pipeline shows clean architecture layering — not just attributes on DTOs.",
      "FluentValidation vs DataAnnotations? — Fluent for complex rules; annotations for simple.",
      "Client-side only validation? — Never sufficient — always server validate.",
      "Validation only in UI",
      "Business rules in FluentValidation that belong in domain",
      "500 instead of 400 on validation failure"),
    q("Exception Handling Middleware", "ASP.NET Core", "Very Common",
      "How implement global exception handling in ASP.NET Core for APIs?",
      "Use exception handling middleware or `IExceptionHandler` (.NET 8+) returning `ProblemDetails` — map domain exceptions to 400/404/409, log 500 with correlation ID, never leak stack traces in prod.",
      "**Pattern:**\n```csharp\napp.UseExceptionHandler(app => app.Run(async ctx => {\n  var ex = ctx.Features.Get<IExceptionHandlerFeature>()?.Error;\n  var (status, title) = ex switch {\n    NotFoundException => (404, \"Not Found\"),\n    ValidationException => (400, \"Validation Failed\"),\n    _ => (500, \"Internal Server Error\")\n  };\n  await Results.Problem(title: title, statusCode: status).ExecuteAsync(ctx);\n}));\n```\n\n**Log:** Structured log with traceId, userId, exception type — not full PII.\n\n**Architect:** Exception taxonomy per bounded context — avoid catching `Exception` in business code.",
      "Global handler is production hygiene — consistent errors and safe leakage policy.",
      "Developer exception page in prod? — Never — security and noise.",
      "Exception as flow control? — Use Result pattern for expected failures.",
      "try/catch in every controller method",
      "Stack trace returned to mobile clients",
      "Swallowing exceptions without logging"),
    q("Logging Correlation ID", "Observability", "Very Common",
      "How propagate correlation IDs across ASP.NET Core services?",
      "Accept `X-Correlation-ID` header or generate on entry, store in `Activity`/scoped service, enrich Serilog `LogContext`, forward on HttpClient calls and message bus headers.",
      "**Implementation:**\n1. Middleware reads/generates correlation ID\n2. `using (LogContext.PushProperty(\"CorrelationId\", id))`\n3. `Activity.Current` for OpenTelemetry trace ID alignment\n4. `HttpClient` delegating handler adds header to downstream calls\n5. MassTransit/RabbitMQ header propagation\n\n**ASP.NET:** `services.AddHttpContextAccessor()` + middleware sets `Items[\"CorrelationId\"]`.\n\n**Architect:** Correlate logs, traces, and support tickets — same ID end-to-end. Dashboard filter by correlation ID for incident triage.",
      "Correlation ID is minimum viable observability for distributed .NET — must mention outbound propagation.",
      "TraceId vs CorrelationId? — Often same or mapped — align with OpenTelemetry.",
      "Missing on async boundaries? — Flow through `AsyncLocal` or Activity.",
      "Correlation ID only in first service",
      "Random GUID per log line",
      "Not returning correlation ID in error response body"),
    q("HttpClient DNS Issue", "HTTP", "Very Common",
      "Explain the HttpClient DNS staleness problem and the fix in .NET.",
      "`new HttpClient()` per request exhausts sockets; singleton `HttpClient` caches DNS forever — stale IPs after scale-out/failover. Fix: `IHttpClientFactory` with configurable handler lifetime (~2–5 min PooledConnectionLifetime).",
      "**Problems:**\n- **Dispose per use** — socket exhaustion (SNAT port exhaustion in Azure)\n- **Static HttpClient** — DNS not refreshed when K8s service IPs change\n\n**Fix:**\n```csharp\nservices.AddHttpClient<IPaymentClient, PaymentClient>(c => {\n  c.BaseAddress = new Uri(\"https://payments\");\n}).ConfigurePrimaryHttpMessageHandler(() => new SocketsHttpHandler {\n  PooledConnectionLifetime = TimeSpan.FromMinutes(2)\n});\n```\n\n**Architect:** Always register typed clients in DI — document in coding standards. Load test validates no socket leak.",
      "Classic .NET production footgun — IHttpClientFactory is the expected senior answer.",
      "Polly with HttpClient? — AddPolicyHandler on named client — retry/timeout.",
      "gRPC same issue? — Channel lifetime management — don't channel per call.",
      "new HttpClient() in using block per request",
      "Static HttpClient never recycled",
      "Ignoring SNAT limits in Azure App Service"),
    q("Polly Retry Interview", "Resilience", "Very Common",
      "How configure Polly retry for HTTP calls — what should you avoid?",
      "Retry transient failures (408, 429, 5xx) with exponential backoff + jitter, max 3 attempts, only on idempotent operations or with idempotency keys. Never blind retry POST payments without dedup.",
      "**Policy:**\n```csharp\nPolicy.Handle<HttpRequestException>()\n  .OrResult(r => (int)r.StatusCode >= 500)\n  .WaitAndRetryAsync(3, attempt =>\n    TimeSpan.FromSeconds(Math.Pow(2, attempt)) + Jitter());\n```\n\n**Combine:** Retry wrapped by circuit breaker and timeout — timeout innermost.\n\n**429:** Respect `Retry-After` header.\n\n**Architect:** Idempotency-Key header on mutating APIs — enable safe retry. Monitor retry storms — they amplify outages.",
      "Polly interview tests idempotency awareness — retry without it is a money bug.",
      "Circuit breaker when? — After sustained failures — fail fast — protect downstream.",
      "Hedging? — Rare — duplicate requests — only if explicitly safe.",
      "Retry non-idempotent POST unlimited",
      "No jitter — thundering herd on recovery",
      "Retry inside retry across service layers"),
    q("Caching Interview Patterns", "Caching", "Very Common",
      "Compare cache-aside, read-through, write-through, and write-behind for .NET APIs.",
      "Cache-aside: app reads cache, on miss loads DB and populates — most common with Redis. Read-through: cache library loads. Write-through: sync write cache+DB. Write-behind: async DB write — higher throughput, data loss risk.",
      "| Pattern | Pros | Cons |\n|---------|------|------|\n| Cache-aside | Simple, app control | Stampede risk, stale data |\n| Read-through | Centralized load | Cache vendor lock-in |\n| Write-through | Consistent | Write latency |\n| Write-behind | Fast writes | Durability window |\n\n**.NET:** `IDistributedCache` + Redis, `IMemoryCache` for local L1.\n\n**Architect:** TTL + invalidation on write. Hot keys — shard or local L1. Never cache auth tokens or personalized medical data without policy review.",
      "Caching pattern choice affects consistency and incident behavior — go beyond 'use Redis'.",
      "Cache stampede? — Mutex, early expiration, probabilistic refresh.",
      "HybridCache (.NET 9)? — L1+L2 built-in — simplifies two-tier.",
      "Cache everything with no TTL",
      "Distributed cache for single-server app unnecessarily",
      "No cache invalidation on update"),
    q("Design Pattern Factory", "Design Patterns", "Common",
      "When use Factory Method vs Abstract Factory in .NET?",
      "Factory Method: subclass or delegate decides which product to create (`IFileParserFactory.Create(extension)`). Abstract Factory: family of related products (`IUiFactory` → WinButton + WinCheckbox). Use when creation logic is complex or config-driven.",
      "**Factory Method example:**\n```csharp\npublic interface INotificationFactory {\n  INotifier Create(NotificationChannel channel);\n}\n```\n\n**Abstract Factory:** Theme kits — all controls match skin.\n\n**DI as factory:** `IServiceProvider.GetRequiredService<T>()` — prefer when types known at compile time use straight DI.\n\n**Architect:** Factory for plugin systems and multi-tenant provider selection — not for every `new`.",
      "Pattern interview: connect to DI and plugin registration — not Gang of Four recitation.",
      "Simple Factory vs static? — Inject factory interface — testable.",
      "Options pattern? — Configuration-driven factory selection.",
      "Factory for every object creation",
      "Service locator disguised as factory",
      "Abstract factory when one product type"),
    q("Strategy Pattern Example", "Design Patterns", "Common",
      "Give a Strategy pattern example in .NET enterprise code.",
      "Encapsulate interchangeable algorithms — `IShippingCalculator` implementations (Standard, Express, Overnight) selected at runtime by rule engine or customer tier.",
      "```csharp\npublic interface IShippingStrategy {\n  decimal Calculate(Order order);\n}\n// DI: services.AddScoped<IShippingStrategy, ExpressShipping>();\n// Or keyed services (.NET 8): keyed by enum\n```\n\n**vs if/else:** Open for new carriers without editing `CheckoutService`.\n\n**Real-world:** Payment providers (`IPaymentGateway`), tax by jurisdiction, discount rules.\n\n**Architect:** Store strategy key in DB/config — enable ops to switch without deploy.",
      "Strategy maps to business variability — interviewers want runtime selection story.",
      "Strategy vs Chain of Responsibility? — Chain passes along until handled; strategy picks one.",
      "Rules engine? — Drools/Azure Logic Apps for complex rule sets.",
      "switch on string for every new variant",
      "Strategy with static global state",
      "No default/fallback strategy"),
    q("Observer Events .NET", "Design Patterns", "Common",
      "How do events and observer pattern work in .NET — and when prefer messaging?",
      "In-process: C# `event`, `IObservable<T>`, or MediatR notifications for domain events within monolith. Cross-service: message bus (Service Bus, Kafka) — not .NET events.",
      "**In-process:**\n```csharp\npublic class Order {\n  public event EventHandler<OrderPlacedArgs> Placed;\n}\n// Or: await _mediator.Publish(new OrderPlaced(orderId));\n```\n\n**Domain events:** Raised on aggregate change, handled after `SaveChanges` in same UoW or via outbox.\n\n**Distributed:** Outbox pattern → Service Bus topic — subscribers decoupled.\n\n**Architect:** Don't use static events in web apps — scoped lifetime issues. Prefer MediatR or explicit dispatcher.",
      "Observer scope boundary — in-proc vs distributed — is the architect distinction.",
      "Event vs command? — Event past tense, multiple subscribers; command one handler.",
      "Ordering guarantees? — In-proc ordered; bus may need partitions.",
      "Static C# events in ASP.NET request scope",
      "Synchronous event handler doing HTTP calls",
      "Missing outbox — dual-write DB and bus"),
    q("Decorator Pattern", "Design Patterns", "Common",
      "Explain Decorator pattern with a .NET example.",
      "Wrap object to add behavior without subclassing — `CachingRepository` decorates `IOrderRepository`, `LoggingHandler` decorates `HttpMessageHandler`.",
      "```csharp\npublic class CachingOrderRepository : IOrderRepository {\n  private readonly IOrderRepository _inner;\n  private readonly IDistributedCache _cache;\n  public async Task<Order?> GetAsync(Guid id) {\n    var cached = await _cache.GetAsync(id);\n    if (cached != null) return cached;\n    var order = await _inner.GetAsync(id);\n    await _cache.SetAsync(id, order);\n    return order;\n  }\n}\n```\n\n**DI registration:** `services.Decorate<IOrderRepository, CachingOrderRepository>()` (Scrutor).\n\n**Architect:** Cross-cutting concerns (cache, audit, retry) as decorators — keeps core clean.",
      "Decorator is the structural pattern behind HttpClient handlers and Scrutor — practical .NET.",
      "Decorator vs middleware? — Similar — middleware is pipeline decorator for HTTP.",
      "Too many layers? — Watch debug difficulty — limit stack depth.",
      "Subclass explosion instead of wrap",
      "Decorator changing interface contract",
      "Order of decorators matters — document"),
    q("Interview Coding Communication", "Interview Skills", "Very Common",
      "How communicate effectively during a live coding interview in .NET?",
      "Think aloud: restate problem, clarify inputs/edge cases, propose approach and complexity, code incrementally, test with example, discuss trade-offs. Silence is worse than wrong direction with correction.",
      "**Structure (25–35 min):**\n1. **Clarify** (3 min) — nulls, scale, duplicates\n2. **Approach** (5 min) — brute force then optimize\n3. **Code** (15 min) — meaningful names, small methods\n4. **Test** (5 min) — walk through example + edge case\n5. **Optimize** (5 min) — time/space, .NET collections choice\n\n**Phrases:** 'I'll use Dictionary for O(1) lookup because...' 'Trade-off: memory for speed.'\n\n**Architect angle:** Mention production concerns — thread safety, testability — even if not asked.",
      "Communication is scored equally to correctness at senior levels — practice narration.",
      "Stuck? — Say so, ask hint, try simpler subproblem.",
      "Finish early? — Discuss monitoring, API shape, failure modes.",
      "Silent coding for 20 minutes",
      "Jump to code without examples",
      "Argue with interviewer instead of incorporating hints"),
]

WEEK_46_Q006_Q030 = [
    q("Azure vs AWS Service Map", "Cloud", "Very Common",
      "Map equivalent Azure and AWS services for compute, storage, identity, and messaging.",
      "Compute: App Service/AKS ↔ Elastic Beanstalk/EKS/ECS. Storage: Blob ↔ S3. Identity: Entra ID ↔ IAM/Cognito. Messaging: Service Bus/Event Hubs ↔ SQS/SNS/Kinesis.",
      "| Capability | Azure | AWS |\n|------------|-------|-----|\n| VM | Virtual Machines | EC2 |\n| PaaS web | App Service | Elastic Beanstalk |\n| Kubernetes | AKS | EKS |\n| Object storage | Blob Storage | S3 |\n| SQL | Azure SQL | RDS |\n| NoSQL | Cosmos DB | DynamoDB |\n| IAM | Entra ID + RBAC | IAM |\n| Queue | Service Bus | SQS |\n| Events | Event Grid | EventBridge |\n| Secrets | Key Vault | Secrets Manager |\n\n**Architect:** Map by capability not name — migration and multi-cloud ADRs use this table. Note semantic gaps (Cosmos multi-model vs Dynamo key-value).",
      "Service mapping proves cloud fluency beyond single-vendor cert trivia.",
      "Networking? — VNet/VNet peering ↔ VPC/VPC peering.",
      "Serverless? — Functions ↔ Lambda.",
      "Assuming 1:1 feature parity",
      "Ignoring identity model differences",
      "Choosing cloud by console familiarity only"),
    q("SQL Index Interview Question", "Database", "Very Common",
      "How do you decide what indexes to add on a slow SQL query?",
      "Analyze execution plan, identify seek vs scan, high logical reads, missing index DMV hints. Index WHERE/JOIN/ORDER BY columns — leading column most selective. Balance write overhead.",
      "**Steps:**\n1. Capture slow query + actual execution plan\n2. Check clustered vs nonclustered — PK clustered default\n3. Covering index includes SELECT columns — avoid key lookups\n4. Avoid over-indexing — each index slows INSERT/UPDATE\n\n**Example:** `WHERE CustomerId = @id ORDER BY Created DESC` → composite index `(CustomerId, Created DESC)`.\n\n**Architect:** Index strategy in capacity planning — review quarterly with workload changes.",
      "Index interview wants methodology — plan analysis not 'add index on id'.",
      "Filtered index? — `WHERE Status = 'Active'` partial — smaller, faster.",
      "Columnstore? — Analytics workloads — different indexing model.",
      "Index every column",
      "Hint without measuring plan",
      "Ignoring index maintenance fragmentation"),
    q("Query Optimization Steps", "Database", "Very Common",
      "Walk through systematic SQL query optimization.",
      "Measure baseline → examine plan → reduce rows early (filters, sargable predicates) → fix joins → add/revise indexes → consider partitioning → cache or materialize if read-heavy.",
      "**Checklist:**\n1. **Baseline** — duration, CPU, logical reads\n2. **Sargability** — no `WHERE YEAR(Date)=2024` — use range\n3. **Join order** — filter driving table first\n4. **Statistics** — update stats if stale\n5. **Parameter sniffing** — OPTION RECOMPILE or optimize for unknown\n6. **Locking** — READ COMMITTED SNAPSHOT for read/write contention\n\n**Architect:** 80% fix without hardware — document before/after metrics in perf ADR.",
      "Structured optimization beats random index adding — show measurement discipline.",
      "N+1 at ORM layer? — Fix in app before DB tuning.",
      "Read replica for reports? — Offload heavy analytics.",
      "Optimize without baseline metrics",
      "SELECT * in hot paths",
      "Functions on indexed columns in WHERE"),
    q("Replication Lag Handling", "Database", "Very Common",
      "How handle read replica lag in production applications?",
      "Route critical reads to primary, eventual reads to replica with stale tolerance UI, session stickiness after write (read-your-writes), or version tokens to detect stale data.",
      "**Patterns:**\n1. **Read-your-writes** — After POST, read from primary for 2s or by cookie\n2. **Lag-aware routing** — If lag > threshold, fall back to primary\n3. **UI honesty** — 'Data may take a minute to update'\n4. **CQRS** — Projections async — user expects delay\n\n**Monitor:** `replica_lag_seconds` alert > 30s.\n\n**Architect:** Payment balance always primary — product catalog OK on 30s stale replica.",
      "Replication lag is consistency trade-off — architects classify reads by tolerance.",
      "Global replication? — Cosmos/AWS Global Tables — conflict resolution rules.",
      "Automatic failover? — Promote replica — RPO/RTO implications.",
      "Assume replica is always current",
      "No lag monitoring",
      "Financial reads from async replica always"),
    q("Sharding Interview", "Database", "Very Common",
      "How would you shard a multi-tenant SaaS database?",
      "Choose shard key (tenantId), route via lookup service or consistent hash, avoid cross-shard joins, plan resharding, replicate per shard for HA.",
      "**Strategies:**\n- **Tenant-per-shard** — enterprise tenants isolated\n- **Hash(tenantId) % N** — even spread — hot tenant risk\n- **Geo shard** — EU tenants EU shard — compliance\n\n**Components:**\n- Router middleware adds shard context\n- Global index for email → tenantId lookup\n- Cross-shard reports via map-reduce or warehouse\n\n**Architect:** Start single DB until >1TB or noisy neighbor — premature sharding hurts.",
      "Sharding interview tests shard key choice and operational pain awareness.",
      "Vitess/Citus? — Managed sharding layers reduce custom router code.",
      "Hot shard? — Split tenant or salting keys.",
      "Cross-shard JOIN in OLTP",
      "Shard key changes later without plan",
      "No single place for routing logic"),
    q("CAP Theorem Interview", "Distributed Systems", "Very Common",
      "Explain CAP theorem and give a real cloud trade-off example.",
      "In partition, choose Consistency or Availability (not both). AP: DynamoDB eventual, DNS. CP: etcd, Zookeeper, strong SQL primary during split. Most systems are PA with tunable C.",
      "**During network partition:**\n- **CP** — refuse writes or minority partition read-only (bank ledger)\n- **AP** — accept writes both sides, reconcile later (shopping cart merge)\n\n**PACELC:** Else latency vs consistency — normal operation trade-off too.\n\n**Azure example:** Cosmos DB consistency levels — Strong vs Session vs Eventual.\n\n**Architect:** Document per bounded context — payments CP, analytics AP.",
      "CAP is about partition behavior — not 'pick two of three always'.",
      "Kafka CAP? — AP with ordering per partition — nuanced.",
      "Multi-region SQL? — CP with failover — availability hit on partition.",
      "CAP means ignore consistency",
      "Same CAP choice for all services",
      "No partition failure in design discussion"),
    q("Multi-Cloud Pitfalls", "Cloud Strategy", "Common",
      "What are common pitfalls of multi-cloud architecture?",
      "Lowest-common-denominator services, duplicated ops expertise, complex networking, inconsistent security, data egress costs, and no single observability — often 2× cost for portability rarely exercised.",
      "**Pitfalls:**\n- Abstraction tax (custom K8s on both vs managed PaaS)\n- Split brain DR untested\n- Different IAM models — role sprawl\n- Egress between clouds expensive\n\n**When justified:** Regulatory, acquisition integration, negotiate vendor pricing.\n\n**Architect:** Multi-cloud by design vs accident — active-active multi-cloud is rare; backup DR in second cloud more common.",
      "Multi-cloud skepticism with valid use cases shows maturity.",
      "Terraform multi-provider? — Modules per cloud — still ops burden.",
      "Exit strategy vs multi-cloud active? — Exit ≠ run equally on two.",
      "Multi-cloud for buzzword compliance",
      "No shared identity plane",
      "Untested failover between clouds"),
    q("Cloud Migration 6 Rs", "Migration", "Very Common",
      "Explain the 6 Rs of cloud migration with examples.",
      "Rehost (lift-shift), Replatform (managed DB), Repurchase (SaaS), Refactor (cloud-native), Retire (decommission), Retain (keep on-prem).",
      "| R | Action | Example |\n|---|--------|--------|\n| Rehost | VM to Azure VM | Fast, least benefit |\n| Replatform | SQL Server to Azure SQL | Managed patches |\n| Repurchase | CRM to Dynamics 365 | SaaS replace custom |\n| Refactor | Monolith to microservices | Max cloud value |\n| Retire | Turn off unused app | Cost save |\n| Retain | Mainframe stays | Compliance/latency |\n\n**Architect:** Portfolio assessment — 70% rehost/replatform wave 1, refactor strategic apps wave 2.",
      "6 Rs framework structures migration portfolio conversations.",
      "7th R — Relocate? — VMware to AVS — sometimes added.",
      "Wave planning? — Dependency order — identity first.",
      "Refactor everything day one",
      "No retire — migration without decommission",
      "Ignore retain compliance constraints"),
    q("Lift and Shift Risks", "Migration", "Very Common",
      "What risks come with lift-and-shift migration to cloud?",
      "Bringing datacenter inefficiency to cloud OpEx, no auto-scale, license mismatch, wrong VM sizing, latent dependencies on LAN, and security groups misconfigured for internet exposure.",
      "**Risks:**\n- **Cost surprise** — oversized VMs 24/7 vs right-sized PaaS\n- **HA illusion** — single VM in cloud still SPOF\n- **Latency** — app assumes local SQL sub-ms\n- **Licensing** — BYOL vs pay-as-you-go audit\n- **Ops model** — no patch automation\n\n**Mitigation:** Assess with Azure Migrate, pilot non-prod, implement monitoring before cutover.\n\n**Architect:** Lift-shift is valid phase 1 with explicit phase 2 modernization roadmap.",
      "Lift-shift risk awareness prevents 'cloud is more expensive' backlash.",
      "ASR replication? — Test failover quarterly.",
      "Network latency profiling? — Measure before move.",
      "Assume cloud magically scales legacy app",
      "Public RDP on migrated VM",
      "No rollback plan"),
    q("Refactor vs Rehost", "Migration", "Common",
      "When recommend refactor vs rehost for a legacy .NET monolith?",
      "Rehost when deadline tight, app stable, team lacks microservices skill. Refactor when scalability blocked, release cadence slow, or cloud services (managed identity, Service Bus) unlock major value.",
      "**Decision matrix:**\n| Factor | Rehost | Refactor |\n|--------|--------|----------|\n| Timeline | Months | 12–24 months |\n| Business change rate | Low | High |\n| Scale pain | Moderate | Severe |\n| Team maturity | Ops-focused | Platform team ready |\n\n**Strangler fig:** Rehost first, extract hot paths to microservices incrementally.\n\n**Architect:** ADR per application — not one strategy for entire portfolio.",
      "Refactor vs rehost is business timeline question — not technical purity.",
      "Containerize without refactor? — Replatform middle ground.",
      "ROI proof for refactor? — Release frequency, incident cost.",
      "Big-bang rewrite",
      "Rehost without monitoring baseline",
      "Refactor without domain boundaries identified"),
    q("Data Migration Strategy", "Migration", "Very Common",
      "Design data migration strategy with minimal downtime.",
      "Dual-write or CDC replication, migrate historical batch, verify checksums, cutover with DNS/connection switch, rollback window, freeze writes briefly if needed.",
      "**Phases:**\n1. **Schema sync** — target schema ready\n2. **Initial load** — bulk copy off-peak\n3. **CDC** — DMS/ADF catch-up replication\n4. **Verify** — row counts, hash samples, reconciliation reports\n5. **Cutover** — read-only window, final sync, flip connection strings\n6. **Rollback** — keep source read-only 48h\n\n**Architect:** Test migration dry-run monthly — measure lag and duration.",
      "Data migration is highest-risk migration phase — detail cutover and verify.",
      "Azure DMS? — Online migration SQL to Azure SQL.",
      "Large BLOB data? — AzCopy parallel — separate from DB.",
      "Big-bang copy production day one",
      "No reconciliation counts",
      "Cutover without tested rollback"),
    q("Blue-Green Database", "Migration", "Common",
      "How apply blue-green deployment to database changes?",
      "Blue runs old schema, green new — expand-contract pattern: deploy backward-compatible schema first, dual-write, backfill, switch app to green, remove old columns later.",
      "**Expand-contract:**\n1. Add new column (nullable)\n2. Deploy app writing both\n3. Backfill job\n4. Deploy app reading new\n5. Drop old column\n\n**Not:** Two full DB copies for every release — expensive. Blue-green DB means schema compatibility strategy.\n\n**Architect:** Flyway/Liquibase versioned migrations in CI — no manual prod DDL.",
      "Blue-green DB is expand-contract — not literal duplicate databases every deploy.",
      "Feature flags with schema? — Flag reads new column when ready.",
      "Breaking rename? — View layer compatibility shim.",
      "Renaming column in one deploy",
      "Long transaction blocking cutover",
      "No backward compatible migrations"),
    q("Connection Pooling Cloud", "Database", "Very Common",
      "Why connection pooling matters in cloud PaaS databases and what breaks it?",
      "Pools reuse TCP+auth handshakes — critical at scale. Broken by: too many app instances each opening max pool, long idle timeouts, opening connection per request without dispose, serverless scale-out explosion.",
      "**Azure SQL:** Gateway connection pooling, app-side `Max Pool Size` tuning. **PgBouncer** for PostgreSQL.\n\n**Anti-patterns:**\n- 50 AKS pods × 100 pool = 5000 connections — exceeds DTU limit\n- `Pooling=false` in connection string\n- Serverless Functions default pool per instance — use RDS proxy/Azure SQL middle tier\n\n**Architect:** Central pooler or limit pod max connections — monitor `connection_failed` metrics.",
      "Cloud DB connection limits bite at scale — pooling math is interview gold.",
      "EF Core DbContext pooling? — Reuse context internals — not same as ADO pool.",
      "Connection string per request? — Always from config — rotate via Key Vault.",
      "Exhaust DTU with connection storm",
      "New connection string per tenant uncapped",
      "Ignore pool metrics in AKS scale events"),
    q("Serverless Cold Start", "Serverless", "Very Common",
      "What causes serverless cold start and how mitigate for latency-sensitive APIs?",
      "Cold start: provision runtime, load dependencies, JIT (.NET), VPC attach. Mitigate: always-on instances, smaller packages, managed identity, premium plan, warmup pings, avoid huge DI graph.",
      "**.NET Azure Functions:**\n- **Premium plan** — pre-warmed instances\n- **ReadyToRun** compilation\n- Trim dependencies\n- Avoid VPC unless needed — ENI attach adds seconds\n\n**Lambda:** Provisioned concurrency, ARM Graviton, smaller handler.\n\n**Architect:** If p99 < 200ms SLA — serverless may need premium tier or container always-on.",
      "Cold start is architecture constraint — not just 'use serverless'.",
      "Consumption plan when? — Sporadic batch — cold OK.",
      ".NET isolated worker? — Faster startup than in-process sometimes.",
      "Serverless for synchronous user-facing without premium",
      "500MB deployment package",
      "Cold start surprise in load test"),
    q("IAM Least Privilege", "Security", "Very Common",
      "How implement least privilege IAM in Azure and AWS?",
      "Grant minimum roles per workload identity — managed identity for app, scoped RBAC (Storage Blob Data Contributor on one container), deny assignments at subscription, regular access reviews, no long-lived keys.",
      "**Azure:**\n- Workload uses **managed identity**\n- `azurerm_role_assignment` scoped to resource group\n- PIM for human admin just-in-time\n\n**AWS:**\n- IAM role per Lambda/EC2 instance profile\n- Policy with specific `arn:aws:s3:::bucket/prefix/*`\n\n**Architect:** Break-glass account separate — audit quarterly. CI validates Terraform IAM policies.",
      "Least privilege is identity architecture — default deny, scope resources.",
      "Permission boundaries? — AWS cap max permissions delegate.",
      "Entra Conditional Access? — Human access layer.",
      "Contributor at subscription for app identity",
      "Shared service principal all environments",
      "Access keys in app settings"),
    q("Landing Zone Interview", "Cloud Architecture", "Very Common",
      "What is a cloud landing zone and what pillars does it include?",
      "Landing zone: governed multi-account/subscription foundation — identity, networking hub-spoke, security baseline, logging, policy guardrails, before workloads land.",
      "**Pillars (CAF/ALZ):**\n1. **Identity** — Entra tenant, RBAC, PIM\n2. **Networking** — Hub VNet, firewall, DNS\n3. **Security** — Defender, Policy, Key Vault\n4. **Governance** — Management groups, tags, budgets\n5. **Operations** — Log Analytics, alert routing\n\n**Azure Landing Zone** — Enterprise-scale reference architecture.\n\n**Architect:** Platform team owns landing zone — app teams deploy into spoke subscriptions.",
      "Landing zone separates platform engineering from app delivery — enterprise interview staple.",
      "Policy as code? — Azure Policy deny public IP on NIC.",
      "Subscription vending? — Automated spoke per team via Terraform.",
      "App teams define own networking ad hoc",
      "No centralized logging subscription",
      "Skip policy guardrails for speed"),
    q("Cost Optimization Interview", "FinOps", "Very Common",
      "Name cloud cost optimization strategies beyond 'turn off VMs'.",
      "Right-sizing, reserved instances/savings plans, spot for batch, storage tiering, autoscale schedules, tagging/chargeback, eliminate zombie resources, serverless for spiky, CDN egress reduction.",
      "**Tactics:**\n- **Azure Advisor** recommendations\n- **Dev/test pricing** for non-prod\n- **Auto-shutdown** schedules\n- **Blob lifecycle** to cool/archive\n- **Commitment discounts** 1–3 year after baseline stable\n\n**Culture:** FinOps dashboard per team — accountability.\n\n**Architect:** Cost is NFR — design review includes $/month estimate like latency.",
      "Cost optimization shows operational maturity — specific levers not generic advice.",
      "Unit economics? — $ per transaction — tie scale to revenue.",
      "Egress cost? — Same-region traffic, CDN, compress payloads.",
      "Reserved instances day one before baseline",
      "No tags — cannot allocate spend",
      "Over-provision 'for safety' forever"),
    q("Hybrid Cloud Connectivity", "Networking", "Common",
      "Design hybrid connectivity between on-prem and Azure.",
      "ExpressRoute (private dedicated) or VPN (IPsec) to hub VNet, DNS resolution for private endpoints, route tables, no hairpin through internet for DB traffic.",
      "**Options:**\n- **ExpressRoute** — predictable latency, SLA, no internet\n- **Site-to-Site VPN** — cheaper, internet dependent\n- **ExpressRoute + VPN** — backup path\n\n**Services:** Azure Arc for on-prem K8s/SQL management plane.\n\n**Architect:** Hub-spoke — on-prem connects to hub only — spokes peer through hub firewall policies.",
      "Hybrid design tests networking and security zoning — not just 'VPN yes'.",
      "Private Link? — PaaS over private IP — no public endpoint.",
      "DNS hybrid? — Conditional forwarders — split brain risk.",
      "Flat network all on-prem and cloud",
      "RDP over public internet for admin",
      "No redundant VPN/ER path"),
    q("Blob vs File Storage", "Storage", "Common",
      "When choose blob storage vs file storage in Azure?",
      "Blob: object storage for unstructured data — images, backups, data lake, static websites. Azure Files: SMB/NFS shared file system — lift-and-shift apps needing file share, legacy ISV.",
      "| | Blob | Azure Files |\n|---|------|-------------|\n| Protocol | REST | SMB/NFS |\n| Use | Objects, analytics | Shared drive |\n| Scale | Massive | Moderate share |\n| Cost | Lower per GB hot | Premium for low latency |\n\n**Architect:** New cloud-native → blob. .NET app with `\\\\server\\share` → Files or refactor to blob SDK.",
      "Storage choice maps to access pattern — object vs POSIX file semantics.",
      "ADLS Gen2? — Blob with hierarchical namespace for Spark.",
      "Lifecycle tiers? — Hot/cool/archive on blob.",
      "Files for static website hosting",
      "Blob when app needs file locking SMB semantics",
      "Ignore egress on frequent small reads"),
    q("Event-Driven Cloud", "Architecture", "Very Common",
      "Design event-driven architecture on Azure.",
      "Producers publish to Event Grid/Service Bus/Event Hubs, consumers idempotent, schema registry, dead-letter queues, outbox from transactional systems.",
      "**Patterns:**\n- **Event notification** — Blob created → Function processes\n- **Event-carried state** — minimal payload + lookup\n- **Choreography** — subscribers react — vs orchestration (Logic Apps)\n\n**Components:** APIM → API → outbox table → publisher → Service Bus topic → multiple subscribers.\n\n**Architect:** Define event contract versioning — additive schema changes only.",
      "Event-driven is decoupling strategy — include failure and ordering semantics.",
      "Service Bus vs Event Hubs? — Messages vs streaming telemetry scale.",
      "Idempotent consumer? — Dedup by eventId store.",
      "Synchronous chain of 6 HTTP calls instead",
      "No dead-letter monitoring",
      "Breaking event schema without version"),
    q("Disaster Recovery Interview", "Reliability", "Very Common",
      "Design disaster recovery for a tier-1 cloud application.",
      "Define RTO/RPO, active-passive or active-active multi-region, automated failover (Traffic Manager/Front Door), regular game days, backup encryption, runbook tested quarterly.",
      "**Tiers:**\n- **Active-passive** — secondary warm standby — lower cost\n- **Active-active** — both regions serve — data sync harder\n\n**Data:** Geo-redundant storage, SQL geo-replication, Cosmos multi-region writes with conflict policy.\n\n**Architect:** DR is tested procedure — backup without restore test is wishful thinking.",
      "DR interview requires RTO/RPO numbers and failover mechanics.",
      "Pilot light? — Minimal secondary — scale on failover.",
      "Chaos engineering? — Inject region failure in staging.",
      "Backup never restored in test",
      "Single region with 'we'll rebuild'",
      "Manual failover untested 18 months"),
    q("RTO RPO Calculation", "Reliability", "Very Common",
      "How calculate and negotiate RTO and RPO with stakeholders?",
      "RPO: max acceptable data loss (time between backups). RTO: max downtime. Derive from business impact $/hour, then map to architecture cost.",
      "**Example:**\n- E-commerce checkout: RPO 0, RTO 15 min → sync replication, hot standby\n- Internal HR portal: RPO 24h, RTO 8h → nightly backup, rebuild VM\n\n**Calculation:**\n`RPO drives backup frequency`. `RTO drives hot/warm/cold standby`.\n\n**Cost:** RPO 0 multi-region active-active costs 2×+ infra.\n\n**Architect:** Document tier per application — not one SLA for entire estate.",
      "RTO/RPO links business to engineering — quantify don't hand-wave.",
      "WRT RPO? — Work Recovery Time — process not just tech.",
      "Compliance minimum? — HIPAA may mandate — floor not ceiling.",
      "Same RTO for all apps",
      "RPO zero without budget discussion",
      "Confuse backup retention with RPO"),
    q("Compliance Cloud Interview", "Compliance", "Common",
      "How address compliance (SOC2, HIPAA, GDPR) in cloud architecture?",
      "Shared responsibility model, data residency region selection, encryption at rest/transit, audit logging, DPA with vendor, access controls, retention policies, breach notification process.",
      "**Controls:**\n- **SOC2** — change management, logging, access reviews\n- **HIPAA** — BAA, PHI encryption, audit trails\n- **GDPR** — lawful basis, right to erasure, DPO\n\n**Azure:** Policy initiatives, Compliance Manager score, private endpoints.\n\n**Architect:** Compliance by design in landing zone — app teams inherit baseline controls.",
      "Compliance is shared responsibility — know what cloud provides vs you configure.",
      "Data residency? — EU data in EU region — no silent replication US.",
      "Encryption customer-managed keys? — CMK in Key Vault for regulated.",
      "Assume cloud cert means app compliant",
      "PII in logs unredacted",
      "No data processing agreement"),
    q("Vendor Lock-In Response", "Strategy", "Common",
      "How respond to vendor lock-in concerns in architecture review?",
      "Acknowledge trade-off: managed services accelerate delivery but increase switch cost. Mitigate with portable interfaces (Kubernetes, Postgres), open formats (Parquet, OAuth), exit plan for tier-0 data, avoid proprietary DSL deep in core.",
      "**Mitigation levels:**\n1. **Accept lock-in** — Cosmos DB — document business value\n2. **Abstraction** — S3-compatible API wrapper\n3. **Multi-cloud** — only where regulatory demands\n\n**Interview answer:** 'We optimize for time-to-market on Azure with open API contracts and data export quarterly tested.'\n\n**Architect:** Lock-in risk register — severity × likelihood.",
      "Balanced lock-in answer shows pragmatism — neither zealot portable nor blind native.",
      "Open source on cloud? — Still ops lock-in on managed K8s.",
      "Data portability test? — Quarterly export to neutral format.",
      "Fear of all managed services",
      "Proprietary format core business data no export",
      "Lock-in ignored in ADR"),
    q("Cloud Architecture Whiteboard", "Interview Skills", "Very Common",
      "How structure a cloud architecture whiteboard interview in 45 minutes?",
      "Requirements 5 min → high-level diagram 10 min → deep dive critical path 15 min → security/ops 10 min → trade-offs and evolution 5 min. Label managed services, data flows, trust boundaries.",
      "**Layers to draw:**\n1. Users/CDN/WAF\n2. API gateway\n3. Compute (App Service/AKS)\n4. Data (SQL, cache, blob)\n5. Messaging\n6. Identity, Key Vault\n7. Observability\n\n**Say aloud:** 'I'll use Azure Front Door for global entry...'\n\n**Architect:** Call out single points of failure and how you'd eliminate them in phase 2.",
      "Whiteboard pacing separates prepared candidates — practice timed mocks.",
      "C4 model? — Context then container — don't drill components too early.",
      "Cost mention? — Rough monthly estimate impresses.",
      "Jump to EC2 instances without requirements",
      "Single box 'database' unlabeled",
      "Ignore security and monitoring until asked"),
]

WEEK_47_Q006_Q030 = [
    q("RESHADED Walkthrough", "System Design", "Very Common",
      "Walk through the RESHADED framework for system design interviews.",
      "RESHADED: Requirements, Estimation, Storage, High-level design, APIs, Deep dive, Edge cases, Distinctive points. Structured 45-min flow interviewers expect.",
      "**Steps:**\n1. **Requirements** — functional + NFR (scale, latency, consistency)\n2. **Estimation** — QPS, storage, bandwidth — back-of-envelope\n3. **Storage** — schema, DB choice, sharding\n4. **High-level design** — boxes and arrows\n5. **APIs** — key endpoints, contracts\n6. **Deep dive** — bottleneck component (feed fanout, cache)\n7. **Edge cases** — failures, hot keys, duplicates\n8. **Distinctive** — trade-offs, evolution, monitoring\n\n**Architect:** RESHADED keeps you from jumping to Redis without clarifying read/write ratio.",
      "Framework shows interview discipline — adapt naming but keep structure.",
      "Similar frameworks? — REPE / FAST — same intent.",
      "When skip estimation? — Never — even rough orders of magnitude.",
      "Random component list no requirements",
      "Deep dive before high-level",
      "Ignore NFR until end"),
    q("Requirements Clarification", "System Design", "Very Common",
      "What requirements must you clarify before designing a system?",
      "Functional scope, users/DAU, read vs write ratio, latency p99, consistency needs, durability, geographic distribution, growth, and explicit out-of-scope.",
      "**Questions to ask:**\n- Mobile + web? Anonymous users?\n- Real-time or eventual OK?\n- Strong consistency for payments?\n- Retention period (affects storage cost)\n- Multi-tenant or consumer?\n\n**Document assumptions:** 'Assume 10M DAU, 100:1 read/write, 200ms p99 feed load.'\n\n**Architect:** Clarification prevents designing Twitter when they wanted internal admin tool.",
      "Requirement questions signal senior judgment — interviewers reward this phase.",
      "Out of scope? — Explicitly state 'not designing ML ranking v2'.",
      "NFR priority? — Ask which matters most if conflict.",
      "Assume requirements silently",
      "Over-engineer without scale numbers",
      "Skip consistency discussion"),
    q("Scale Estimation Interview", "System Design", "Very Common",
      "Perform back-of-envelope scale estimation for 10M DAU social app.",
      "DAU 10M, 10 actions/day = 100M ops/day ≈ 1.2K QPS average, 3–5K peak. 1KB per post × 10M posts/day = 10GB/day storage growth before replication.",
      "**Template:**\n- Ops/day = DAU × actions\n- QPS = ops/day / 86400 × peak factor 3\n- Storage = objects/day × size × retention\n- Bandwidth = QPS × payload size\n\n**Example:**\n- 100M reads, 1M writes → cache wins\n- 3-year retention → plan TB archive tier\n\n**Architect:** Estimation drives SQL vs NoSQL, shard count, CDN need — show math on whiteboard.",
      "Estimation doesn't need precision — orders of magnitude and assumptions stated.",
      "Power of two? — 1M seconds ≈ 11.5 days — quick mental math.",
      "Wrong by 10×? — OK if you note sensitivity.",
      "No math at all",
      "Memorized numbers without derivation",
      "Ignore peak factor"),
    q("API Design Interview", "System Design", "Very Common",
      "Design REST APIs for a URL shortener — key endpoints and considerations.",
      "`POST /urls` create short link, `GET /{code}` redirect, `GET /urls/{id}/stats` analytics. Idempotent create with client key, 301 vs 302, rate limits, auth for management.",
      "**Design:**\n```\nPOST /v1/urls  { longUrl, customAlias?, ttl? } → { shortUrl, id }\nGET  /{code}    → 302 Location (track click async)\nGET  /v1/urls/{id}/analytics → { clicks, referrers }\n```\n\n**Considerations:**\n- Version prefix `/v1`\n- Pagination on list endpoints\n- ProblemDetails errors\n- API keys for create, public redirect\n\n**Architect:** Separate read (redirect) hot path from write API — different scale profiles.",
      "API design shows product thinking — not just CRUD names.",
      "GraphQL when? — Complex clients — shortener doesn't need it.",
      "gRPC internal? — Redirect edge HTTP, internal gRPC fine.",
      "Unversioned breaking changes",
      "Analytics on synchronous redirect path",
      "No rate limit on create"),
    q("Database Schema Design", "System Design", "Very Common",
      "Design database schema for e-commerce orders.",
      "Tables: customers, products, orders, order_items, payments. Normalize OLTP, index (customer_id, created_at), avoid duplicating product price — snapshot at order time.",
      "**Schema:**\n```\norders(id, customer_id, status, total, created_at)\norder_items(order_id, product_id, qty, unit_price_snapshot)\npayments(order_id, provider, status, amount)\n```\n\n**Decisions:**\n- Snapshot price in order_items — historical accuracy\n- Status enum — state machine documented\n- Soft delete vs archive\n\n**Architect:** Identify aggregate root — Order owns items — transactional boundary.",
      "Schema design tests normalization vs read optimization balance.",
      "JSON columns? — Flexible attributes — index carefully.",
      "UUID vs bigint PK? — UUID for distributed ID gen.",
      "Store only product_id — price changes break history",
      "God table orders+items+payments",
      "No index on customer order history query"),
    q("Caching Layer Design", "System Design", "Very Common",
      "Design caching layer for read-heavy product catalog.",
      "CDN for static assets, Redis cache-aside for product JSON by ID, local in-process L1 for top 1K SKUs, TTL 5–15 min, invalidate on product update event.",
      "**Layers:**\n1. **CDN** — images, static JSON bundles\n2. **Redis cluster** — `product:{id}` hash\n3. **App L1** — MemoryCache 30s for hot items\n\n**Invalidation:** ProductUpdated event → cache delete + CDN purge API.\n\n**Architect:** Measure hit rate target 90%+. Stampede protection on popular product drop.",
      "Multi-tier cache is standard senior answer — specify invalidation.",
      "Cache key design? — Include locale, version — `product:us:123:v2`.",
      "Negative caching? — Cache 404 briefly — prevent DB hammer.",
      "Infinite TTL on inventory counts",
      "Single Redis no HA",
      "Cache without miss path to DB"),
    q("Load Balancer Selection", "System Design", "Very Common",
      "When use L4 vs L7 load balancer?",
      "L4 (TCP): ultra-high throughput, pass-through TLS, gaming, DB proxy. L7 (HTTP): path-based routing, TLS termination, sticky sessions, WAF integration, gRPC/HTTP2.",
      "**L7 (ALB, Azure App Gateway):**\n- Route `/api` vs `/static`\n- Health checks on HTTP path\n- SSL offload\n\n**L4 (NLB, Azure Load Balancer):**\n- Millions RPS, static IP, preserve client IP\n\n**Architect:** Public entry often L7 + WAF; internal microservice mesh may use L4 sidecar.",
      "LB choice ties to routing needs — not 'always ALB'.",
      "Sticky sessions? — Session affinity — prefer stateless JWT.",
      "Global LB? — Front Door, CloudFront, Route53 latency routing.",
      "L7 when only TCP needed — unnecessary parsing cost",
      "No health checks",
      "Single LB SPOF no multi-AZ"),
    q("CDN Design", "System Design", "Very Common",
      "Design CDN usage for global media streaming startup.",
      "Origin in S3/Blob, CloudFront/Azure CDN PoPs, cache-control headers, signed URLs for premium content, origin shield, separate cache behaviors for video segments vs thumbnails.",
      "**Design:**\n- **HLS/DASH segments** — long cache, immutable URLs with version\n- **Thumbnails** — shorter TTL\n- **Signed cookies** — prevent hotlinking\n- **Origin shield** — reduce origin load\n\n**Architect:** 90%+ egress from CDN not origin — monitor cache hit ratio.",
      "CDN design includes security (signed URLs) and cache semantics.",
      "Cache invalidation? — Version in URL path — preferred over purge.",
      "Dynamic content CDN? — Edge workers limited — mostly static.",
      "CDN for uncacheable personalized API",
      "No TTL strategy",
      "Origin exposed publicly bypassing CDN"),
    q("Message Queue Design", "System Design", "Very Common",
      "Design message queue integration for order processing.",
      "Order API writes DB + outbox, publisher sends to queue, workers process payment/shipping idempotently, DLQ for failures, visibility timeout, poison message handling.",
      "**Flow:**\n```\nAPI → DB txn (order + outbox) → relay → Service Bus queue\n→ workers (competing consumers) → external APIs\n```\n\n**Choices:**\n- Ordering: partition by orderId\n- At-least-once + idempotent consumer\n- DLQ alert + replay tooling\n\n**Architect:** Backpressure — queue depth metric autoscales workers.",
      "Queue design requires delivery semantics and failure paths.",
      "Queue vs stream? — Kafka when replay needed; queue for task distribution.",
      "Priority queue? — Separate queues premium vs standard.",
      "Fire-and-forget HTTP no persistence",
      "No DLQ",
      "Exactly-once without idempotency plan"),
    q("Microservices Decomposition", "System Design", "Very Common",
      "How decompose monolith into microservices?",
      "By bounded context (DDD), align to team topology, split on change frequency and scale profile, strangler fig migration, avoid distributed monolith shared DB.",
      "**Criteria:**\n- **Business capability** — payments, catalog, notifications\n- **Independent deploy** — separate CI/CD\n- **Different scale** — search vs checkout\n\n**Anti-pattern:** 50 services sharing one database.\n\n**Architect:** Start with modular monolith — extract when pain proven by metrics.",
      "Decomposition by org and domain — not entity-per-service.",
      "Conway's law? — Team boundaries shape service boundaries.",
      "Shared library vs service? — Duplicate small code OK — avoid shared DB.",
      "Microservice per database table",
      "Distributed monolith synchronous chain",
      "Split before identifying bounded contexts"),
    q("Saga Interview", "System Design", "Very Common",
      "Explain saga pattern for distributed order checkout.",
      "Sequence of local transactions with compensating actions on failure — choreography (events) or orchestration (central coordinator).",
      "**Orchestrated saga:**\n1. Reserve inventory → OK\n2. Charge payment → FAIL → compensate: release inventory\n3. Create shipment — skipped\n\n**Choreography:** Each service listens and publishes — OrderCreated → PaymentRequested.\n\n**Architect:** Orchestration easier to debug; choreography looser coupling. Store saga state machine durable (SQL, Temporal).",
      "Saga replaces 2PC — interviewers want compensation examples.",
      "Outbox? — Reliable event publish with local txn.",
      "Duplicate saga step? — Idempotent handlers + sagaId.",
      "Distributed two-phase commit across clouds",
      "No compensation defined",
      "Saga state only in memory"),
    q("Circuit Breaker Design", "System Design", "Very Common",
      "Design circuit breaker for payment service dependency.",
      "States: closed (normal), open (fail fast), half-open (probe). Open after 5 failures in 30s, half-open after 60s, close after 3 successes.",
      "**Implementation:**\n- Polly / Istio / resilience4j\n- Fallback: queue payment retry job, show 'payment pending' UX\n- Monitor breaker state metric\n\n**Architect:** Breaker protects your SLA when vendor down — don't retry storm into failing service.",
      "Circuit breaker is failure isolation — pair with timeout and bulkhead.",
      "Bulkhead? — Separate thread pool per dependency.",
      "Half-open thundering herd? — Single probe request.",
      "Infinite retry to failing payment API",
      "No fallback UX",
      "Breaker never monitored"),
    q("Rate Limiter Design", "System Design", "Very Common",
      "Design distributed rate limiter — 100 req/min per user.",
      "Token bucket or sliding window in Redis with Lua atomicity, key `ratelimit:{userId}`, return 429 + Retry-After, optional global limit per API key.",
      "**Algorithm:**\n```\nSliding window log in Redis ZSET\nOR token bucket: tokens += rate * dt, cap burst\n```\n\n**Placement:** API gateway edge — reject before app tier.\n\n**Architect:** Different limits per tier (free vs paid). Log rate limit hits for abuse detection.",
      "Distributed rate limiter is classic design — Redis + atomic ops.",
      "Local vs global? — Per-node limit unfair — use Redis.",
      "Fair queuing? — Advanced — weighted shares.",
      "In-memory limit per server only",
      "Race condition without atomic Redis script",
      "No 429 response headers"),
    q("Notification System Design", "System Design", "Very Common",
      "High-level design for multi-channel notification system (email, SMS, push).",
      "Event ingestion → template service → priority queues per channel → workers with provider adapters → delivery status webhook → user preference filter.",
      "**Components:**\n- Notification API / event consumer\n- Template store (i18n)\n- User preferences (opt-out per channel)\n- Queue per channel (SMS rate limited)\n- Provider abstraction (SendGrid, Twilio, FCM)\n- Idempotency key per notification\n\n**Scale:** Millions/day — batch digest emails separately from transactional OTP.",
      "Notification design hits fanout, preferences, and provider failure — classic interview.",
      "Priority? — OTP > marketing — separate queues.",
      "Quiet hours? — Scheduler delays non-urgent.",
      "Synchronous send in API request",
      "No unsubscribe compliance",
      "Single queue all channels"),
    q("URL Shortener Design", "System Design", "Very Common",
      "Design URL shortener supporting 100M URLs and 10K redirects/sec.",
      "Base62 encode ID or hash, SQL/NoSQL store mapping, Redis cache hot codes, read replicas, 302 redirect, analytics async via Kafka.",
      "**ID generation:** Counter shard or Snowflake — avoid collision.\n**Read path:** Cache → DB replica → 302.\n**Write path:** Primary DB, populate cache.\n\n**Storage:** 100M × 500B ≈ 50GB — single shard OK initially.\n\n**Architect:** Separate redirect service minimal deps — ultra-low latency.",
      "URL shortener tests read-heavy optimization and ID generation.",
      "Custom alias collision? — Unique constraint, 409 conflict.",
      "Expired URLs? — TTL job or lazy check.",
      "MD5 truncate collision prone",
      "Analytics on redirect critical path",
      "No cache for hot links"),
    q("News Feed Design", "System Design", "Very Common",
      "Design news feed for 10M users — fanout on write vs read?",
      "Hybrid: fanout on write for normal users (precompute timeline in Redis), fanout on read for celebrities (millions followers). Ranked feed optional ML tier async.",
      "**Write fanout:** Post created → push postId to followers' feed lists (cap 1000 recent).\n**Read:** Pull merged timeline, rank, paginate.\n\n**Celebrity:** Followers fetch from celebrity post list at read time.\n\n**Storage:** User feed cache (Redis), posts table (Cassandra/SQL).\n\n**Architect:** Define consistency — follower may see post few seconds late OK.",
      "Feed fanout trade-off is THE news feed interview topic.",
      "Ranking? — Score = time decay + engagement — precompute or realtime.",
      "Pagination? — Cursor not offset — stable under new posts.",
      "Fanout on write for 50M follower account",
      "No feed cap unbounded memory",
      "Pull all posts global on read"),
    q("Chat System Design", "System Design", "Very Common",
      "Design real-time chat for 1M concurrent connections.",
      "WebSocket gateway cluster, message service, conversation store (Cassandra by conversationId), presence service, push for offline, sequence numbers per conversation.",
      "**Flow:**\n- Client WS to gateway (sticky or user routing table)\n- Message → queue → persist → fanout to recipient gateways\n- Delivery receipts async\n\n**History:** Paginate by sequenceId. **Groups:** fanout to member list.\n\n**Architect:** Partition by conversationId. Multi-region: CRDT or last-write-wins policy documented.",
      "Chat combines real-time, ordering, and presence — state connection routing.",
      "WebSocket vs SSE? — Bidirectional needs WS.",
      "E2E encryption? — Out of scope or key exchange layer.",
      "HTTP poll every second",
      "No message ordering guarantee",
      "Single WS server no scale plan"),
    q("Search System Design", "System Design", "Very Common",
      "Design product search for e-commerce.",
      "Ingest pipeline (CDC) → Elasticsearch index, search API with filters/facets, autocomplete from edge n-grams, spell correction, ranking by relevance + business rules.",
      "**Components:**\n- Catalog changes → Kafka → indexer\n- ES cluster sharded by category\n- Search API: query parsing, pagination, aggregations\n- Cache popular queries\n\n**Architect:** Search is eventually consistent with catalog — lag SLA 1–5 min acceptable.",
      "Search is inverted index + pipeline — not SQL LIKE.",
      "Personalization? — Boost user brand affinity — separate ranker.",
      "Zero results? — Synonyms, fuzzy match fallback.",
      "SQL LIKE on product name",
      "Synchronous index on every write to ES in API",
      "No facet/filter design"),
    q("File Storage Design", "System Design", "Very Common",
      "Design file upload/storage for Dropbox-like service.",
      "Client uploads to blob via presigned URL, metadata DB (fileId, userId, chunks, hash), dedup by content hash, CDN download, multipart for large files, virus scan async.",
      "**Upload:**\n1. Client requests upload URL\n2. Direct PUT to S3/Blob\n3. Callback registers metadata\n4. Chunking for >100MB resume\n\n**Dedup:** SHA-256 block-level saves storage.\n\n**Architect:** Never proxy large files through API servers — direct to object storage.",
      "File storage design emphasizes presigned URLs and metadata separation.",
      "Conflict resolution? — Version vector or last-write-wins.",
      "Sharing links? — ACL table + signed download URL.",
      "Files through app server memory",
      "No multipart resume",
      "Metadata and blob orphan risk on failed callback"),
    q("Distributed Cache", "System Design", "Very Common",
      "Design distributed cache cluster for 100GB working set.",
      "Redis Cluster or Memcached consistent hash, replication for HA, client-side hashing, TTL policy, monitor evictions and hot keys.",
      "**Sizing:** 100GB + 30% overhead = 130GB RAM across 6 nodes.\n**HA:** Primary-replica per shard, auto-failover.\n**Hot key:** Local replica read, or key splitting `user:123:part{n}`.\n\n**Architect:** Cache is not source of truth — define cold-start warmup.",
      "Distributed cache ops: sharding, eviction, hot keys — all fair game.",
      "Redis vs Memcached? — Redis richer data structures; Memcached simpler.",
      "Near cache? — Client library L1 in app pod.",
      "Single Redis instance production",
      "No eviction policy",
      "Cache stampede unmitigated"),
    q("ID Generation Snowflake", "System Design", "Very Common",
      "Explain Snowflake ID generation and trade-offs.",
      "64-bit: timestamp + machineId + sequence — sortable, unique without coordination, ~4096 IDs/ms per machine. Needs clock sync and machine ID assignment.",
      "**Layout (Twitter Snowflake):** 41b timestamp | 10b node | 12b sequence.\n\n**Pros:** Time-ordered, DB index friendly, no central DB bottleneck.\n**Cons:** Clock rollback handling, machine ID management in K8s.\n\n**Alternatives:** UUID v4 (random, not sortable), DB sequence (bottleneck), ULID.\n\n**Architect:** Snowflake for high-write sharded OLTP; UUID for client-generated offline IDs.",
      "ID generation appears in every high-scale design — know Snowflake layout.",
      "Clock skew? — Wait or use logical clock increment.",
      "K8s pod machineId? — Assign from StatefulSet ordinal or coordination service.",
      "Auto-increment single DB at 100K writes/sec",
      "UUID v1 MAC leakage concern ignored",
      "Duplicate machine IDs"),
    q("Consistency Models Interview", "Distributed Systems", "Very Common",
      "Compare strong, eventual, and causal consistency with examples.",
      "Strong: read sees latest write (SQL primary read). Eventual: replicas converge (DNS, Dynamo). Causal: preserve cause ordering (social comments thread).",
      "**Examples:**\n- **Bank balance** — strong or linearizable\n- **Like count** — eventual OK\n- **Chat order** — causal or per-partition ordering\n\n**Cosmos:** Tunable consistency per request.\n\n**Architect:** Pick per operation not per system — payment strong, analytics eventual.",
      "Consistency model literacy avoids one-size-fits-all CAP hand-waving.",
      "Read-your-writes? — Session consistency — common UX need.",
      "Linearizability cost? — Coordination latency cross-region.",
      "Strong everywhere unnecessarily",
      "Eventual for inventory deduction",
      "No user-visible consistency definition"),
    q("Partition Strategy", "System Design", "Very Common",
      "Choose partition key for IoT time-series telemetry.",
      "Partition by (deviceId, date) or deviceId hash — avoid hot partition on single device flood, enable time-range pruning, consider Timescale/Cosmos partition key design.",
      "**Strategies:**\n- **deviceId** — even if devices equal rate\n- **deviceId + hour** — if single device very hot\n- **Geo region** — compliance + locality\n\n**Anti-pattern:** Partition only by timestamp — all writes one partition.\n\n**Architect:** Validate with peak device telemetry rate estimate.",
      "Partition key choice is irreversible pain — interview focus area.",
      "Repartition migration? — Dual-write new shard scheme — expensive.",
      "Synthetic key? — Salt hot keys `{deviceId}#{random 0-9}`.",
      "Timestamp-only partition key",
      "Cross-partition queries in hot path",
      "Ignore storage growth per partition limit"),
    q("Hot Key Problem", "System Design", "Very Common",
      "How solve hot key problem in distributed cache or database?",
      "Key splitting, local in-process cache replica, read replicas with random fanout, pre-warm, combine request coalescing, or application-level aggregation.",
      "**Techniques:**\n1. **Split key** — `popularProduct:{0..N}` round-robin\n2. **Local cache** — 1s TTL on app node for scorching key\n3. **Read replica random** — spread reads\n4. **Coalescing** — single flight pattern — one DB hit\n\n**Celebrity post:** Fanout on read not write.\n\n**Architect:** Monitor per-key QPS — alert threshold.",
      "Hot key is production war story — multiple mitigations show experience.",
      "Redis hot key? — Redis Cluster slot still one node — split key.",
      "Write hot key? — Queue serialization — shard writers.",
      "Ignore viral event scenario",
      "Only vertical scale bigger Redis",
      "No monitoring per-key metrics"),
    q("System Design Rubric Self-Check", "Interview Skills", "Very Common",
      "Self-check rubric before ending system design interview.",
      "Did I clarify requirements, estimate scale, draw diagram, define APIs, deep dive bottleneck, address failures, security, monitoring, and trade-offs?",
      "**Checklist (score 0–2 each):**\n- Requirements & assumptions stated\n- Back-of-envelope math\n- Clear component diagram\n- Data model / storage choice justified\n- API contracts sketched\n- Deep dive on critical path\n- Single points of failure addressed\n- Caching / async where appropriate\n- Security (auth, encryption)\n- Observability (metrics, logs, alerts)\n- Evolution path mentioned\n\n**Architect:** 16+ / 22 points — strong loop. Leave 3 min for questions.",
      "Self-check prevents leaving easy points on table — practice with rubric.",
      "Weak deep dive? — Pick cache or queue — always have go-to.",
      "Time box? — Move on if stuck 5 min — breadth beats perfect depth.",
      "End without trade-off summary",
      "Never mention monitoring",
      "No assumptions documented"),
]

WEEK_48_Q006_Q030 = [
    q("Notification System Mock", "Whiteboard Mock", "Very Common",
      "45-minute mock: design notification system. What do you cover in each phase?",
      "0–5 req, 5–15 HLD (ingest, queue, workers, templates), 15–30 deep dive channel fanout + idempotency, 30–40 failures/DLQ, 40–45 trade-offs.",
      "**Mock script:**\n- Clarify channels, volume (1M/day), latency (OTP <10s)\n- Draw: API → Service Bus → worker pool → providers\n- Deep dive: priority queues, user prefs, retry\n- Mention: CAN-SPAM opt-out, rate limits per provider\n\n**Self-score:** Did you ask about transactional vs marketing?",
      "Timed mock builds muscle memory — notification is high-frequency interview prompt.",
      "Follow-up trap? — Exactly-once delivery — honest at-least-once + idempotency.",
      "Scale 1B/day? — Partition queues, batch email.",
      "Spend 25 min on email template HTML",
      "Skip idempotency",
      "No provider failure handling"),
    q("E-Commerce Cart Mock", "Whiteboard Mock", "Very Common",
      "Mock: design shopping cart for flash sale — 50K concurrent add-to-cart.",
      "Cart service Redis-backed per userId, inventory reservation separate service, optimistic concurrency, queue checkout, CDN for product static data.",
      "**Key points:**\n- Cart: `HSET cart:{userId}` TTL 7 days\n- Inventory: reserve with timeout — saga on checkout\n- Flash sale: token queue or virtual waiting room\n- Don't hammer DB — cache product availability snapshot with short TTL\n\n**Time:** 5 min reqs, 10 min diagram, 15 min inventory deep dive.",
      "Cart mock tests concurrency and inventory — classic e-commerce stress.",
      "Oversell prevention? — Atomic decrement or reservation tokens.",
      "Guest cart merge? — On login merge Redis keys.",
      "Cart in SQL row per session at 50K RPS",
      "No inventory reservation timeout",
      "Ignore flash sale queue"),
    q("Queue Design Mock", "Whiteboard Mock", "Very Common",
      "Mock: design task queue for image processing — 10K uploads/hour.",
      "Upload to blob, metadata queue, workers scale on queue depth, priority lane for paid users, DLQ, result callback webhook.",
      "**Architecture:**\n- S3 event → SQS → auto-scaling worker pool (GPU optional)\n- Visibility timeout > max processing time\n- Status API polled or webhook on complete\n\n**Deep dive:** Poison image crashing worker — DLQ + maxReceiveCount.",
      "Queue mock — emphasize autoscale signal and visibility timeout math.",
      "FIFO needed? — Only if strict order per user album.",
      "Exactly-once processing? — Idempotent output keys by uploadId.",
      "Sync processing in upload API",
      "Visibility timeout 30s for 5min job",
      "No DLQ"),
    q("API Pagination Design", "Whiteboard Mock", "Very Common",
      "Mock: design pagination for activity feed API — stable under concurrent writes.",
      "Cursor-based pagination with `(created_at, id)` tuple, not offset. `?cursor=eyJ...` opaque token, `limit=20`, `hasMore` flag.",
      "**Why not offset:** New items shift pages — duplicates/skips.\n\n**Cursor:** `WHERE (created_at, id) < (@ts, @id) ORDER BY created_at DESC, id DESC LIMIT 21`\n\n**Mock tip:** State default limit max 100, rate limit list endpoint.",
      "Pagination mock is quick win if you know cursor vs offset cold.",
      "Bidirectional? — prev_cursor for infinite scroll up.",
      "Keyset on UUID? — Use indexed tuple not OFFSET.",
      "OFFSET pagination on live feed",
      "Expose internal DB id unsorted",
      "No max page size"),
    q("Rate Limiting Mock", "Whiteboard Mock", "Very Common",
      "Mock: rate limit public API 1000 req/hour per API key with burst.",
      "Token bucket in Redis, key per API key, burst 50, refill 1000/3600 per sec, 429 + Retry-After, gateway enforcement.",
      "**Whiteboard:**\n```\nClient → API GW → [Rate Limit Check Redis] → Service\n```\n\n**Discuss:** Global vs per-endpoint limits, DDoS at IP layer (WAF) separate from plan quota.\n\n**5 min:** Draw algorithm. **10 min:** Redis atomicity Lua script.",
      "Rate limit mock should fit in 15 min deep dive — don't over-engineer.",
      "Sliding window vs token bucket? — Mention both — pick one.",
      "Distributed denial? — Edge WAF before app rate limit.",
      "Per-server in-memory only",
      "No burst allowance UX",
      "Forget API key auth context"),
    q("Authentication System Mock", "Whiteboard Mock", "Very Common",
      "Mock: design auth for B2B SaaS — SSO, API keys, MFA.",
      "Entra ID/Okta SAML/OIDC for SSO, JWT access + refresh rotation, API keys hashed in DB, MFA via TOTP, RBAC per tenant, session revocation list.",
      "**Components:**\n- Identity provider federation\n- Auth service issues JWT (short TTL 15m)\n- Refresh token httpOnly cookie rotation\n- API keys for automation scoped per tenant\n\n**Security:** PKCE for SPA, no tokens in localStorage if avoidable.",
      "Auth mock spans human SSO and machine API keys — clarify both.",
      "Tenant isolation? — tenantId claim validated every request.",
      "Password storage? — bcrypt/Argon2 — if local auth at all.",
      "JWT forever no refresh",
      "API key plaintext in DB",
      "No MFA for admin"),
    q("Logging Aggregation Mock", "Whiteboard Mock", "Very Common",
      "Mock: centralized logging for 200 microservices.",
      "Structured JSON logs, OpenTelemetry collector, Kafka buffer, Elasticsearch/OpenSearch index, retention tiers, correlation ID, PII scrubbing pipeline.",
      "**Flow:**\nApp → sidecar/agent → collector → Kafka → indexer → Kibana/Grafana\n\n**Discuss:** Log volume cost — sample debug in prod, hot/warm/cold indices.\n\n**Alert:** Error rate anomaly on `service.name`.",
      "Logging mock — mention cost and PII not just 'send to ELK'.",
      "Log levels in prod? — Info default, debug sampled.",
      "Cardinality explosion? — Avoid high-cardinality labels in metrics/logs.",
      "Unstructured printf logs",
      "No correlation ID standard",
      "Infinite retention all logs"),
    q("Metrics System Mock", "Whiteboard Mock", "Very Common",
      "Mock: metrics and alerting for payment platform.",
      "Prometheus scrape or OTLP push, RED metrics (rate, errors, duration), business metrics (payment success $), Alertmanager PagerDuty, SLO burn alerts.",
      "**Key metrics:**\n- `http_server_duration_seconds` histogram\n- `payment_success_total` counter by method\n- Queue depth, DB connection pool\n\n**SLO:** 99.9% success → error budget policy.\n\n**Dashboard:** Golden signals per service.",
      "Metrics mock ties technical RED to business KPIs.",
      "Histogram vs summary? — Histogram for percentile aggregation in Prometheus.",
      "Alert fatigue? — SLO-based not every blip.",
      "Only CPU monitoring",
      "No business success metric",
      "1000 static threshold alerts"),
    q("Deployment Architecture Mock", "Whiteboard Mock", "Very Common",
      "Mock: zero-downtime deployment for API on Kubernetes.",
      "Rolling update maxUnavailable 0, readiness probe, preStop hook, blue-green or canary with service mesh weights, DB migrations backward compatible.",
      "**Diagram:**\n- CI builds image → registry\n- ArgoCD sync\n- K8s Deployment rolling\n- Ingress canary 10% → 100%\n\n**Discuss:** Rollback via previous image tag, feature flags decouple deploy from release.",
      "Deployment mock connects K8s mechanics to schema compatibility.",
      "Readiness vs liveness? — Readiness removes from LB during startup.",
      "Database migration job? — Init container or Job before traffic.",
      "Recreate deployment strategy production",
      "Breaking schema same deploy as code",
      "No health probes"),
    q("Multi-Tenant Mock", "Whiteboard Mock", "Very Common",
      "Mock: multi-tenant SaaS data isolation strategy.",
      "Options: shared DB row-level tenantId, schema-per-tenant, DB-per-tenant enterprise. Start shared + RLS, premium isolated DB.",
      "**Comparison:**\n| Model | Cost | Isolation |\n|-------|------|----------|\n| Shared table | Low | RLS + app filter |\n| Schema/tenant | Medium | Better |\n| DB/tenant | High | Strongest |\n\n**Must:** tenantId in every query, JWT claim validation, no cross-tenant cache keys.",
      "Multi-tenant mock — pick model and justify for given customer mix.",
      "Noisy neighbor? — Per-tenant rate limits and query timeouts.",
      "Compliance tenant? — Dedicated DB + VPC for one client.",
      "Forget tenantId in cache key",
      "Only app-level filter no DB RLS",
      "One size for SMB and Fortune 500"),
    q("Read Replica Mock", "Whiteboard Mock", "Very Common",
      "Mock: when route reads to replica and handle lag.",
      "Analytics and list pages → replica. Post-checkout balance → primary. Session stickiness after write or `read_consistency=strong` query param internal.",
      "**Diagram:** Primary (writes) → async replication → 2+ replicas (reads)\n\n**Monitor lag:** Route away if >5s.\n\n**Mock answer structure:** Classify each endpoint read tolerance.",
      "Read replica mock is consistency classification exercise.",
      "Globally distributed replicas? — Near-user read — lag varies.",
      "Replica failure? — Fallback primary — capacity plan.",
      "All reads replica always",
      "No lag monitoring",
      "User sees stale own payment immediately"),
    q("Write Path vs Read Path", "Whiteboard Mock", "Very Common",
      "Mock: separate write and read paths for high-traffic blog platform.",
      "CQRS-lite: write API → normalized DB → event → projector builds read model (denormalized ES/Redis). Reads never hit write schema.",
      "**Write path:** Validate → DB txn → publish PostCreated\n**Read path:** Query materialized view / cache\n\n**Trade-off:** Eventual consistency seconds on read after publish.\n\n**Mock time:** 10 min draw both paths explicitly.",
      "Write/read split is CQRS gateway — common follow-up in mocks.",
      "When not CQRS? — Low traffic CRUD — keep simple.",
      "Dual write problem? — Outbox not write DB+ES synchronously.",
      "Same fat model read and write at scale",
      "ES updated in API txn synchronously",
      "No projection rebuild plan"),
    q("Event Sourcing When Mock", "Whiteboard Mock", "Common",
      "Mock: when would you use event sourcing for audit-heavy ledger?",
      "When complete audit trail, temporal queries ('balance at date'), and replay are requirements — finance ledger, not simple CRUD blog.",
      "**ES design:**\n- Append-only event store\n- Projections rebuild state\n- Snapshots every N events\n\n**When NOT:** Simple CMS, no audit law, team inexperienced — operational cost high.\n\n**Mock:** State trade-offs clearly — don't ES by default.",
      "Event sourcing mock tests when NOT as much as when.",
      "CQRS required? — Often paired — not mandatory.",
      "Event schema evolution? — Upcasting old events.",
      "Event sourcing for every microservice",
      "No snapshot strategy",
      "Delete events GDPR impossible"),
    q("CQRS Mock Scenario", "Whiteboard Mock", "Very Common",
      "Mock: CQRS for order dashboard with heavy reports.",
      "Command side OLTP normalized, query side denormalized read DB or ES updated by events, separate scale, different indexes optimized per query.",
      "**Commands:** PlaceOrder, CancelOrder — strong consistency\n**Queries:** Sales dashboard, order search — eventual OK\n\n**Sync mechanism:** Outbox → Kafka → read model updater\n\n**Mock pitfall:** Don't say separate DB without sync story.",
      "CQRS mock needs sync mechanism — outbox/event handler.",
      "Read model rebuild? — Replay events — disaster recovery.",
      "Multiple read models? — One per query shape OK.",
      "CQRS with synchronous dual write",
      "Same team owns 12 read models day one",
      "No lag SLA to read side"),
    q("Backpressure Mock", "Whiteboard Mock", "Very Common",
      "Mock: backpressure when downstream email service slows.",
      "Queue buffers, bounded channels, slow consumer detection, pause ingestion, 503 to clients with retry-after, scale workers, circuit breaker to provider.",
      "**Signals:** Queue age growing, consumer lag metric.\n\n**Responses:**\n1. Scale consumers\n2. Shed load — reject non-critical\n3. Circuit open — stop accepting marketing sends\n\n**Mock:** Draw feedback loop producer ← queue depth → throttle.",
      "Backpressure mock shows flow control not unlimited queues.",
      "Reactive streams? — Bounded buffer concept in any stack.",
      "User experience? — Graceful degradation message.",
      "Unbounded queue OOM",
      "Block threads waiting forever",
      "No shedding strategy"),
    q("Timeout Design Mock", "Whiteboard Mock", "Very Common",
      "Mock: timeout strategy for 5-deep microservice call chain.",
      "Budget 300ms total user SLA — allocate per hop, shortest timeout innermost, context deadline propagation, fail fast, avoid serial if parallel possible.",
      "**Example:**\n- Gateway: 300ms total\n- Service A: 250ms ctx\n- Service B: 100ms (called by A)\n\n**Use:** gRPC deadline, Polly timeout, cancellation token .NET.\n\n**Mock mistake:** 30s default HttpClient timeout everywhere.",
      "Timeout budget propagation is staff-level distributed systems detail.",
      "Partial results? — Return degraded response if optional svc timeout.",
      "Retry after timeout? — Only idempotent with capped retry.",
      "Default 100s timeout all calls",
      "No cancellation propagation",
      "Serial chain when parallel possible"),
    q("Retry Design Mock", "Whiteboard Mock", "Very Common",
      "Mock: retry policy for idempotent GET vs payment POST.",
      "GET: retry 3× exponential backoff on 5xx/timeout. POST payment: no retry unless idempotency-key stored — retry only same key returns same result.",
      "**Layers:**\n- Client retry (careful)\n- Gateway retry (idempotent only)\n- Worker retry with DLQ max attempts\n\n**Jitter:** Prevent synchronized retry storm.\n\n**Mock:** Draw idempotency key store lookup before execute.",
      "Retry mock must distinguish safe vs unsafe methods.",
      "Retry-After header? — Honor on 429.",
      "Nested retries? — Multiply attempts — dangerous.",
      "Retry POST payment blindly",
      "Infinite retry on 400",
      "Retry at every layer 3× each"),
    q("Idempotency Mock", "Whiteboard Mock", "Very Common",
      "Mock: idempotent payment API design.",
      "Client sends `Idempotency-Key: UUID`, server stores key → result mapping 24h, duplicate key returns cached response without double charge.",
      "**Flow:**\n1. Receive request + key\n2. If key exists → return stored response\n3. Else process payment, store result atomically\n4. TTL cleanup\n\n**Storage:** Redis or SQL unique constraint on key.\n\n**Mock:** Mention race — two requests same key — DB unique or distributed lock.",
      "Idempotency is payment mock must-have — key header pattern.",
      "Key scope? — Per merchant + key — global uniqueness.",
      "Partial failure? — Store processing state — client retries safely.",
      "No idempotency on charge endpoint",
      "Key only client-side",
      "Race double charge"),
    q("Schema Evolution Mock", "Whiteboard Mock", "Very Common",
      "Mock: evolve event schema without breaking consumers.",
      "Additive changes only (new optional fields), version field in envelope, consumers ignore unknown fields, dual-schema period, contract tests in CI.",
      "**Rules:**\n- Never remove/rename in v1 stream\n- New `schemaVersion: 2` parallel topic optional\n- Upconverters for ES replay\n\n**Mock:** Example add `shippingMethod` optional to OrderCreated.",
      "Schema evolution mock — backward compatible contracts.",
      "Protobuf? — Field numbers never reuse.",
      "Breaking change? — New topic/version consumer group.",
      "Breaking rename field production",
      "No contract tests",
      "Force all consumers deploy same day"),
    q("Versioning API Mock", "Whiteboard Mock", "Very Common",
      "Mock: API versioning strategy for mobile clients slow to update.",
      "URL path `/v1` `/v2` or header `Accept-Version`, maintain v1 minimum 12 months, deprecation sunset headers, feature parity doc.",
      "**Approach:**\n- Path versioning visible in logs — common\n- Breaking: new major only\n- Non-breaking: add fields OK in same version\n\n**Mobile:** Long tail — never force upgrade without grace.",
      "API versioning mock — mobile long tail drives policy.",
      "GraphQL versioning? — Deprecate fields — schema registry.",
      "Internal vs public? — Internal can break faster with coordinated deploy.",
      "Breaking change in minor version",
      "No deprecation timeline",
      "Version only in docs not URL"),
    q("Mobile Offline Sync Mock", "Whiteboard Mock", "Common",
      "Mock: offline-first mobile notes app sync architecture.",
      "Client local SQLite, operation log queue, sync API with vector clocks or last-write-wins per note, conflict UI for merge, background sync on connectivity.",
      "**Server:**\n- `POST /sync` batch upsert with client revision\n- Return conflicts array\n\n**IDs:** Client-generated UUID for offline creates.\n\n**Mock:** State conflict resolution policy explicitly.",
      "Offline sync mock — conflict strategy is the crux.",
      "CRDT? — Auto-merge rich text — advanced mention.",
      "Delta sync? — Only changes since cursor token.",
      "Server-only auto-increment IDs",
      "Last-write-wins no user visibility on conflict",
      "Full dump sync every time"),
    q("Webhook Delivery Mock", "Whiteboard Mock", "Very Common",
      "Mock: reliable webhook delivery to customer endpoints.",
      "Event queue, HMAC signature, exponential retry 24h, DLQ, delivery log UI, idempotent eventId, circuit per subscriber URL.",
      "**Payload:** `{ eventId, type, data, timestamp }`\n**Security:** `X-Signature: HMAC-SHA256(secret, body)`\n\n**Subscriber down:** Retry schedule 1m, 5m, 30m... disable after N days fail.",
      "Webhook mock — signing and retry schedule expected.",
      "Ordering? — Per resource sequence number optional.",
      "Replay? — Customer fetches missed via API by eventId cursor.",
      "Fire HTTP once no retry",
      "No signature verification",
      "No delivery visibility for customer"),
    q("Batch Processing Mock", "Whiteboard Mock", "Common",
      "Mock: nightly batch ETL 500M rows.",
      "Partition input files, Spark/Databricks parallel transform, idempotent writes to warehouse, checkpoint, SLA by 6am, alert on failure, rerun from checkpoint.",
      "**Architecture:**\n- Blob raw zone → Spark cluster → Synapse/BigQuery\n- Partition by date shard parallel\n- Data quality checks gate publish\n\n**Mock:** Estimate runtime with cluster size rough calc.",
      "Batch mock — partition parallelism and idempotent output.",
      "Incremental vs full? — Watermark high since last run.",
      "Failure mid-job? — Checkpoint partition completion.",
      "Single thread 500M rows",
      "Overwrite production no staging",
      "No data quality validation"),
    q("Stream Processing Mock", "Whiteboard Mock", "Common",
      "Mock: real-time fraud detection on transaction stream.",
      "Kafka ingest, Flink/Spark Streaming windowed aggregates, rules + ML model side input, sub-second alert to case management, dead letter for malformed events.",
      "**Windows:** 5-min sliding velocity check\n**State:** RocksDB state store per Flink\n**Output:** Flag → human review queue + block payment API callback\n\n**Mock:** Discuss at-least-once and idempotent alerts.",
      "Stream mock — windowing and state store mention scores well.",
      "Event time vs processing time? — Watermarks for late events.",
      "Cold path? — Batch retrain model offline.",
      "Batch poll DB every minute",
      "Unbounded state per user forever",
      "No late event handling"),
    q("Mock Interview Time Management", "Interview Skills", "Very Common",
      "How manage time in 45-minute system design mock?",
      "5 clarify, 10 HLD, 15 deep dive ONE area, 5 failure/ops, 5 trade-offs, 5 buffer. Set timer mentally at 15 min — must have diagram by then.",
      "**Rules:**\n- Interviewer redirect — follow their interest\n- Stuck >3 min — state assumption, move on\n- Leave last 5 min for their questions\n- Practice with phone timer weekly\n\n**Signals to move on:** 'I'll deep dive caching unless you prefer API design.'\n\n**Architect:** Breadth with one solid depth beats shallow everything.",
      "Time management is trainable — mocks worthless without timer discipline.",
      "45 vs 60 min? — Adjust deep dive proportionally.",
      "Post-mock retro? — Score rubric, one improvement next mock.",
      "No diagram until minute 35",
      "Argue one detail entire session",
      "Skip trade-offs at end"),
]

ALL_WEEKS_45_48_Q006_Q030 = {
    45: WEEK_45_Q006_Q030,
    46: WEEK_46_Q006_Q030,
    47: WEEK_47_Q006_Q030,
    48: WEEK_48_Q006_Q030,
}
