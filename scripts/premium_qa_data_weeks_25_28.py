"""Hand-crafted premium Q001–Q030 for Weeks 25–28 — Docker, Kubernetes, Linux."""

from premium_qa_data import q

WEEK_25 = [
    q("Docker Image Layers", "Docker", "Very Common",
      "How do Docker image layers work and why do architects care about layer design?",
      "Each Dockerfile instruction creates a read-only layer stacked on a base image. Layers are cached and shared across images — smaller, stable lower layers speed builds and reduce registry storage.",
      "**Mechanism:** `FROM`, `RUN`, `COPY` each add layers. Union filesystem presents single view. Changed layer invalidates cache for all layers above it.\n\n**Architect decisions:**\n- Put rarely-changing dependencies (`dotnet restore`) before frequently-changing source (`COPY . .`)\n- Share base layers across microservices via common `aspnet` runtime image\n- Monitor image size — 500MB+ .NET images slow deploy and increase attack surface\n\n**Production:** `docker history myapp:tag` shows layer sizes; `dive` tool inspects waste.",
      "Layer design directly affects CI time, deploy speed, and security patch velocity.",
      "Layer deduplication across nodes? — Same layer pulled once per node — shared base reduces bandwidth.",
      "Squash layers? — `docker build --squash` loses cache benefits — rarely recommended.",
      "COPY . . before restore — cache bust every commit",
      "Single RUN apt-get without cleanup — bloated layer",
      "Ignore layer size in image governance"),
    q("Multi-Stage Builds", "Docker", "Very Common",
      "Design a multi-stage Dockerfile for a .NET 8 API. What goes in each stage?",
      "Build stage: SDK image, restore, publish. Runtime stage: aspnet runtime only, copy published output. Final image excludes SDK, source, and build tools.",
      "```dockerfile\nFROM mcr.microsoft.com/dotnet/sdk:8.0 AS build\nWORKDIR /src\nCOPY *.csproj ./\nRUN dotnet restore\nCOPY . .\nRUN dotnet publish -c Release -o /app/publish\n\nFROM mcr.microsoft.com/dotnet/aspnet:8.0 AS final\nWORKDIR /app\nCOPY --from=build /app/publish .\nUSER $APP_UID\nENTRYPOINT [\"dotnet\", \"MyApi.dll\"]\n```\n\n**Architect:** Multi-stage is default for production — never ship SDK in runtime. Separate test stage optional for CI `docker build --target test`.",
      "Multi-stage separates build-time attack surface from runtime minimalism.",
      "Distroless final stage? — Even smaller — no shell — harder to debug.",
      "BuildKit cache mounts? — `RUN --mount=type=cache` speeds NuGet restore.",
      "Single-stage SDK image in production",
      "Copy entire repo including secrets into final stage",
      "No non-root USER in final stage"),
    q("Dockerfile Best Practices", "Docker", "Very Common",
      "List Dockerfile best practices for enterprise .NET container pipelines.",
      "Pin base image digests, multi-stage build, non-root user, .dockerignore, health check, explicit EXPOSE, no secrets in layers, minimal packages.",
      "**Checklist:**\n1. `FROM mcr.microsoft.com/dotnet/aspnet:8.0@sha256:...` — reproducible builds\n2. `.dockerignore` excludes `bin/`, `obj/`, `.git`\n3. `USER app` — never run as root\n4. `HEALTHCHECK` or K8s probes — not both conflicting\n5. One process per container — PID 1 handles signals\n6. Label with `org.opencontainers.image.revision` for traceability\n\n**Architect:** Central template Dockerfile in platform repo; teams extend via ARG not copy-paste.",
      "Dockerfile standards prevent security drift across dozens of services.",
      "Hadolint in CI? — Lint Dockerfile on every PR.",
      "Chiseled images? — Ubuntu chiseled .NET — smaller attack surface.",
      "`:latest` tag in production Dockerfile",
      "apt-get update without version pin in same RUN",
      "Secrets via ENV or ARG persisted in image history"),
    q("Container vs VM", "Docker", "Very Common",
      "When containers vs VMs for .NET workloads on Azure?",
      "Containers: microservices, fast deploy, density, K8s/Container Apps. VMs: legacy apps, stateful monoliths needing full OS control, licensed software, strong isolation boundary.",
      "**Containers excel:** Homogeneous .NET APIs, horizontal scale, GitOps deploy, 50+ services.\n\n**VMs excel:** SQL Server legacy, Active Directory integration, custom kernel modules, compliance requiring dedicated hypervisor isolation.\n\n**Hybrid:** AKS nodes are VMs running containers — you get both. App Service runs containers on managed VM pools.\n\n**Architect:** Default cloud-native .NET to containers; VM only with documented justification in ADR.",
      "Container vs VM is isolation and operational model trade-off.",
      "VMs inside containers? — Nested virt niche — not typical.",
      "Windows containers vs Linux? — Linux default for .NET 8 — cost and ecosystem.",
      "VM per microservice at scale",
      "Containers for everything including stateful DB",
      "Ignore resource isolation differences"),
    q("Docker Networking Bridge", "Docker", "Common",
      "Explain Docker bridge networking for local multi-container apps.",
      "Default bridge network: containers get private IPs, communicate by container name on user-defined bridge. Published ports map host:container for external access.",
      "**User-defined bridge** (`docker network create app-net`): DNS resolution between containers by name — `http://api:8080` from `web` container.\n\n**vs host network:** Container shares host NIC — performance but no isolation.\n\n**Architect local dev:** Compose creates default network; match service names to production K8s DNS patterns.\n\n**Production note:** Docker bridge is local dev only — K8s CNI handles prod networking.",
      "Bridge networking models service discovery for local dev parity.",
      "Overlay network? — Multi-host Swarm/K8s — not default single-host bridge.",
      "host.docker.internal? — Reach host from container — Mac/Windows dev.",
      "Link containers via --link deprecated flag",
      "Published port conflicts on shared dev machine",
      "Assume bridge behavior equals K8s ClusterIP"),
    q("Docker Compose Local Dev", "Docker", "Very Common",
      "Design docker-compose.yml for local .NET API + Postgres + Redis dev stack.",
      "Define services with build contexts, env files, depends_on, healthchecks, named volumes for DB persistence, and shared network.",
      "```yaml\nservices:\n  api:\n    build: .\n    ports: [\"8080:8080\"]\n    environment:\n      ConnectionStrings__Default: Host=db;Database=app\n    depends_on:\n      db: { condition: service_healthy }\n  db:\n    image: postgres:16\n    volumes: [pgdata:/var/lib/postgresql/data]\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready\"]\nvolumes:\n  pgdata:\n```\n\n**Architect:** `compose.override.yml` for dev-only mounts; never commit prod secrets; document `docker compose up` in README.",
      "Compose is architect's local parity tool — not production orchestrator.",
      "Compose Watch? — Auto-rebuild on file change — dev velocity.",
      "Testcontainers vs Compose? — Testcontainers for CI integration tests; Compose for daily dev.",
      "Prod secrets in compose.yml committed",
      "depends_on without healthcheck — race on startup",
      "Bind mount entire repo over published DLL in container"),
    q("Image Scanning Vulnerabilities", "Docker", "Very Common",
      "How integrate container image vulnerability scanning in CI/CD?",
      "Scan every image build with Trivy, Microsoft Defender for Containers, or ACR Tasks scan. Fail pipeline on Critical CVE without exception process; quarantine non-compliant images in registry.",
      "**Pipeline gate:**\n1. `docker build` → push to staging tag\n2. Scan — `trivy image --severity CRITICAL,HIGH`\n3. Block deploy if Critical > 0 (or SLA to patch within 7 days)\n4. ACR retention policy + continuous scan on push\n\n**Architect:** Base image patching weekly automated PR; SBOM attached for audit. Exception workflow with risk acceptance sign-off.",
      "Image scanning is supply chain hygiene — non-negotiable in enterprise.",
      "Distroless reduces CVEs? — Fewer packages — fewer findings — not zero.",
      "False positives? — Vulnerability without exploit path — triage process.",
      "Scan only on deploy not on build",
      "Ignore base image CVEs — only scan app layer",
      "No policy for stale images in registry"),
    q("Non-Root Container User", "Docker", "Very Common",
      "Why run containers as non-root and how for .NET on Linux?",
      "Root in container escaping to host is catastrophic. Non-root limits blast radius. .NET 8 images include `APP_UID` — set `USER $APP_UID` in Dockerfile.",
      "**Implementation:**\n```dockerfile\nUSER $APP_UID\n```\n\n**K8s:** `securityContext.runAsNonRoot: true` + `runAsUser: 1654`.\n\n**File permissions:** Ensure `/app` writable only if needed; read-only root filesystem where possible.\n\n**Architect:** Admission policy deny images running as UID 0; platform base images pre-configured.",
      "Non-root is CIS Docker benchmark requirement.",
      "fsGroup for volumes? — K8s sets volume ownership for non-root write.",
      "OpenShift random UID? — .NET images support arbitrary UID with proper permissions.",
      "Run as root because 'easier permissions'",
      "USER directive missing — defaults root",
      "Writable root filesystem with root user"),
    q("Read-Only Root Filesystem", "Docker", "Common",
      "When enforce read-only root filesystem for containers?",
      "When app writes only to explicit volumes/tmp — prevents runtime malware persistence. Enable `readOnlyRootFilesystem: true` in K8s securityContext; mount `emptyDir` for `/tmp` if needed.",
      "**.NET APIs:** Often stateless — ideal for read-only root. Logs to stdout — no file write needed.\n\n**Exceptions:** Apps writing temp uploads — mount writable `emptyDir` at `/tmp` only.\n\n**Architect:** Platform default read-only; services request writable mounts via chart values with justification.",
      "Read-only root limits container compromise impact.",
      "writable /tmp emptyDir? — Standard pattern for read-only root.",
      "Break-glass debug? — Ephemeral debug container with writable FS.",
      "Read-only but app writes logs to /var/log — crash loop",
      "Entire FS writable without reason",
      "No exception process for writable mount requests"),
    q("Resource Limits cgroups", "Docker", "Very Common",
      "How set CPU and memory limits on Docker containers and why?",
      "`docker run --memory=512m --cpus=1.5` maps to cgroup limits. Prevents one container starving host; enables capacity planning.",
      "**Docker Compose:**\n```yaml\ndeploy:\n  resources:\n    limits: { cpus: '1', memory: 512M }\n```\n\n**K8s equivalent:** requests and limits — scheduler uses requests; OOMKill uses limits.\n\n**Architect:** Always set memory limits — .NET GC behaves differently under cgroup cap. Load test at limit before production. No limit = noisy neighbor on shared nodes.",
      "cgroups enforce fairness — critical on multi-tenant nodes.",
      ".NET GC under memory limit? — `DOTNET_GCHeapHardLimit` auto from cgroup — verify behavior.",
      "CPU limits vs throttling? — CFS quota throttles — requests guarantee minimum.",
      "No memory limit on Java/.NET heap-heavy apps",
      "Limit below minimum app footprint — OOMKill loop",
      "Docker local dev with no limits hiding prod issues"),
    q("Docker Registry ACR", "Docker", "Very Common",
      "Design Azure Container Registry strategy for multi-team .NET platform.",
      "Premium ACR for geo-replication and private link; one registry per environment or tenant; RBAC scoped to teams; ACR Tasks for build/scan; retention policies.",
      "**Structure:**\n- `mycompany.azurecr.io/platform/orders-api:1.2.3`\n- Prod: pull from geo-replicated Premium ACR via Private Endpoint\n- CI: Managed Identity `AcrPush` — no admin user\n\n**Architect:** Webhook or ARC triggers deploy; image signing with Notation/Cosign optional for zero-trust.",
      "ACR is private artifact store — treat like production database.",
      "ACR vs Docker Hub? — ACR integrated RBAC, no rate limits, geo-rep.",
      "Tasks cache? — ACR Tasks remote cache speeds multi-stage builds.",
      "ACR admin user enabled in prod",
      "Single `:latest` tag deploy pipeline",
      "No geo-replication for multi-region DR"),
    q("Image Tagging Strategy", "Docker", "Very Common",
      "Define image tagging strategy for GitOps .NET microservices.",
      "Immutable tags: `{semver}` + `{git-sha}` + `{build-id}`. Never deploy `:latest` to prod. Promote same digest across environments.",
      "**Tags:**\n- `orders-api:2.4.1` — release\n- `orders-api:2.4.1-abc1234` — traceability\n- `orders-api:sha-abc1234` — GitOps pin\n\n**Promotion:** Dev deploys `sha-abc1234`; prod deploys same digest after gate — not rebuilt.\n\n**Architect:** Document in platform ADR; admission controller rejects mutable tags.",
      "Tag immutability enables rollback and audit.",
      "Digest vs tag? — Deploy by digest `@sha256:...` for absolute immutability.",
      "Semantic versioning policy? — Major API break = major tag.",
      "`:latest` in production Helm values",
      "Rebuild image per environment — different digests",
      "Overwrite existing semver tag in registry"),
    q(".NET Container Base Images", "Docker", "Very Common",
      "Which Microsoft .NET container base images to use and when?",
      "`mcr.microsoft.com/dotnet/aspnet:8.0` for runtime; `sdk:8.0` for build only. Alpine/smaller variants for size; chiseled for security-minimal.",
      "**Choices:**\n- **aspnet** — production API default\n- **sdk** — CI build stage only\n- **Alpine** — smaller but musl compatibility testing required\n- **Chiseled Ubuntu** — no shell/package manager — hardened\n\n**Architect:** Pin major version (`8.0`); automate patch updates via Renovate; test Alpine in CI if adopted.",
      "Base image choice affects size, CVE surface, and glibc compatibility.",
      "Self-contained vs framework-dependent container? — FDD smaller layers with shared runtime base.",
      "Native AOT container? — Distroless-friendly — smallest — reflection limits.",
      "SDK image in production runtime",
      "Unpinned `aspnet:latest`",
      "Alpine in prod without integration test on musl"),
    q("Health Checks Dockerfile", "Docker", "Common",
      "HEALTHCHECK in Dockerfile vs Kubernetes probes — how coordinate?",
      "Docker HEALTHCHECK for local/single-host; K8s liveness/readiness for orchestration. Prefer K8s probes in production — disable conflicting Dockerfile HEALTHCHECK or align endpoints.",
      "**Dockerfile:**\n```dockerfile\nHEALTHCHECK --interval=30s CMD curl -f http://localhost:8080/health/live || exit 1\n```\n\n**K8s:** Separate `/health/live` and `/health/ready` — readiness excludes external deps from liveness.\n\n**Architect:** Standard health endpoints in all .NET services; platform Helm chart wires probes consistently.",
      "Health checks drive restart and traffic routing decisions.",
      "curl in distroless? — Use `/health` TCP probe or minimal wget alternative.",
      "Startup probe? — Slow .NET cold start — avoid premature liveness kill.",
      "HTTP check to payment provider in liveness",
      "No health endpoint — default TCP only",
      "Docker HEALTHCHECK and K8s probe different paths — confusion"),
    q("Volume vs Bind Mount", "Docker", "Common",
      "When Docker named volume vs bind mount?",
      "Named volumes: portable, Docker-managed storage, prod data (DB). Bind mounts: dev hot-reload source code, config files from host path.",
      "**Dev:** `volumes: [\".:/src\"]` bind mount for live edit — not for prod.\n\n**Prod DB:** Named volume or cloud disk — `postgres_data:/var/lib/postgresql/data`.\n\n**K8s mapping:** PVC for named volume equivalent; ConfigMap mount for config.\n\n**Architect:** Never bind mount host paths in production K8s — use PVC and secrets.",
      "Volume type affects portability and performance.",
      "volume mount performance Mac? — VirtioFS/osxfs slower — use delegated mode.",
      "ReadOnlyMany PVC? — Shared config across pods.",
      "Bind mount prod database data directory",
      "Anonymous volumes without backup plan",
      "Bind mount secrets from developer laptop path"),
    q("Secrets in Containers", "Docker", "Very Common",
      "How manage secrets for .NET containers without baking into images?",
      "Runtime injection: K8s Secrets + CSI, Azure Key Vault Provider, Docker secrets (Swarm), environment from orchestrator. Never ARG/ENV secrets in Dockerfile.",
      "**Patterns:**\n- **AKS:** Secrets Store CSI Driver → Key Vault → mount as files\n- **Local dev:** User secrets / `.env` gitignored / Docker Compose secrets\n- **.NET:** `AddAzureKeyVault()` or file-based config provider\n\n**Architect:** Secrets rotation without image rebuild; audit access; prefer workload identity over client secrets.",
      "Secret management separates build artifacts from runtime credentials.",
      "Sealed Secrets / External Secrets? — GitOps-safe secret delivery.",
      "Build-time secret in multi-stage? — BuildKit secret mount — not in layer.",
      "Connection string in Dockerfile ENV",
      "Secrets in image layers from COPY .env",
      "Same secret volume world-readable in container"),
    q("Docker Security CIS", "Docker", "Common",
      "Key CIS Docker Benchmark controls for production?",
      "Non-root user, read-only root FS, no privileged mode, limit capabilities, scan images, use user namespaces where supported, log to centralized sink, pin versions.",
      "**Top CIS items:**\n1. 4.1 — non-root default\n2. 4.6 — no new privileges\n3. 5.1 — verify image provenance\n4. 5.7 — no privileged containers\n5. 5.15 — single main process\n\n**Architect:** OPA/Gatekeeper or Azure Policy for AKS enforces CIS-aligned admission; quarterly benchmark audit.",
      "CIS benchmark translates container security to checklist.",
      "Pod Security Standards? — K8s restricted profile maps to CIS.",
      "seccomp/AppArmor profiles? — Limit syscalls — platform-provided defaults.",
      "Privileged container for 'simple fix'",
      "CAP_SYS_ADMIN granted broadly",
      "No image provenance verification"),
    q("Container Orchestration When", "Docker", "Very Common",
      "When move from Docker Compose to Kubernetes or Container Apps?",
      "When you need: multi-host scheduling, rolling updates, autoscaling, service discovery at scale, self-healing, or >3 services in prod with SLA.",
      "**Stay Compose/local:** Developer machines, integration test fixtures.\n\n**Container Apps:** HTTP scale-to-zero, simpler ops, KEDA built-in — sweet spot for many .NET APIs.\n\n**AKS:** Full K8s APIs, service mesh, complex networking, multi-team platform.\n\n**Architect:** Decision matrix in ADR — team K8s skill, scale, compliance drive choice.",
      "Orchestration choice is operational complexity vs capability.",
      "Docker Swarm? — Legacy — greenfield choose K8s or managed PaaS.",
      "Nomad? — Simpler scheduler — niche multi-workload.",
      "AKS for two static containers",
      "Compose in production at scale",
      "Skip orchestration with manual docker run fleet"),
    q("Dockerfile Cache Optimization", "Docker", "Common",
      "Optimize Dockerfile layer caching for fast CI builds?",
      "Order instructions least-to-most frequently changing; copy csproj before source; use BuildKit cache mounts for NuGet; parallel stages where independent.",
      "**Pattern:**\n```dockerfile\nCOPY *.csproj ./\nRUN --mount=type=cache,target=/root/.nuget/packages \\\n    dotnet restore\nCOPY . .\nRUN dotnet publish ...\n```\n\n**CI:** `--cache-from` previous image; ACR Tasks remote cache.\n\n**Architect:** Target <3 min image build for typical API; monitor build metrics in pipeline dashboard.",
      "Cache strategy saves CI minutes and developer wait time at scale.",
      "BuildKit inline cache? — Export cache metadata in image for pull.",
      "Remote cache backend? — GitHub Actions cache or ACR.",
      "COPY . . before restore",
      "Clean dotnet cache every build intentionally",
      "No cache-from in CI — full rebuild always"),
    q("Distroless Images", "Docker", "Common",
      "When use distroless or chiseled images for .NET?",
      "When security posture prioritizes minimal attack surface over shell debugging convenience. .NET chiseled images from Microsoft — no apt, no bash.",
      "**Pros:** Fewer CVEs, smaller image, compliance friendly.\n\n**Cons:** No `kubectl exec` shell — debug via ephemeral debug containers or logs.\n\n**Architect:** Platform standard for prod; developers use debug variant tag in non-prod. Pair with good observability.",
      "Distroless trades debug convenience for security minimalism.",
      "debug distroless variant? — Non-prod images with shell — separate tag.",
      "Google distroless dotnet? — Community; prefer Microsoft chiseled for .NET.",
      "Distroless without centralized logging — blind debugging",
      "Shell-dependent healthcheck in distroless",
      "Distroless without team training on debug patterns"),
    q("Sidecar Pattern Containers", "Docker", "Common",
      "Explain sidecar pattern — production examples for .NET on K8s.",
      "Sidecar: auxiliary container in same pod sharing network/volumes with main app. Examples: Envoy proxy, log shipper, config sync, service mesh dataplane.",
      "**Examples:**\n- **Istio sidecar** — mTLS, telemetry\n- **Fluent Bit** — log forwarding\n- **Dapr sidecar** — state/pubsub abstractions\n\n**Architect:** Sidecar adds resource overhead — justify per pod. Init vs sidecar: init runs to completion before app starts.",
      "Sidecars extend pod capability without changing app code.",
      "Sidecar resource limits? — Include in pod quota — sidecars consume CPU/memory.",
      "Ambient mesh? — Sidecar-less Istio — reduces overhead — emerging.",
      "Sidecar for every trivial concern",
      "Ignore sidecar memory in pod sizing",
      "Two app containers without clear sidecar boundary"),
    q("Init Containers Concept", "Docker", "Common",
      "When use init containers vs main container startup logic?",
      "Init containers run sequentially to completion before app starts — migrations, wait-for dependency, download config, set permissions.",
      "**Use init when:**\n- DB schema migrate once per deploy\n- Wait for Kafka topic creation\n- Fetch secrets from vault before app reads them\n\n**Avoid:** Long-running init — blocks pod ready. Use Job for one-off batch migrate instead if slow.\n\n**Architect:** Standard init chart patterns in platform Helm library.",
      "Init containers serialize pre-start dependencies cleanly.",
      "Init container failure? — Pod restarts — ensure idempotent migrations.",
      "Share volume with app? — `emptyDir` for downloaded artifacts.",
      "Heavy migration in init blocking scale-out",
      "Init container with different image tag unvetted",
      "Duplicate migration in app AND init"),
    q("Container Logging Drivers", "Docker", "Common",
      "How should .NET containers emit logs in production?",
      "Write structured logs to stdout/stderr — container runtime collects. Avoid file-based logging inside container. Centralize with Fluent Bit/DaemonSet → Log Analytics/ELK.",
      "**.NET:** Serilog `WriteTo.Console(new JsonFormatter())` — one JSON line per event.\n\n**Docker driver:** `json-file` default locally; prod uses cluster collector not docker logs.\n\n**Architect:** Correlation ID in every log line; no sensitive data in logs; retention policy per compliance.",
      "Stdout logging is 12-factor container standard.",
      "Fluent Bit vs Fluentd? — Fluent Bit lighter for DaemonSet.",
      "Log rotation docker json-file? — Local only — size limits on dev.",
      "Logs written only to file inside container",
      "Plain text logs without correlation ID",
      "Container logs as only observability — no metrics/traces"),
    q("Docker on Windows vs Linux", "Docker", "Common",
      "Compare Docker on Windows vs Linux for .NET 8 deployments.",
      "Linux containers dominate .NET 8 — smaller images, K8s default, lower cost. Windows containers for legacy .NET Framework and Windows-specific dependencies only.",
      "**Linux:** `mcr.microsoft.com/dotnet/aspnet:8.0` on AKS Linux nodes — standard.\n\n**Windows:** Larger images, Windows node pools, higher cost, slower pull — isolate Framework apps.\n\n**Architect:** Migration path Framework → .NET 8 on Linux containers; Windows container only with ADR justification.",
      "Platform default Linux — Windows is exception path.",
      "WSL2 Docker Desktop? — Linux containers on Windows dev machine.",
      "Hyper-V isolation? — Windows container isolation modes — perf impact.",
      "Windows containers for new .NET 8 APIs",
      "Mixed OS in same K8s pod — impossible",
      "Ignore Windows node licensing cost"),
    q("BuildKit Features", "Docker", "Common",
      "Which BuildKit features matter for enterprise .NET image builds?",
      "Cache mounts, secret mounts, SSH mounts, parallel stage execution, provenance attestations, and improved layer export.",
      "**Enable:** `DOCKER_BUILDKIT=1` default in modern Docker.\n\n**Key features:**\n```dockerfile\nRUN --mount=type=cache,target=/root/.nuget/packages dotnet restore\nRUN --mount=type=secret,id=nugetconfig dotnet restore\n```\n\n**Architect:** BuildKit in ACR Tasks and GitHub Actions; SBOM/provenance for supply chain.",
      "BuildKit is modern build engine — enable platform-wide.",
      "buildx bake? — Multi-image matrix builds monorepo.",
      "Provenance attestation? — SLSA level — link image to source commit.",
      "Legacy builder without cache mounts",
      "Secrets in RUN command line visible",
      "No build provenance in regulated environment"),
    q("OCI Image Spec", "Docker", "Occasional",
      "What is the OCI image spec and why does portability matter?",
      "OCI (Open Container Initiative) defines image format and runtime spec — Docker, containerd, CRI-O interoperate. Image = manifest + config + layers.",
      "**Architect relevance:** Images built in CI run on AKS (containerd), local Docker, and other clouds without rebuild. Sign manifests with Notation.\n\n**Components:**\n- **Image spec** — layer tarball format\n- **Runtime spec** — how to run bundle\n\nVendor neutrality reduces lock-in.",
      "OCI standard enables multi-cloud container portability.",
      "OCI artifacts? — Store SBOM, signatures alongside images in registry.",
      "Docker vs OCI media types? — Compatible — registries translate.",
      "Proprietary image format dependency",
      "Assume Docker-only runtime in design",
      "Ignore manifest list for multi-arch"),
    q("Container Escape Risks", "Docker", "Common",
      "What is container escape and how mitigate architecturally?",
      "Escape: exploit kernel or misconfiguration to access host. Mitigate: patch nodes, non-root, no privileged, seccomp/AppArmor, network policies, minimal images, runtime threat detection.",
      "**Attack vectors:**\n- Privileged container → host devices\n- Kernel CVE (runc, containerd)\n- Mounted docker.sock in container\n\n**Architect:** AKS auto-patches nodes; Defender for Containers; deny privileged in admission; regular pen test.",
      "Container security assumes hostile workload — defense in depth.",
      "gVisor/Kata runtime? — VM-isolated containers — higher isolation cost.",
      "Rootless Docker? — Daemonless rootless — dev security.",
      "Mount /var/run/docker.sock in app container",
      "Privileged pods for performance",
      "Unpatched node images for months"),
    q("SBOM for Containers", "Docker", "Common",
      "Why generate SBOM for container images and how?",
      "SBOM (Software Bill of Materials) lists components for CVE matching and compliance. Generate in CI with Syft/Trivy; attach to registry as OCI artifact.",
      "**Pipeline:**\n```bash\nsyft packages myapp:1.0 -o spdx-json > sbom.spdx.json\n```\n\n**Architect:** Required for executive order / supply chain compliance in many enterprises. Link SBOM to vulnerability scan results; retain per release.",
      "SBOM enables precise impact analysis when Log4Shell-style CVEs hit.",
      "SPDX vs CycloneDX? — Both common — pick one org standard.",
      "SBOM only at release not per build? — Per immutable image digest.",
      "No SBOM in regulated industry",
      "SBOM generated but never stored",
      "Scan image without SBOM component linkage"),
    q("Local Dev vs Prod Parity", "Docker", "Common",
      "How maximize dev/prod parity without running full AKS locally?",
      "Same container image digest, same env var names, Compose mimics service topology, Testcontainers for integration tests, feature flags for external service stubs.",
      "**Practices:**\n- Build once, promote digest\n- `appsettings.Development.json` mirrors prod structure not values\n- Local Aspire or Tilt for K8s-like dev optional\n- Contract tests against real Postgres in Testcontainers\n\n**Architect:** Document acceptable dev/prod gaps; minimize 'works on my machine' via CI integration environment.",
      "Parity reduces deployment surprises — architect defines minimum bar.",
      "Dev containers? — VS Code devcontainer.json — onboarding standard.",
      "Kind/minikube local K8s? — For platform team not every developer.",
      "Different DB engine local vs prod",
      "Mock everything — no integration tests",
      "Developers never run container locally"),
    q("Docker ADR Template", "Docker", "Common",
      "What belongs in an ADR for adopting containers on Azure?",
      "Context, decision (AKS vs Container Apps vs App Service), base image standard, registry choice, security controls, networking, observability, rollback, and team skill plan.",
      "**ADR sections:**\n1. **Context** — monolith deploy pain, scale needs\n2. **Decision** — AKS + ACR Premium + GitOps\n3. **Consequences** — ops overhead, benefits\n4. **Standards** — Dockerfile template, tagging, scanning gates\n5. **Rejected** — VM-only, serverless-only\n\n**Architect:** ADR is living doc — review quarterly.",
      "Container ADR aligns platform and product teams.",
      "When revise ADR? — Major cost or security incident triggers review.",
      "Link ADR to runbooks? — On-call references ADR decisions.",
      "ADR written after production firefight only",
      "No rejected alternatives documented",
      "ADR ignores team training plan"),
]

WEEK_26 = [
    q("Pod Lifecycle", "Kubernetes", "Very Common",
      "Walk through Kubernetes pod lifecycle phases and architect implications.",
      "Phases: Pending (scheduled) → Running (containers started) → Succeeded/Failed → Terminating. Containers have waiting/running/terminated states.",
      "**Pending causes:** Insufficient resources, image pull, PVC bind, taints. **Running:** probes determine ready. **Termination:** `terminationGracePeriodSeconds` — .NET needs graceful shutdown.\n\n**Architect:** Set `preStop` hook + drain connections; liveness ≠ readiness; monitor `CrashLoopBackOff` and `ImagePullBackOff` in alerts.",
      "Pod lifecycle drives availability during deploy and failure.",
      "Pod phase vs container state? — Init containers affect startup sequence.",
      "Eviction? — Node pressure kills pods — QoS class matters.",
      "Ignore terminationGracePeriod — hard kill mid-request",
      "No preStop for connection draining",
      "Alert only on pod Running not Ready"),
    q("Deployment vs StatefulSet", "Kubernetes", "Very Common",
      "When Deployment vs StatefulSet for .NET workloads?",
      "Deployment: stateless APIs, random pod names, interchangeable replicas. StatefulSet: stable network ID, ordered deploy, persistent identity — databases, Kafka, clustered state.",
      "**Deployment:** Orders API, BFF, workers — scale horizontally, any pod serves any request.\n\n**StatefulSet:** SQL on K8s (rare), Redis cluster, Elasticsearch — stable `pod-0`, `pod-1` DNS.\n\n**Architect:** Default Deployment; StatefulSet only with documented stateful need. Managed Azure SQL/Redis preferred over StatefulSet for data.",
      "Workload statefulness drives controller choice.",
      "DaemonSet when? — Log collector, node agent on every node.",
      "Job/CronJob? — Batch migrate, scheduled reports.",
      "StatefulSet for stateless API",
      "Deployment for database without external storage plan",
      "Ignore ordered rollout in clustered stateful app"),
    q("Service ClusterIP LoadBalancer", "Kubernetes", "Very Common",
      "Explain Kubernetes Service types — ClusterIP, NodePort, LoadBalancer.",
      "ClusterIP: internal VIP — pod-to-pod. NodePort: host port exposure. LoadBalancer: cloud LB provisions public IP (AKS Azure LB).",
      "**Typical AKS:**\n- API pods behind ClusterIP Service\n- Ingress controller Service type LoadBalancer (or internal LB)\n- East-west traffic ClusterIP only\n\n**Architect:** Never expose every service as LoadBalancer — cost and attack surface. Single ingress entry point; NetworkPolicy restricts internal traffic.",
      "Service types define traffic entry and cost.",
      "Internal LoadBalancer annotation? — Private ingress for corp network.",
      "Headless Service? — StatefulSet stable DNS — ClusterIP None.",
      "LoadBalancer per microservice",
      "NodePort in production without firewall",
      "ClusterIP assumed routable from internet"),
    q("Ingress Controller", "Kubernetes", "Very Common",
      "Design ingress architecture for .NET APIs on AKS.",
      "Ingress controller (NGINX, App Gateway Ingress Controller) routes HTTP/S by host/path to ClusterIP services. TLS termination at ingress; WAF optional.",
      "**Layers:**\n1. Azure Front Door / App Gateway (WAF, global)\n2. Ingress controller (path routing, cert-manager TLS)\n3. Service → Pod\n\n**Architect:** One ingress class per cluster; annotate for AGIC vs NGINX; rate limiting and auth at edge; backend protocol HTTP/2 to Kestrel.",
      "Ingress is L7 routing layer — central policy enforcement point.",
      "Gateway API vs Ingress? — Gateway API successor — evaluate for new clusters.",
      "Multiple ingress controllers? — Separate internal/external controllers.",
      "Ingress without TLS in prod",
      "Path routing conflicts between teams",
      "Bypass ingress — expose Service LoadBalancer directly"),
    q("ConfigMap vs Secret", "Kubernetes", "Very Common",
      "When ConfigMap vs Secret for .NET app configuration on K8s?",
      "ConfigMap: non-sensitive config (feature flags, logging level). Secret: credentials, connection strings, API keys — base64 encoded at rest, encrypt etcd.",
      "**.NET pattern:**\n```yaml\nenv:\n- name: Logging__LogLevel__Default\n  valueFrom: { configMapKeyRef: ... }\n- name: ConnectionStrings__Default\n  valueFrom: { secretKeyRef: ... }\n```\n\n**Architect:** Prefer Key Vault CSI over native Secrets for rotation; never commit Secret YAML to Git; Sealed Secrets for GitOps.",
      "Config separation prevents credential leakage in logs and repos.",
      "Secret encryption at rest? — Enable AKS KMS/etcd encryption.",
      "Reload on ConfigMap change? — .NET reloadable options vs restart required.",
      "Secrets in ConfigMap because 'base64 is encryption'",
      "Secrets committed to Git plain YAML",
      "Same ConfigMap prod and dev values"),
    q("Liveness vs Readiness Probes", "Kubernetes", "Very Common",
      "Design liveness and readiness probes for ASP.NET Core on AKS.",
      "Liveness: restart if process deadlocked. Readiness: remove from Service endpoints if cannot serve (DB down). Startup: slow init before liveness kicks in.",
      "```yaml\nlivenessProbe:\n  httpGet: { path: /health/live, port: 8080 }\n  initialDelaySeconds: 10\nreadinessProbe:\n  httpGet: { path: /health/ready, port: 8080 }\nstartupProbe:\n  httpGet: { path: /health/live, port: 8080 }\n  failureThreshold: 30\n```\n\n**Architect:** DB check in readiness only — not liveness — avoids restart storm on DB blip.",
      "Probe design prevents cascading outages — top AKS interview topic.",
      "grpc health probe? — Native gRPC health for gRPC services.",
      "Probe timeout tuning? — Shorter than P99 handler time.",
      "External payment API in liveness probe",
      "No startup probe on slow .NET cold start",
      "Readiness always true — traffic to broken pods"),
    q("Resource Requests Limits", "Kubernetes", "Very Common",
      "How set CPU/memory requests and limits for .NET pods?",
      "Requests: scheduler placement guarantee. Limits: max usage — memory limit triggers OOMKill. Set requests ≈ steady state; limits headroom for spikes.",
      "**Process:**\n1. Load test without limits — observe usage\n2. Request = P95 usage; Limit = P99 × 1.5 for memory\n3. Use VPA recommendations as starting point\n\n**QoS:** Requests = Limits → Guaranteed; only requests → Burstable.\n\n**Architect:** Namespace quotas cap team totals; deny pods without requests.",
      "Right-sizing prevents node overload and wasted capacity.",
      ".NET memory under limit? — GC adapts to cgroup — validate under load.",
      "CPU limit throttling? — .NET CPU-bound may throttle — measure.",
      "No requests — BestEffort evicted first",
      "Limit 128Mi on .NET API — OOMKill",
      "Copy limits from blog without load test"),
    q("Namespace Strategy", "Kubernetes", "Common",
      "Design namespace strategy for multi-team AKS platform.",
      "Namespace per team per environment slice, or per application system. Apply RBAC, ResourceQuota, NetworkPolicy per namespace.",
      "**Patterns:**\n- `team-orders-dev`, `team-orders-prod` — or single cluster prod with `orders-prod`\n- Platform namespace: `ingress-nginx`, `cert-manager`, `monitoring`\n\n**Architect:** Avoid namespace-per-microservice sprawl; NetworkPolicy default deny between namespaces; label standards enforced.",
      "Namespaces are blast-radius and RBAC boundaries.",
      "Namespace per env vs cluster per env? — Prod cluster isolation common.",
      "Hierarchical namespaces? — Subnamespace operator — advanced.",
      "Everything in default namespace",
      "50 namespaces no RBAC",
      "Cross-namespace traffic wide open"),
    q("kubectl Debugging", "Kubernetes", "Very Common",
      "kubectl commands for debugging failing .NET pod in production?",
      "`kubectl describe pod`, `kubectl logs`, `kubectl get events`, `kubectl exec`, `kubectl port-forward`, ephemeral debug containers.",
      "**Workflow:**\n1. `kubectl get pods -n orders` — status Restarts/CrashLoop\n2. `kubectl describe pod` — Events: OOMKilled, FailedMount\n3. `kubectl logs pod --previous` — crash before restart\n4. `kubectl port-forward` — local curl health endpoint\n\n**Architect:** Restrict prod exec to break-glass RBAC; prefer observability over shell in prod.",
      "Systematic kubectl flow impresses in platform interviews.",
      "kubectl debug ephemeral container? — Distroless debug with shell.",
      "stern/lens? — Multi-pod log tail — ops tooling.",
      "kubectl delete pod first without logs",
      "exec as root in prod without audit",
      "Ignore Events section in describe"),
    q("Helm Charts Basics", "Kubernetes", "Common",
      "How use Helm for packaging .NET microservices on AKS?",
      "Helm chart templates K8s YAML with values per environment. Chart: Deployment, Service, Ingress, HPA, ConfigMap references.",
      "**Structure:**\n- `values.yaml` defaults\n- `values-prod.yaml` overrides replicas, resources\n- `helm upgrade --install orders ./chart -f values-prod.yaml`\n\n**Architect:** OCI Helm registry in ACR; chart library for common .NET patterns; version charts with app semver.",
      "Helm is package manager — reduces YAML duplication.",
      "Helm vs Kustomize? — Helm templating vs overlay patches — both valid.",
      "Helm hooks? — Pre-upgrade migration Job.",
      "Raw YAML copy-paste per env",
      "Secrets in values.yaml committed",
      "Unpinned chart dependencies"),
    q("K8s Networking Model", "Kubernetes", "Very Common",
      "Explain Kubernetes networking model — pod IP, CNI, Services.",
      "Every pod gets unique cluster-routable IP. CNI plugin (Azure CNI, Calico) implements networking. Services provide stable VIP via kube-proxy or eBPF dataplane.",
      "**Rules:**\n- Pods communicate by IP without NAT within cluster\n- Service abstracts pod churn\n- NetworkPolicy optional L3/L4 firewall\n\n**AKS:** Azure CNI assigns VNet IPs — plan IP exhaustion. Overlay mode conserves IPs.\n\n**Architect:** IP planning in landing zone; document CNI choice in platform ADR.",
      "Networking model underpins service mesh and security design.",
      "kube-proxy modes? — iptables vs IPVS vs eBPF (Cilium).",
      "Host network pod? — Rare — bypasses network isolation.",
      "Assume unlimited pod IPs in VNet",
      "Ignore Azure CNI IP preallocation",
      "No NetworkPolicy because 'internal network'"),
    q("DNS in Cluster", "Kubernetes", "Common",
      "How does DNS work inside Kubernetes for service discovery?",
      "CoreDNS resolves `service.namespace.svc.cluster.local`. Pods use short names `http://orders-api.orders` within same namespace.",
      "**Patterns:**\n- Full FQDN: `orders-api.orders.svc.cluster.local`\n- Cross-namespace: `http://payment.payment.svc.cluster.local`\n- Headless: returns pod A records directly\n\n**Architect:** Align K8s service names with local Compose names for dev parity. Custom stub domains via CoreDNS config for external services.",
      "Cluster DNS is service discovery — names must be stable contracts.",
      "ndots and search paths? — Misconfig causes external DNS leakage.",
      "ExternalName service? — CNAME to external host — migration bridge.",
      "Hardcode pod IPs in app config",
      "Wrong namespace short name assumption",
      "Ignore DNS for private link endpoints"),
    q("PersistentVolume Claims", "Kubernetes", "Common",
      "When do .NET workloads need PVCs on AKS?",
      "Stateless APIs: no PVC. Need PVC: file uploads on disk, local cache, legacy app requiring filesystem state. Prefer blob storage over PVC for files.",
      "**AKS:** StorageClass `managed-csi` provisions Azure Disk. Access modes: RWO typical. StatefulSet + volumeClaimTemplates for stable storage per pod.\n\n**Architect:** Default stateless; Azure Blob via SDK for uploads; PVC only with backup/restore runbook.",
      "PVC introduces state — complicates pod mobility.",
      "Azure Files vs Disk? — Files ReadWriteMany — shared storage niche.",
      "Volume expansion? — Allow resize — plan capacity.",
      "PVC for every API 'just in case'",
      "No backup for stateful PVC",
      "RWO disk attached to multiple pods"),
    q("Rolling Update Strategy", "Kubernetes", "Very Common",
      "Configure rolling update for zero-downtime .NET API deploy?",
      "Deployment `strategy: RollingUpdate` with `maxUnavailable: 0`, `maxSurge: 1`. Readiness probe gates new pods before old terminate.",
      "```yaml\nstrategy:\n  type: RollingUpdate\n  rollingUpdate:\n    maxSurge: 25%\n    maxUnavailable: 0\n```\n\n**Architect:** PDB ensures min available during drain; graceful shutdown handles in-flight requests; blue/green or canary for high-risk releases.",
      "Rolling update + readiness = safe default deploy.",
      "Recreate strategy? — Downtime — dev only.",
      "minReadySeconds? — Extra stability before counting ready.",
      "maxUnavailable 50% on single replica",
      "No readiness — traffic to starting pods",
      "Kill pods without graceful shutdown"),
    q("ReplicaSet Purpose", "Kubernetes", "Common",
      "What is a ReplicaSet and relationship to Deployment?",
      "ReplicaSet maintains desired pod replica count via label selector. Deployment manages ReplicaSets — handles rolling updates by creating new ReplicaSet.",
      "**Flow:** Deployment spec change → new ReplicaSet scaled up → old scaled down.\n\n**Architect:** Never manage ReplicaSet directly — always Deployment. `kubectl rollout history` for revision tracking.",
      "Deployment abstracts ReplicaSet lifecycle for updates.",
      "Manual ReplicaSet? — Only advanced rollback scenarios.",
      "Replica count 0? — Pause service without deleting Deployment.",
      "Edit ReplicaSet directly for image update",
      "Label selector mismatch — orphan pods",
      "Ignore rollout history for rollback"),
    q("Labels Selectors", "Kubernetes", "Very Common",
      "Design label taxonomy for AKS multi-team platform.",
      "Standard labels: `app`, `version`, `team`, `env`, `tier`. Services select pods via `app: orders-api`.",
      "**Example:**\n```yaml\nlabels:\n  app.kubernetes.io/name: orders-api\n  app.kubernetes.io/version: \"2.4.1\"\n  team: commerce\n  env: prod\n```\n\n**Architect:** Enforce required labels via admission policy; used in NetworkPolicy, HPA, Service selectors, cost allocation.",
      "Labels are platform metadata backbone — design early.",
      "Recommended labels? — kubernetes.io/docs recommended standard labels.",
      "Annotations vs labels? — Annotations non-identifying metadata — not in selectors.",
      "Random labels per developer",
      "Selector too broad — captures wrong pods",
      "No labels — cannot filter metrics or logs"),
    q("K8s RBAC Basics", "Kubernetes", "Very Common",
      "Explain Kubernetes RBAC — Role, ClusterRole, Binding.",
      "Role: namespace permissions. ClusterRole: cluster-wide. RoleBinding/ClusterRoleBinding attach to User/Group/ServiceAccount.",
      "**Example:** Developer Role `get,list,watch pods,logs` in `orders-dev` namespace. CI ServiceAccount `deploy` Role with `apply` on specific resources.\n\n**Architect:** Least privilege; no cluster-admin for apps; Azure AD integration with AKS for group bindings; audit RBAC quarterly.",
      "RBAC prevents lateral movement in compromised cluster.",
      "Aggregate ClusterRoles? — Extend default roles carefully.",
      "impersonate permission? — Powerful — restrict.",
      "cluster-admin for all developers",
      "Shared ServiceAccount across all deployments",
      "No RBAC — ABAC legacy"),
    q("Service Account", "Kubernetes", "Common",
      "How use Kubernetes ServiceAccount for .NET pod identity?",
      "Each pod runs as ServiceAccount — mounts token for K8s API access. On AKS, link to Azure Workload Identity for Azure resource access without secrets.",
      "**Pattern:**\n```yaml\nserviceAccountName: orders-api\n```\n\n**Workload Identity:** Federated credential → pod SA → Key Vault, Storage, SQL AAD.\n\n**Architect:** One SA per microservice; disable default SA token mount if unused (`automountServiceAccountToken: false`).",
      "ServiceAccount is pod identity for K8s and cloud federation.",
      "Legacy pod-managed identity? — Deprecated on AKS — migrate WI.",
      "Token projection? — Bound short-lived tokens — security.",
      "Default SA for all pods with API access",
      "Long-lived Azure client secret in pod",
      "Same SA cluster-wide admin rights"),
    q("Node Pools AKS", "Kubernetes", "Very Common",
      "Design AKS node pool strategy for .NET platform.",
      "Separate pools: system (critical addons), user workloads, GPU (if ML), spot (batch). Size pools per workload profile; autoscale node pools.",
      "**Example:**\n- `system` — 3 nodes min, taint `CriticalAddonsOnly`\n- `apps` — D4s_v5, cluster autoscaler 3-20\n- `spot` — batch workers, taint `kubernetes.azure.com/scalesetpriority=spot`\n\n**Architect:** AZ spread across pools; patch surge settings; monitor node CPU/memory pressure.",
      "Node pools isolate workload types and cost profiles.",
      "Node image upgrade? — AKS channel — plan maintenance windows.",
      "Private cluster? — API server private — jump host or ARC.",
      "Single node pool everything",
      "Spot pool without tolerations",
      "No system pool — apps evict addons"),
    q("K8s vs App Service Decision", "Kubernetes", "Very Common",
      "When AKS vs Azure App Service for .NET APIs?",
      "App Service: faster time-to-market, slots, built-in auth, less ops. AKS: multi-service platform, custom networking, service mesh, fine-grained control.",
      "**App Service when:** Small team, few APIs, PaaS ops preference, VNet integration sufficient.\n\n**AKS when:** 10+ services, GitOps platform team, KEDA/custom operators, multi-tenant cluster.\n\n**Architect:** Honest TCO — AKS needs platform engineers; App Service + Container Apps middle ground.",
      "Orchestration choice is org capability as much as tech.",
      "Container Apps vs AKS? — Serverless containers — simpler — evaluate.",
      "Migrate App Service to AKS trigger? — Team scale, K8s-specific needs.",
      "AKS for single CRUD API startup",
      "App Service for complex service mesh",
      "Decision without 3-year ops cost model"),
    q("Multi-Container Pods", "Kubernetes", "Common",
      "When put multiple containers in one pod vs separate deployments?",
      "Same pod: tight coupling, shared network (localhost), shared volumes — sidecar, adapter, log shipper. Separate deployments: independent scale and lifecycle.",
      "**Same pod:** App + Envoy sidecar, app + log forwarder.\n\n**Separate:** Orders API and Catalog API — different scale curves.\n\n**Architect:** Default one primary app container per pod; add sidecars deliberately with resource accounting.",
      "Pod = atomic scheduling unit — co-scheduled containers.",
      "Share process namespace? — `shareProcessNamespace: true` — debug niche.",
      "Adapter container pattern? — Normalize legacy protocol.",
      "Two microservices in one pod for convenience",
      "Sidecar without resource limits",
      "Scale pod because sidecar needs scale not app"),
    q("Ephemeral Containers Debug", "Kubernetes", "Common",
      "How debug distroless .NET pod without shell using ephemeral containers?",
      "`kubectl debug` attaches ephemeral container with shell/tools into running pod namespace — no restart required.",
      "```bash\nkubectl debug -it pod/orders-api --image=mcr.microsoft.com/dotnet/sdk:8.0 --target=orders-api --share-processes\n```\n\n**Architect:** Enable EphemeralContainers feature (GA); RBAC restrict debug; audit trail; prefer logs/traces over prod debug.",
      "Ephemeral debug is break-glass for minimal images.",
      "Copy debug tools via kubectl cp? — Alternative without ephemeral.",
      "Network capture sidecar? — tcpdump ephemeral for connectivity.",
      "Restart prod pod to add debug image permanently",
      "Unrestricted debug for all developers in prod",
      "No audit of ephemeral container usage"),
    q("K8s API Server", "Kubernetes", "Occasional",
      "Role of Kubernetes API server in control plane?",
      "API server is front door — validates, persists to etcd, admits resources. All kubectl, controllers, schedulers communicate via API.",
      "**Architect:** Secure API — private cluster, authorized IPs, Azure AD auth. Rate limit abusive clients. Audit logging enabled for compliance.\n\n**High availability:** Multiple API server instances behind load balancer in managed AKS.",
      "API server security is cluster security perimeter.",
      "API aggregation? — Extension APIs — metrics, custom metrics.",
      "Watch vs list? — Controllers watch for changes — efficient.",
      "Public API server unauthenticated",
      "Disable audit logs",
      "Direct etcd access for app config"),
    q("etcd Role", "Kubernetes", "Occasional",
      "What does etcd store and why backup matters on AKS?",
      "etcd is K8s backing store — all cluster state: pods, secrets, configmaps, deployments. AKS manages etcd — you backup cluster state via Azure Backup or Velero.",
      "**Architect:** etcd loss = cluster brain loss. Use Velero for resource backup; Azure Backup for AKS; test restore quarterly. Application data backup separate (SQL, Blob).",
      "Cluster backup ≠ application data backup.",
      "Velero? — Backup K8s resources to Blob — migration and DR.",
      "etcd encryption? — AKS encrypt secrets at rest.",
      "Assume AKS backup covers app databases",
      "Never test cluster restore",
      "Store critical state only in ConfigMap without backup"),
    q("Scheduler Function", "Kubernetes", "Common",
      "How does Kubernetes scheduler place pods on nodes?",
      "Filters (resource, taints, affinity) → score nodes → bind pod. Considers requests, affinity/anti-affinity, topology spread, priority.",
      "**Architect controls:**\n- `nodeSelector` / `nodeAffinity` — GPU, spot\n- `podAntiAffinity` — spread replicas across AZ\n- `topologySpreadConstraints` — even distribution\n- `PriorityClass` — preempt lower priority if needed",
      "Scheduling affects HA and cost — not random.",
      "Custom scheduler? — Scheduler framework plugins — advanced.",
      "Descheduler? — Rebalance pods after node changes.",
      "No requests — scheduler blind to memory needs",
      "All pods on single AZ",
      "Ignore taints on specialized pools"),
    q("Kubelet Responsibilities", "Kubernetes", "Occasional",
      "What does kubelet do on each AKS node?",
      "Kubelet: register node, watch API for pod specs, run containers via CRI (containerd), report status, run probes, mount volumes.",
      "**Architect relevance:** Kubelet failures → NodeNotReady → pod eviction. Node problems surface in `kubectl describe node` — disk pressure, memory pressure, PID pressure.\n\n**Patching:** AKS node image updates recycle kubelets — plan surge.",
      "Node health starts with kubelet — understand pressure signals.",
      "CRI-O vs containerd? — AKS uses containerd.",
      "Kubelet config? — Eviction thresholds — tune for workloads.",
      "Ignore NodeNotReady alerts",
      "Disk pressure from container logs unrotated",
      "Manual containerd bypass kubelet"),
    q("Container Runtime containerd", "Kubernetes", "Common",
      "What is containerd and how does it relate to Docker on AKS?",
      "containerd is CRI-compliant runtime pulling images and running containers. AKS uses containerd directly — Docker CLI optional for dev only.",
      "**Flow:** kubelet → CRI → containerd → runc → container.\n\n**Architect:** Build images in CI (ACR Tasks), not on nodes. `crictl` for node-level debug. Docker Desktop dev still fine — image format compatible.",
      "Runtime layer changed — Docker daemon not on AKS nodes.",
      "nerdctl? — Docker-compatible CLI for containerd nodes.",
      "Runtime class? — gVisor/Kata via RuntimeClass resource.",
      "SSH to node and docker run",
      "Assume Docker socket on AKS worker",
      "Build images on production nodes"),
    q("K8s Security Contexts", "Kubernetes", "Very Common",
      "Configure securityContext for production .NET pod?",
      "runAsNonRoot, readOnlyRootFilesystem, drop ALL capabilities, allowPrivilegeEscalation: false, seccompProfile RuntimeDefault.",
      "```yaml\nsecurityContext:\n  runAsNonRoot: true\n  runAsUser: 1654\n  readOnlyRootFilesystem: true\n  allowPrivilegeEscalation: false\n  capabilities:\n    drop: [ALL]\n```\n\n**Architect:** Pod Security Standards `restricted` enforced via admission; platform chart defaults secure context.",
      "securityContext is pod-level CIS compliance.",
      "fsGroup for volume write? — Supplemental group for mounted volumes.",
      "Capabilities add NET_BIND_SERVICE? — Only if binding <1024 — prefer >1024.",
      "Privileged true for file access",
      "Run as root with writable root FS",
      "No security context — platform default only"),
    q("NetworkPolicy Basics", "Kubernetes", "Common",
      "Design NetworkPolicy for zero-trust between namespaces?",
      "Default deny all ingress/egress; allow explicit: ingress from ingress-nginx namespace, egress to kube-dns and Azure SQL private endpoint.",
      "```yaml\nspec:\n  podSelector: {}\n  policyTypes: [Ingress, Egress]\n  ingress:\n  - from: [{ namespaceSelector: { matchLabels: { name: ingress } } }]\n```\n\n**Architect:** Azure CNI + network policy addon (Calico or Azure NPM); test policies in staging — misconfig causes silent failures.",
      "NetworkPolicy is micro-segmentation inside cluster.",
      "Egress to internet? — Explicit allow Azure services tags or proxy.",
      "Cilium network policies? — L7 policies — advanced.",
      "No NetworkPolicy — flat cluster trust",
      "Policy blocks DNS — all egress fails",
      "Copy policy without testing staging"),
    q("K8s Learning Path for Architects", "Kubernetes", "Common",
      "Recommended Kubernetes learning path for .NET architects?",
      "CKA fundamentals → deploy sample .NET API → ingress/TLS → observability → GitOps → security (PSA, NetworkPolicy) → AKS production checklist.",
      "**Phases:**\n1. **Week 1-2:** Pods, Deployments, Services, kubectl fluency\n2. **Week 3-4:** Helm, probes, resources, ConfigMap/Secret\n3. **Week 5-6:** AKS — node pools, ACR, Workload Identity\n4. **Week 7-8:** HPA, PDB, upgrades, DR, cost\n\n**Architect:** Build platform reference app; whiteboard pod networking; read AKS well-architected.",
      "Architects need breadth across app, platform, and ops — not only kubectl trivia.",
      "CKA vs CKAD vs CKS? — CKA admin; CKAD app dev; CKS security — architect benefits CKA+CKS awareness.",
      "Local kind cluster enough? — Supplement with AKS lab for Azure integration.",
      "Certification without hands-on deploy",
      "Only YAML memorization",
      "Skip Linux and networking fundamentals"),
]

WEEK_27 = [
    q("Horizontal Pod Autoscaler", "Kubernetes", "Very Common",
      "Design HPA for .NET API on AKS — metrics and pitfalls?",
      "HPA scales Deployment replicas based on metrics — typically CPU/memory or custom (requests/sec). Needs resource requests set; min/max bounds; scale-down stabilization.",
      "```yaml\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nspec:\n  minReplicas: 3\n  maxReplicas: 20\n  metrics:\n  - type: Resource\n    resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }\n```\n\n**Architect:** CPU alone insufficient for IO-bound — use KEDA or custom metrics from Prometheus. Cooldown prevents flapping.",
      "HPA is default scale mechanism — configure with load test validation.",
      "HPA v2 behavior? — Multiple metrics — max of recommendations.",
      "Scale to zero? — HPA min 1 — use KEDA for zero.",
      "HPA without resource requests",
      "maxReplicas unlimited — cost surprise",
      "CPU target 90% — no headroom for spikes"),
    q("Vertical Pod Autoscaler", "Kubernetes", "Common",
      "When use Vertical Pod Autoscaler vs HPA?",
      "VPA adjusts CPU/memory requests per pod — right-sizing. HPA adds replicas. Use VPA in recommendation mode first; avoid VPA + HPA on same CPU metric conflict.",
      "**Modes:** Off (recommend only), Initial, Auto (restart pods to apply).\n\n**Architect:** Run VPA recommender — review recommendations quarterly; apply to batch jobs; for APIs prefer HPA + manual request tuning after load test.",
      "VPA solves 'wrong request size' not 'need more pods'.",
      "VPA updater eviction? — Auto mode restarts pods — plan disruption.",
      "Goldilocks? — Dashboard for VPA recommendations.",
      "VPA Auto on prod without PDB",
      "VPA and HPA same CPU metric",
      "Ignore VPA recommendations forever"),
    q("KEDA Event Scaling", "Kubernetes", "Very Common",
      "Scale .NET worker from zero using KEDA on Azure Service Bus?",
      "KEDA ScaledObject watches queue depth — scales Deployment 0→N. No messages = zero replicas — cost savings.",
      "```yaml\nspec:\n  scaleTargetRef: { name: order-worker }\n  minReplicaCount: 0\n  maxReplicaCount: 30\n  triggers:\n  - type: azure-servicebus\n    metadata:\n      queueName: orders\n      messageCount: \"5\"\n```\n\n**Architect:** KEDA on AKS via add-on; idempotent workers mandatory; cold start acceptable for async workloads.",
      "KEDA enables event-driven scale including zero — cloud cost optimization.",
      "KEDA vs HPA? — KEDA for external metrics and scale-to-zero.",
      "Multiple triggers? — Scale on max of queue depth and CPU.",
      "Non-idempotent worker at scale",
      "minReplicaCount 0 with strict latency SLA",
      "No dead-letter handling at scale-out"),
    q("GitOps Argo CD", "Kubernetes", "Very Common",
      "Implement GitOps with Argo CD for .NET microservices on AKS?",
      "Git repo is source of truth — Argo CD syncs cluster state to manifests/Helm. PR merge triggers deploy; drift detection and rollback via Git revert.",
      "**Architecture:**\n- Repo: `deploy/orders-api/overlays/prod`\n- Argo Application per service or app-of-apps\n- Image updater or CI commits new tag to Git\n\n**Architect:** RBAC on Argo; secrets via External Secrets; progressive sync waves for CRDs first.",
      "GitOps provides audit trail and reproducible deploys.",
      "App of apps pattern? — Bootstrap cluster apps from one root Application.",
      "Sync hooks? — PreSync migration Job.",
      "kubectl apply from laptop prod",
      "Manual cluster changes not reverted",
      "Secrets in Git plain text"),
    q("Flux vs Argo", "Kubernetes", "Common",
      "Compare Flux and Argo CD for AKS GitOps platform?",
      "Argo CD: UI-first, application-centric, strong multi-cluster dashboard. Flux: CNCF, controller-native, integrates with Helm/Kustomize, lighter UI.",
      "**Argo strengths:** Visibility for many teams, RBAC projects, sync waves.\n\n**Flux strengths:** Git-native composition, Terraform integration, smaller footprint.\n\n**Architect:** Pick one per org — both excellent. Azure GitOps (Flux-based) is managed option on AKS.",
      "Tool choice less important than GitOps discipline.",
      "Azure GitOps Flux extension? — Managed install on AKS.",
      "Multi-cluster Argo? — ApplicationSet — fleet deploy.",
      "Both Flux and Argo same cluster fighting",
      "No drift detection enabled",
      "GitOps without PR review"),
    q("Service Mesh Istio Basics", "Kubernetes", "Common",
      "When adopt Istio service mesh for .NET microservices?",
      "When you need: mTLS east-west, traffic splitting canary, unified telemetry, retries/timeouts as policy without code change — and team can operate mesh complexity.",
      "**Components:** Control plane (istiod) + data plane sidecar (Envoy).\n\n**Architect:** Start without mesh; add when 8+ services need mTLS policy; evaluate ambient mesh for reduced sidecar overhead.",
      "Service mesh is platform investment — not day-one default.",
      "Linkerd vs Istio? — Linkerd simpler lighter — Istio feature-rich.",
      "Dapr vs mesh? — Dapr app-level building blocks — can coexist.",
      "Mesh for two services",
      "No mesh observability before adoption",
      "Sidecar resource cost ignored in capacity plan"),
    q("mTLS East-West", "Kubernetes", "Very Common",
      "Why mTLS for east-west traffic in Kubernetes?",
      "Encrypts pod-to-pod traffic — zero-trust inside cluster. Prevents lateral eavesdropping if attacker compromises one pod.",
      "**Istio:** PeerAuthentication STRICT — auto mTLS. **Without mesh:** manual cert distribution impractical at scale.\n\n**Architect:** Compliance (PCI, HIPAA) may require encryption in transit even 'internal'. Document performance overhead (~1-5% latency).",
      "East-west mTLS is zero-trust inside the perimeter.",
      "SPIFFE/SPIRE? — Identity framework underlying mesh certs.",
      "PERMISSIVE mode? — Migration — not prod end state.",
      "TLS only at ingress — plaintext inside cluster",
      "Shared cert all services — no identity",
      "mTLS without rotation plan"),
    q("Canary with Argo Rollouts", "Kubernetes", "Common",
      "Implement canary deployment for .NET API using Argo Rollouts?",
      "Argo Rollouts replaces Deployment — progressive traffic shift 10%→50%→100% with analysis gates on error rate and latency.",
      "**Steps:**\n1. Install Rollouts controller\n2. Define Rollout with canary steps and AnalysisTemplate (Prometheus metrics)\n3. Ingress/Service mesh splits traffic\n\n**Architect:** Automated rollback on SLO breach; manual promotion gate for high-risk changes.",
      "Canary reduces blast radius vs big-bang rolling update.",
      "Flagger vs Rollouts? — Flagger similar — GitOps native.",
      "Blue/green vs canary? — Blue/green instant switch — higher resource cost.",
      "Canary without metrics analysis",
      "10% canary on untested error budget",
      "No rollback automation"),
    q("Pod Disruption Budget", "Kubernetes", "Very Common",
      "Configure PDB during AKS node drain and cluster upgrade?",
      "PDB ensures minAvailable or maxUnavailable pods during voluntary disruptions — node drain, rollout.",
      "```yaml\napiVersion: policy/v1\nkind: PodDisruptionBudget\nspec:\n  minAvailable: 2\n  selector:\n    matchLabels: { app: orders-api }\n```\n\n**Architect:** PDB + minReplicas ≥ minAvailable + 1; without PDB node upgrade kills too many pods — outage.",
      "PDB protects availability during planned maintenance.",
      "minAvailable vs maxUnavailable? — Choose one — math with replicas.",
      "PDB blocks node drain? — Expected — scale up or adjust temporarily.",
      "No PDB on critical API",
      "minAvailable equals replica count — no drains possible",
      "PDB only on Deployment not StatefulSet"),
    q("Cluster Autoscaling", "Kubernetes", "Very Common",
      "How cluster autoscaler works on AKS with multiple node pools?",
      "Pending unschedulable pods trigger scale up; underutilized nodes scale down after cooldown. Per node pool autoscale profile.",
      "**Settings:**\n- `min-count`, `max-count` per pool\n- Balance similar node groups\n- PodDisruptionBudget affects drain speed\n\n**Architect:** Set max to prevent runaway cost; separate spot pool autoscale; alert on failed scale-up (quota, subnet full).",
      "Cluster autoscaler links pod demand to infrastructure cost.",
      "Scale-up failures? — Azure quota, subnet IP exhaustion — monitor.",
      "Over-provisioning? — Placeholder pods reserve capacity — advanced.",
      "max-count unlimited",
      "Single node pool autoscale only",
      "Ignore scale-up failures in alerts"),
    q("AKS Node Pool Taints", "Kubernetes", "Common",
      "Use taints and tolerations for specialized AKS node pools?",
      "Taint repels pods unless toleration matches — dedicates nodes to workloads (GPU, spot, system).",
      "**Example:** Spot pool taint `kubernetes.azure.com/scalesetpriority=spot:NoSchedule` — only batch workers with toleration schedule there.\n\n**Architect:** System pool taint protects addons; document toleration in Helm chart values for spot workloads.",
      "Taints enforce workload placement policy.",
      "NoSchedule vs PreferNoSchedule? — Hard vs soft eviction.",
      "Taint by default AKS? — CriticalAddonsOnly on system pool.",
      "Spot workload without toleration — never schedules",
      "Taint user pool blocking all apps",
      "No taint on GPU pool — wrong workloads scheduled"),
    q("Spot Instances Workloads", "Kubernetes", "Common",
      "Which workloads suit AKS spot node pools?",
      "Fault-tolerant batch: report generation, image processing, CI runners, queue workers with retry — not synchronous API frontends.",
      "**Pattern:** Pod disruption toleration + graceful shutdown + checkpoint progress. Mix spot + on-demand pools — critical on on-demand.\n\n**Architect:** Cost savings 60-90% — design for eviction notice (30s).",
      "Spot is cost lever for interruptible compute.",
      "Spot eviction handler? — Save state on SIGTERM.",
      "Spot-only API Deployment? — SLA risk — avoid.",
      "Stateful API on spot without fallback",
      "Ignore eviction grace period",
      "No retry on spot preemption"),
    q("Multi-Zone Deployment", "Kubernetes", "Very Common",
      "Design multi-AZ deployment for .NET API on AKS?",
      "Spread nodes across availability zones; pod anti-affinity or topologySpreadConstraints; zone-redundant Load Balancer; external dependencies multi-AZ.",
      "```yaml\ntopologySpreadConstraints:\n- maxSkew: 1\n  topologyKey: topology.kubernetes.io/zone\n  whenUnsatisfiable: DoNotSchedule\n```\n\n**Architect:** Azure SLA requires zones for 99.95%+; test single-AZ failure; SQL/Redis zone redundancy separate concern.",
      "AZ spread is baseline HA for regional services.",
      "ZRS storage? — Zone redundant for PVC backup targets.",
      "Cross-zone latency? — Usually negligible within region.",
      "All nodes single AZ",
      "Replicas 2 across 3 zones — uneven spread",
      "Ignore dependency AZ redundancy"),
    q("Cluster Upgrades Strategy", "Kubernetes", "Very Common",
      "Plan AKS Kubernetes version upgrade with minimal risk?",
      "Upgrade control plane first, then node pools one at a time with surge. Test in staging cluster; read release notes for breaking API changes.",
      "**Process:**\n1. Staging cluster on target version — run integration tests\n2. Upgrade dev → staging → prod clusters\n3. Node image upgrade with `maxSurge` — PDB respected\n4. Pin deprecated API versions before upgrade\n\n**Architect:** Stay within N-2 supported versions; automate `pluto` API deprecation scan.",
      "Cluster upgrade is recurring platform event — plan not panic.",
      "Auto-upgrade channels? — patch vs stable — choose policy.",
      "Blue cluster migration? — New cluster cutover for major jumps.",
      "Skip staging upgrade test",
      "Upgrade prod control plane Friday 5pm",
      "Deprecated APIs break workloads post-upgrade"),
    q("etcd Backup", "Kubernetes", "Common",
      "Backup strategy for AKS cluster state beyond Azure-managed etcd?",
      "Velero backs up K8s resources to Blob; Azure Backup for AKS; export critical manifests in Git (GitOps). Application data separate.",
      "**Velero schedule:** Daily namespace backup; retention 30 days; test restore to dev cluster quarterly.\n\n**Architect:** DR runbook: restore cluster + redeploy apps from Git + restore SQL from backup.",
      "Cluster backup enables migration and disaster recovery of K8s config.",
      "Velero volume snapshots? — PVC snapshot — coordinate with Azure Disk.",
      "Backup encryption? — Customer-managed keys in Blob.",
      "Only GitOps — no Velero for cluster-scoped resources",
      "Never test restore",
      "Confuse etcd backup with database backup"),
    q("Disaster Recovery AKS", "Kubernetes", "Very Common",
      "Design DR for critical .NET platform on AKS?",
      "Active-passive secondary region: ACR geo-replication, Terraform/Bicep infra, GitOps redeploy, SQL geo-replication, Front Door failover, RTO/RPO defined.",
      "**Tiers:**\n- **RPO 1h:** Async DB replicate + Velero + GitOps\n- **RTO 4h:** Documented runbook + quarterly drill\n\n**Architect:** DR is tested procedure not diagram — failover drill mandatory.",
      "AKS DR spans registry, DNS, data, and Git — not just cluster.",
      "Multi-region active-active? — Complex — conflict resolution — rare.",
      "Chaos failover test? — Game day validates runbook.",
      "DR slide deck never tested",
      "Single region ACR no geo-rep",
      "RTO undefined — no prioritization"),
    q("Cost Optimization K8s", "Kubernetes", "Common",
      "Top Kubernetes cost optimizations on Azure for .NET workloads?",
      "Right-size requests, KEDA scale-to-zero, spot pools for batch, cluster autoscaler max bounds, reserved instances for baseline nodes, ACR retention, off-hours dev cluster shutdown.",
      "**FinOps:**\n- VPA recommendations → reduce over-provisioned requests\n- Remove unused LoadBalancers and orphaned PVCs\n- Azure Cost Management tags by namespace label\n\n**Architect:** Monthly cost review per team namespace; chargeback model.",
      "K8s cost surprises come from idle capacity and forgotten resources.",
      "Kubecost? — Allocation visibility by label.",
      "Dev cluster schedule? — Start/stop nights — 50% savings.",
      "Requests 4x actual usage",
      "LoadBalancer per service",
      "No namespace cost allocation"),
    q("Resource Quotas", "Kubernetes", "Common",
      "Apply ResourceQuota per team namespace on AKS?",
      "ResourceQuota caps total CPU/memory/pods/objects per namespace — prevents one team consuming cluster.",
      "```yaml\nspec:\n  hard:\n    requests.cpu: \"20\"\n    requests.memory: 40Gi\n    pods: \"50\"\n```\n\n**Architect:** Pair with LimitRange for default container limits; quota aligned to cluster capacity / team allocation.",
      "Quotas enforce fair sharing on multi-tenant platform.",
      "Object count quotas? — Limit ConfigMaps, Services explosion.",
      "Quota increase process? — Ticket with capacity review.",
      "No quotas — one team exhausts cluster",
      "Quota too low — legitimate deploy blocked silently",
      "Quota without LimitRange — pods with no limits"),
    q("Limit Ranges", "Kubernetes", "Common",
      "What does LimitRange provide vs ResourceQuota?",
      "LimitRange sets default/min/max per container or pod in namespace — ResourceQuota caps namespace total.",
      "**Example:** Default request 100m CPU if unspecified; max single pod 4 CPU — prevents typo `cpu: 100`.\n\n**Architect:** Admission rejects pods violating limits — safer than runtime OOM surprises.",
      "LimitRange catches misconfigured manifests early.",
      "Default vs defaultRequest? — Request for scheduling; limit for cap.",
      "PVC LimitRange? — Min/max storage per claim.",
      "No defaults — BestEffort pods evicted",
      "Max limit blocks legitimate batch job",
      "LimitRange and quota conflict unreviewed"),
    q("Admission Controllers", "Kubernetes", "Common",
      "What admission controllers enforce on AKS and why architects care?",
      "Mutating and validating webhooks intercept API requests — enforce labels, security context, image registry allowlist, resource requirements before pod schedules.",
      "**Built-in:** NamespaceLifecycle, ResourceQuota, PodSecurity.\n\n**Custom:** OPA Gatekeeper, Kyverno — policy as code.\n\n**Architect:** Platform policies: deny `:latest`, require probes, require non-root, allowed registries only.",
      "Admission is policy enforcement gate — shift-left security.",
      "Validating vs mutating? — Mutate injects sidecar/defaults; validate rejects.",
      "Webhook failure policy? — Fail closed for security policies.",
      "No admission policies — any image deploys",
      "Webhook timeout blocks all deploys",
      "Policies without exception workflow"),
    q("OPA Gatekeeper", "Kubernetes", "Common",
      "Example Gatekeeper policies for AKS .NET platform?",
      "ConstraintTemplates + Constraints: require labels, block privileged, require resource limits, allowed ACR registry prefix only.",
      "```rego\n# Pseudologic: image must start with mycompany.azurecr.io\nviolation[{...}] {\n  not startswith(input.review.object.spec.containers[_].image, \"mycompany.azurecr.io/\")\n}\n```\n\n**Architect:** Start with 5 critical constraints; expand; dry-run audit mode before enforce.",
      "OPA Gatekeeper is policy-as-code for K8s.",
      "Kyverno vs Gatekeeper? — Kyverno YAML-native — easier for some teams.",
      "Azure Policy for AKS? — Managed Gatekeeper integration.",
      "Policy sprawl 100 constraints day one",
      "Enforce without audit dry-run",
      "No exemption process for emergencies"),
    q("Secrets CSI Driver", "Kubernetes", "Very Common",
      "Integrate Azure Key Vault with AKS using Secrets Store CSI Driver?",
      "CSI driver mounts Key Vault secrets as files in pod; sync to K8s Secret optional; Workload Identity authenticates without client secret.",
      "**Flow:**\n1. Enable Workload Identity on AKS\n2. SecretProviderClass references Key Vault secrets\n3. Pod volume mount → `/mnt/secrets-store`\n4. .NET reads file or synced K8s Secret\n\n**Architect:** Rotation without pod restart if app supports reload; prefer file mount over env for rotation.",
      "CSI driver eliminates long-lived secrets in Git and images.",
      "Rotation poll interval? — Driver refreshes — app must reload.",
      "Multiple Key Vaults? — SecretProviderClass per vault region.",
      "Key Vault secrets in GitOps YAML",
      "Client secret in cluster for Key Vault",
      "No rotation — static mounted secret forever"),
    q("Workload Identity AKS", "Kubernetes", "Very Common",
      "Configure Workload Identity for .NET pod accessing Azure SQL and Blob?",
      "Federated identity credential links K8s SA to Entra app — pod gets token via OIDC — no secrets in cluster.",
      "**Steps:**\n1. AKS OIDC issuer enabled\n2. Entra app + federated credential (namespace, SA name)\n3. ServiceAccount annotation `azure.workload.identity/client-id`\n4. .NET `DefaultAzureCredential` in pod\n\n**Architect:** Replace pod-managed identity and secrets; audit federated credentials per service.",
      "Workload Identity is standard Azure-K8s auth pattern.",
      "DefaultAzureCredential chain? — Works in pod with federated token.",
      "Multi-tenant? — Separate app registration per environment.",
      "Connection string with password in K8s Secret",
      "Shared Entra app all microservices",
      "Federated credential wildcard namespace"),
    q("Ingress TLS cert-manager", "Kubernetes", "Common",
      "Automate TLS certificates for AKS ingress with cert-manager?",
      "cert-manager issues and renews certs from Let's Encrypt or private CA — stores in Secret; ingress references `tls.secretName`.",
      "```yaml\nannotations:\n  cert-manager.io/cluster-issuer: letsencrypt-prod\n```\n\n**Architect:** DNS-01 for wildcard; HTTP-01 for simple; monitor cert expiry alerts as backup; private CA for internal services.",
      "Automated TLS prevents expiry outages.",
      "cert-manager vs App Gateway managed cert? — Choose per ingress layer.",
      "Rate limits Let's Encrypt? — Staging issuer for testing.",
      "Manual cert paste yearly",
      "Expired cert outage no alert",
      "TLS only on external not internal east-west"),
    q("External DNS", "Kubernetes", "Common",
      "How External DNS manages Azure DNS for Kubernetes ingress?",
      "External DNS controller watches Ingress/Service — creates/updates A records in Azure DNS zone automatically.",
      "**Flow:** Ingress `host: api.mycompany.com` → External DNS → A record to LoadBalancer IP.\n\n**Architect:** TXT registry ownership records; restrict DNS zone RBAC; GitOps still owns ingress manifest — DNS follows.",
      "External DNS removes manual DNS ticket per deploy.",
      "Private DNS zone? — Internal ingress — private link integration.",
      "DNS TTL? — Lower for faster failover — higher for cache.",
      "Manual DNS per service deploy",
      "Wrong zone — prod record in dev zone",
      "No ownership TXT — orphan records"),
    q("Chaos Engineering K8s", "Kubernetes", "Occasional",
      "Run chaos experiments safely on AKS staging?",
      "Use Chaos Mesh or Litmus to kill pods, inject network latency, fill disk — in staging with blast radius controls and steady-state hypothesis.",
      "**Example experiment:** Kill 1 orders-api pod — hypothesis: error rate <0.1%, P99 <500ms.\n\n**Architect:** Game days quarterly; never chaos in prod without mature observability and rollback; start with pod delete not network partition.",
      "Chaos validates resilience assumptions before real incidents.",
      "Azure Chaos Studio? — Managed fault injection — VM and AKS.",
      "Blast radius? — Namespace isolated staging — not shared.",
      "Chaos in prod first experiment",
      "No steady-state metrics during chaos",
      "Chaos without PDB understanding"),
    q("Multi-Cluster Strategy", "Kubernetes", "Common",
      "When operate multiple AKS clusters vs one large cluster?",
      "Multi-cluster: blast isolation (prod/dev), regulatory boundaries, region presence, team autonomy. Single cluster: cost efficiency, simpler ops — namespaces isolate.",
      "**Patterns:**\n- **Prod cluster + non-prod cluster** — common minimum\n- **Cluster per region** — data residency\n- **Fleet management** — GitOps ApplicationSet\n\n**Architect:** Avoid cluster sprawl without automation — each cluster needs upgrades and monitoring.",
      "Cluster count is org and compliance decision.",
      "Hub-spoke clusters? — Centralized ingress/logging hub.",
      "Cluster API? — Unified K8s API across clusters — advanced.",
      "20 clusters without GitOps automation",
      "Single prod cluster no env isolation",
      "Multi-cluster without fleet visibility"),
    q("Fleet Management", "Kubernetes", "Occasional",
      "What is Kubernetes fleet management for architects?",
      "Unified governance across clusters — Azure Fleet Manager, Argo CD ApplicationSet, Cluster API — consistent policy, upgrade orchestration, multi-cluster deploy.",
      "**Azure Fleet:** Update orchestration across member clusters; resource propagation.\n\n**Architect:** Platform team operates fleet; application teams deploy via GitOps to assigned clusters; central policy (Azure Policy, OPA) synced.",
      "Fleet scales GitOps and policy to many clusters.",
      "Hub cluster pattern? — Management cluster deploys to spokes.",
      "Fleet upgrade waves? — Canary cluster first — then fleet.",
      "Manual kubectl each cluster upgrade",
      "Inconsistent versions across fleet",
      "No central policy sync"),
    q("K8s Observability Stack", "Kubernetes", "Very Common",
      "Design observability stack for .NET on AKS?",
      "Metrics: Prometheus + Grafana or Azure Monitor managed Prometheus. Logs: Fluent Bit → Log Analytics. Traces: OpenTelemetry → App Insights/Jaeger. Alerts on SLO burn rate.",
      "**Layers:**\n- **Infra:** Node metrics, kube-state-metrics\n- **App:** OTel SDK in .NET — traces correlate logs\n- **Synthetic:** Availability tests hit ingress\n\n**Architect:** Standard dashboards per service golden signals; alert runbooks linked.",
      "Observability is platform mandate — not per-team optional.",
      "Managed Grafana on Azure? — Integrated with AMP and AMW.",
      "eBPF monitoring? — Cilium Hubble — network observability.",
      "Logs only no metrics",
      "No trace propagation across services",
      "Alert on every pod restart without SLO context"),
    q("Production Readiness Checklist AKS", "Kubernetes", "Very Common",
      "Production readiness checklist before .NET API on AKS go-live?",
      "Probes, resources, PDB, HPA, NetworkPolicy, Workload Identity, image scan gate, GitOps deploy, DR tested, observability, runbooks, capacity tested, security context restricted.",
      "**Checklist categories:**\n1. **Reliability:** replicas≥3, multi-AZ, PDB, graceful shutdown\n2. **Security:** non-root, WI, NetworkPolicy, no secrets in Git\n3. **Ops:** on-call runbook, alerts, upgrade path\n4. **Performance:** load test at 2× expected traffic\n5. **Compliance:** SBOM, audit logs, backup verified\n\n**Architect:** Sign-off gate — no exceptions without risk acceptance.",
      "Production readiness is explicit gate — not implicit hope.",
      "Production readiness review template? — Platform team document — per service.",
      "Go-live without game day? — Schedule post-launch hypercare.",
      "Missing readiness probe go-live",
      "Single replica production",
      "No load test evidence in ADR"),
]

WEEK_28 = [
    q("Essential File Permissions", "Linux", "Very Common",
      "Explain Linux file permissions and architect relevance for container hosts?",
      "rwx for owner/group/others (octal 755, 640). Directories need x to traverse. Principle of least privilege on config, keys, and scripts.",
      "**Key checks:**\n```bash\nls -la /etc/shadow  # should be root-only\nfind / -perm -4000  # SUID binaries audit\n```\n\n**Architect:** AKS nodes — restrict SSH; app configs 640; no world-writable `/tmp` exploits. Container: non-root USER aligns with file ownership in image.",
      "Permission mistakes are common privilege escalation path.",
      "chmod 777 fix? — Never in prod — diagnose owner/group instead.",
      "ACLs getfacl? — Fine-grained beyond rwx — enterprise filesystems.",
      "chmod 777 on app directory",
      "Secrets world-readable in /etc",
      "Run container as root to avoid permission issues"),
    q("Process Management ps top", "Linux", "Very Common",
      "Use ps and top to diagnose high CPU on Linux AKS node?",
      "`top` — live CPU/memory; `ps aux --sort=-%cpu | head` — snapshot ranking; `ps -ef` — full process tree.",
      "**Workflow:**\n1. `top` — load average, which PID hot\n2. `ps -p PID -o pid,ppid,cmd` — identify process\n3. Map to container: `crictl ps` on AKS node\n\n**Architect:** Distinguish node problem vs pod problem — drain node if kubelet healthy but noisy neighbor pod.",
      "Process tools bridge host and container troubleshooting.",
      "htop/atop? — Better UX — atop records for post-mortem.",
      "PID namespace? — Container PIDs differ from host view — use crictl.",
      "Kill -9 first without identifying process",
      "Ignore load average vs CPU count",
      "Debug prod node without change control"),
    q("systemd Services", "Linux", "Common",
      "How does systemd relate to AKS nodes and Linux VMs?",
      "systemd is init system — manages units (services, timers, mounts). AKS nodes run kubelet, containerd as systemd units.",
      "**Commands:**\n```bash\nsystemctl status kubelet\njournalctl -u containerd -f\nsystemctl restart kubelet  # caution prod\n```\n\n**Architect:** On VMs (jump boxes, build agents): define services as unit files — `Restart=on-failure`, not manual nohup.",
      "systemd is standard service lifecycle on Linux infrastructure.",
      "systemd vs Docker restart policy? — Different layers — kubelet manages pods.",
      "Unit file hardening? — User=, ProtectSystem= for services.",
      "nohup java & on production VM",
      "Restart kubelet without understanding impact",
      "Disable systemd logging"),
    q("journalctl Logging", "Linux", "Very Common",
      "Query systemd journal logs for kubelet failure on AKS node?",
      "`journalctl -u kubelet --since '1 hour ago' -p err` — errors only; `-f` follow; `--no-pager` for scripts.",
      "**Examples:**\n```bash\njournalctl -u kubelet -b  # current boot\njournalctl -k  # kernel messages — OOM killer\n```\n\n**Architect:** Correlate journal timestamps with AKS control plane events and pod events. Export to Log Analytics for retention beyond local journal size.",
      "journalctl is first stop for node-level failures.",
      "journal vacuum? — Disk full — limit journal size in /etc/systemd/journald.conf.",
      "Structured fields? — journalctl -o json — parsing.",
      "grep /var/log without checking journal",
      "Ignore kernel OOM in journal",
      "No central log shipping from nodes"),
    q("Networking ip ss curl", "Linux", "Very Common",
      "Diagnose connectivity from Linux pod to Azure SQL using ip, ss, curl?",
      "`ip addr/route` — interfaces and routing; `ss -tlnp` — listening ports; `curl -v telnet://host:1433` — TCP reachability.",
      "**Workflow:**\n1. `nslookup sqlserver.database.windows.net` — DNS resolves?\n2. `curl -v --connect-timeout 5 telnet://...` — port open?\n3. `traceroute` / `mtr` — path issues\n4. `ip route` — private endpoint route exists?\n\n**Architect:** NetworkPolicy egress, NSG, private DNS zone — systematic elimination.",
      "Linux network CLI is essential for connectivity triage.",
      "ss vs netstat? — ss faster modern replacement.",
      "dig +trace? — DNS delegation debug.",
      "ping only — ICMP blocked doesn't mean TCP dead",
      "Assume DNS works without nslookup",
      "Skip TLS layer when debugging HTTPS"),
    q("DNS Resolution Debug", "Linux", "Common",
      "Debug DNS resolution failure from AKS pod to internal service?",
      "Check `/etc/resolv.conf`, `nslookup`, `dig`, CoreDNS logs, ndots search path misconfiguration.",
      "**Common issues:**\n- Wrong search domain — `orders` vs `orders.svc.cluster.local`\n- CoreDNS pod down — `kubectl get pods -n kube-system`\n- Azure Private DNS zone not linked to VNet\n\n**Architect:** Document required FQDN patterns; test DNS in CI smoke tests.",
      "DNS failures look like application bugs — isolate early.",
      "dig @10.0.0.10? — Query specific CoreDNS IP.",
      "ndots:5 issue? — Short name resolves externally wrongly.",
      "Hardcode IP bypassing DNS debt",
      "Ignore resolv.conf in pod",
      "Private endpoint DNS zone missing link"),
    q("SSH Hardening", "Linux", "Common",
      "SSH hardening for Linux jump boxes and emergency AKS node access?",
      "Key-only auth, disable root login, AllowUsers, fail2ban, non-standard port optional, audit logging, session timeout.",
      "**sshd_config:**\n```\nPermitRootLogin no\nPasswordAuthentication no\nAllowUsers ops@corp.com\n```\n\n**Architect:** AKS — prefer `az aks command invoke` or bastion over SSH to nodes; break-glass only with MFA and session recording.",
      "SSH is attack surface — minimize and audit.",
      "Bastion vs direct SSH? — Azure Bastion — no public IP on VM.",
      "Certificate-based SSH? — Short-lived certs — Teleport/Okta.",
      "Password SSH enabled",
      "Shared ops key on all servers",
      "Routine SSH to AKS nodes for app debug"),
    q("sudo Least Privilege", "Linux", "Common",
      "Configure sudo least privilege for ops team on Linux?",
      "sudoers per-command aliases — not ALL=(ALL) NOPASSWD:ALL. Use groups; log commands; require password for destructive ops.",
      "**Example sudoers:**\n```\n%ops ALL=(ALL) /bin/systemctl restart nginx, /usr/bin/journalctl\n```\n\n**Architect:** Prefer identity-based access (Azure RBAC, AKS admin via AAD) over shared root; audit `/var/log/auth.log`.",
      "sudo scope creep is insider threat vector.",
      "visudo? — Always edit sudoers safely — syntax check.",
      "sudo without logging? — Enable auditd session recording.",
      "NOPASSWD ALL for developers",
      "Shared root password",
      "sudo su - as daily workflow"),
    q("Package Management apt yum", "Linux", "Common",
      "Package management on Ubuntu AKS nodes vs container images?",
      "AKS nodes: apt for node OS patches (managed by AKS). Containers: install packages in Dockerfile build — not apt at runtime in prod.",
      "**Commands:**\n```bash\napt list --upgradable\napt-cache policy package\n```\n\n**Architect:** Don't SSH apt-upgrade nodes manually — use AKS node image upgrade. Pin package versions in Dockerfile `apt-get install package=version`.",
      "Patch strategy differs for nodes vs containers.",
      "unattended-upgrades? — VMs yes — AKS nodes via platform.",
      "RPM yum/dnf? — RHEL-based images — same principles.",
      "apt install in running prod container",
      "Manual node patching outside AKS channel",
      "Unpinned packages in Dockerfile — drift"),
    q("Shell Scripting Basics", "Linux", "Common",
      "Shell scripting patterns for architect automation?",
      "Use bash for glue: CI checks, backup scripts, health probes. `set -euo pipefail`, quote variables, log actions.",
      "```bash\n#!/bin/bash\nset -euo pipefail\nLOG_FILE=/var/log/deploy.log\nexec > >(tee -a \"$LOG_FILE\") 2>&1\necho \"$(date) Starting deploy\"\n```\n\n**Architect:** Prefer Python/PowerShell for complex logic; bash for kubectl wrappers; idempotent scripts safe to re-run.",
      "Script robustness prevents 3am automation failures.",
      "shellcheck? — Lint bash in CI.",
      "Idempotent scripts? — Check state before create.",
      "Unquoted $variables break on spaces",
      "set -e missing — silent failure",
      "Curl bash pipe install in prod script"),
    q("grep awk sed", "Linux", "Very Common",
      "Use grep, awk, sed for log analysis during incident?",
      "grep filters lines; awk columns and aggregates; sed stream edit — extract correlation IDs, count errors per minute.",
      "**Examples:**\n```bash\ngrep 'ERROR' app.log | wc -l\nawk '$9 ~ /^5/ {count++} END {print count}' access.log\ngrep -oP 'CorrelationId=\\K[^ ]+' app.log | sort | uniq -c\n```\n\n**Architect:** Prefer structured JSON logs + Log Analytics KQL at scale — CLI for quick SSH triage.",
      "CLI log slicing is incident triage skill.",
      "jq for JSON logs? — Better than grep on structured logs.",
      "zgrep? — Search compressed rotated logs.",
      "grep million-line file without time filter",
      "Regex catastrophic backtracking",
      "Only CLI — no centralized log query"),
    q("Disk Usage df du", "Linux", "Very Common",
      "Diagnose disk full on AKS node — df and du workflow?",
      "`df -h` — filesystem usage; `du -sh /var/*` — find large dirs; common: container logs, images, journal.",
      "**AKS:**\n```bash\ndf -h /var/lib/containerd\nctr images ls\ncrictl rmi --prune\n```\n\n**Architect:** Alert disk >80% on nodes; image GC policy; log rotation; PVC capacity separate from node disk.",
      "Disk full causes evictions and pod failures.",
      "du --inodes? — inode exhaustion rare but real.",
      "ncdu? — Interactive du navigator.",
      "Delete random files without identifying",
      "Ignore container image accumulation",
      "No disk alerts until 100%"),
    q("Memory Analysis free vmstat", "Linux", "Common",
      "Interpret free and vmstat during memory pressure on Linux?",
      "`free -h` — available memory (includes cache); `vmstat 1` — si/so swap, us/sy CPU, r runnable queue.",
      "**OOM:** Kernel kills process — `dmesg | grep -i oom`. **Container:** cgroup limit triggers OOMKill before node OOM.\n\n**Architect:** Distinguish node memory pressure (evictions) vs pod limit too low (.NET OOMKill).",
      "Memory metrics misread causes wrong scaling decisions.",
      "available vs free? — Linux uses cache — available matters.",
      "slab top? — Kernel cache — advanced tuning.",
      "free shows 0 free — panic without reading available",
      "Ignore swap activity si/so",
      "Raise pod limit without investigating leak"),
    q("CPU Profiling top htop", "Linux", "Common",
      "CPU profiling approach on Linux for .NET container?",
      "Host: `top -H -p PID` threads; `perf top` kernel/user. Container: `dotnet-trace`, `dotnet-counters` preferred over host perf for managed code.",
      "**Workflow:**\n1. Confirm CPU in `kubectl top pod`\n2. `dotnet-trace collect` CPU sample\n3. Speedscope flame graph\n\n**Architect:** CPU limit throttling shows as high throttled time — not always high CPU% — check cgroup metrics.",
      "Right tool per layer — managed vs kernel profiling.",
      "perf permissions? — paranoid setting blocks — adjust carefully.",
      "dotnet-gcdump? — Memory not CPU — complementary.",
      "perf on managed without dotnet tools",
      "Ignore CPU throttling from limits",
      "Profile prod without approval"),
    q("Load Average Meaning", "Linux", "Very Common",
      "What does load average mean on 8-core AKS node?",
      "Load average = runnable + uninterruptible tasks averaged over 1/5/15 min. On 8 cores, load 8 ≈ full utilization; load 16 = sustained overload.",
      "**Interpret:** High load + low CPU% → IO wait (disk). High load + high CPU → compute bound.\n\n**Architect:** Alert load per core ratio; correlate with pod resource requests — oversubscription causes scheduling latency not just CPU.",
      "Load average context requires core count.",
      "iowait in top? — Disk bottleneck despite low CPU.",
      "run queue latency? — Scheduler delay — advanced.",
      "Load 4 on 32 core called overload",
      "Ignore 15-min load trend",
      "Load high but all pods fine — miss IO wait"),
    q("File Descriptors ulimit", "Linux", "Common",
      "File descriptor exhaustion — symptoms and fix on Linux?",
      "Symptoms: 'too many open files', accept errors, connection failures. Check `ulimit -n`, `lsof -p PID | wc -l`, `/proc/PID/limits`.",
      "**Fix:** Raise `nofile` in systemd unit or `/etc/security/limits.conf`; fix connection leaks in app (.NET HttpClientFactory).\n\n**Architect:** Set reasonable container limits; monitor open FDs metric; load test connection counts.",
      "FD leaks take down high-connection services silently.",
      "fs.file-max sysctl? — System-wide ceiling.",
      "lsof -i? — Network connections as FDs.",
      "ulimit unlimited in prod without monitoring",
      "new HttpClient per request leak",
      "Ignore TIME_WAIT socket accumulation"),
    q("Cron vs systemd Timers", "Linux", "Common",
      "Cron vs systemd timers for scheduled jobs?",
      "Cron: simple time tables, universal. systemd timers: dependency aware, logging to journal, randomized delay, missed run catch-up.",
      "**Architect:** K8s CronJob replaces host cron for containerized workloads. VM maintenance: systemd timer with `OnCalendar` and `Persistent=true`.\n\n**Avoid:** Cron on AKS nodes — use K8s CronJob or Azure Functions timer.",
      "Scheduler choice follows deployment model.",
      "CronJob concurrency policy? — Forbid overlap — long jobs.",
      "at command? — One-off scheduled — rare.",
      "Cron on node for app batch job",
      "Overlapping cron without lock",
      "No logging of cron failures"),
    q("Firewall ufw iptables", "Linux", "Common",
      "Linux firewall relevance for AKS and Azure VMs?",
      "Azure NSG is primary cloud firewall. Host ufw/iptables on VMs and optional defense-in-depth; AKS pod traffic via NetworkPolicy not iptables on node manually.",
      "**VM:** `ufw allow 22/tcp`, default deny incoming.\n\n**AKS:** Don't manually iptables on nodes — kube-proxy/CNI manages. Use Calico/Azure NPM NetworkPolicy.\n\n**Architect:** Defense layers: NSG → NetworkPolicy → app auth.",
      "Know which firewall layer applies where.",
      "iptables-nft vs legacy? — Modern distros use nft backend.",
      "Azure Firewall vs NSG? — Centralized egress vs subnet.",
      "Disable NSG relying on host ufw only",
      "Manual iptables on AKS worker",
      "NetworkPolicy wide open because NSG exists"),
    q("TLS Cert Inspection openssl", "Linux", "Common",
      "Inspect TLS certificate expiry and chain with openssl?",
      "```bash\nopenssl s_client -connect api.example.com:443 -servername api.example.com </dev/null 2>/dev/null | openssl x509 -noout -dates -subject\n```\n\nCheck issuer, SAN, expiry — notAfter date.",
      "**Architect:** Monitor cert expiry 30 days ahead; cert-manager automates on K8s; Azure Key Vault certs for apps. Test full chain trust — missing intermediate causes client errors.",
      "openssl s_client is quick cert health check.",
      "check_cert_expiry scripts? — Nagios/Prometheus blackbox exporter.",
      "TLS 1.0/1.1 test? — openssl s_client -tls1 — verify disabled protocols.",
      "Expired cert discovered by users",
      "Self-signed in prod without trust chain",
      "Check cert but ignore hostname mismatch"),
    q("tcpdump Wireshark Basics", "Linux", "Occasional",
      "When use tcpdump for AKS network debugging?",
      "Capture packets to diagnose DNS, TLS handshake, connection reset — ephemeral debug container with tcpdump on pod network namespace.",
      "```bash\ntcpdump -i any host 10.0.1.5 and port 443 -w capture.pcap\n```\n\n**Architect:** Break-glass only — sensitive data in captures; time-bound; prefer observability mesh telemetry over packet capture in steady state.",
      "Packet capture is last resort — powerful and sensitive.",
      "nsenter pod network? — Host capture pod traffic.",
      "Wireshark analyze offline? — Share pcap — scrub PII.",
      "tcpdump in prod without approval",
      "Capture full payloads with PII",
      "No capture size limit — disk fill"),
    q("strace Debugging", "Linux", "Occasional",
      "When strace a process on Linux and limitations in containers?",
      "strace traces syscalls — diagnose hanging I/O, permission denied, missing files. `strace -p PID -f -tt`.",
      "**Container:** Need CAP_SYS_PTRACE or debug ephemeral container; distroless has no strace — use debug sidecar.\n\n**Architect:** High overhead — don't strace prod under load long. Prefer app-level tracing OpenTelemetry.",
      "strace is syscall microscope — use sparingly.",
      "strace connect hang? — See blocked connect syscall — firewall.",
      "eBPF bpftrace? — Lower overhead modern alternative.",
      "strace prod all day — performance kill",
      "strace without -f misses child processes",
      "No correlation with app trace ID"),
    q("lsof Network Ports", "Linux", "Common",
      "Use lsof to find which process holds port 8080?",
      "`lsof -i :8080` or `ss -tlnp | grep 8080`. Shows PID/command binding port — resolve port conflicts on VMs and debug Connection refused.",
      "**Container host:** Map to container via crictl. **.NET:** Kestrel binding `ASPNETCORE_URLS` — address already in use means duplicate bind.\n\n**Architect:** Document required ports per service; avoid hostNetwork unless necessary.",
      "Port binding conflicts cause mysterious startup failures.",
      "lsof -p PID? — All open files for leak debug.",
      "fuser -k? — Kill process on port — dangerous prod.",
      "Kill random PID on port without identification",
      "hostNetwork:true without port coordination",
      "Ignore IPv6 vs IPv4 bind differences"),
    q("Environment Variables", "Linux", "Common",
      "How Linux environment variables affect .NET containers?",
      "K8s sets env from ConfigMap/Secret; Docker `-e`; systemd `Environment=`. .NET config hierarchy: env vars override appsettings with `__` nesting.",
      "**Inspect:**\n```bash\nprintenv | sort\nkubectl exec pod -- env | grep ConnectionStrings\n```\n\n**Architect:** Never log secrets from env; use `envFrom` carefully; document required env contract in Helm chart README.",
      "Env vars are 12-factor config — understand precedence.",
      ".NET __ nesting? — `Logging__LogLevel__Default=Warning`.",
      "export in profile only? — Non-interactive pods miss it.",
      "Secrets printed in startup log",
      "Env var typo silent default misconfig",
      "Huge env block — use file mount instead"),
    q("Path and Symlinks", "Linux", "Common",
      "PATH and symlink issues in containers and Linux scripts?",
      "PATH order determines which binary runs; symlinks can break relative paths or point to wrong version. Use absolute paths in production scripts.",
      "**Container:** Minimal PATH in distroless — only bundled binaries. **Shebang:** `#!/usr/bin/env bash` vs pinned `/bin/bash`.\n\n**Architect:** Immutable infrastructure — don't rely on `/usr/local/bin` drift on nodes.",
      "PATH assumptions cause 'works in dev shell' failures.",
      "readlink -f? — Resolve symlink canonical path.",
      "which -a? — All PATH matches for command.",
      "Relative path in cron job fails",
      "Symlink to wrong dotnet version",
      "Trust PATH on shared jump box"),
    q("Log Rotation logrotate", "Linux", "Common",
      "Configure logrotate for VM apps vs container stdout logging?",
      "logrotate compresses/rotates file logs — `/etc/logrotate.d/app`. Containers should log stdout — collected by cluster not logrotate on node.",
      "**VM example:**\n```\n/var/log/myapp/*.log {\n  daily\n  rotate 14\n  compress\n  copytruncate\n}\n```\n\n**Architect:** AKS node disk — journald size limits; app logs via Fluent Bit — not files in container.",
      "Log rotation prevents disk full — know your logging model.",
      "copytruncate vs create? — App must reopen log file — know app behavior.",
      "Docker json-file max-size? — Dev Docker only.",
      "Unbounded file logs in container",
      "logrotate missing on VM — disk full",
      "Rotate without ship to central store"),
    q("User Group Management", "Linux", "Common",
      "Linux user/group management for service accounts?",
      "Dedicated service user per app — no login shell `/usr/sbin/nologin`. Group for shared file access. UID/GID stable for volume mounts.",
      "**Container:** Match `runAsUser` with image USER. **VM:** `useradd -r -s /usr/sbin/nologin appuser`.\n\n**Architect:** Central identity (AAD) for humans; local service accounts for processes only.",
      "Service account hygiene limits compromise blast radius.",
      "usermod -aG? — Group membership for file access.",
      "nscd/sssd? — Enterprise LDAP/NSS integration.",
      "App runs as shared user 'app'",
      "UID mismatch volume permission denied",
      "Human users share service account"),
    q("SELinux AppArmor Awareness", "Linux", "Occasional",
      "SELinux and AppArmor awareness for container architects?",
      "Mandatory access control — confines process capabilities beyond Unix permissions. AKS/Ubuntu may use AppArmor profiles for containers.",
      "**Symptoms:** Permission denied despite chmod 777 — MAC blocking. **Check:** `aa-status`, audit logs.\n\n**Architect:** Don't disable SELinux/AppArmor globally — adjust profiles. Container seccomp/AppArmor defaults from runtime.",
      "MAC explains 'impossible' permission denials.",
      "seccomp profile? — K8s RuntimeDefault — syscall filter.",
      "Privileged bypasses MAC? — Another reason deny privileged.",
      "setenforce 0 as fix",
      "Custom profile without testing",
      "Ignore audit denials in logs"),
    q("Container Host Linux", "Linux", "Common",
      "What Linux knowledge matters for AKS node troubleshooting?",
      "cgroup memory/CPU, containerd/crictl, networking bridges/CNI, systemd kubelet, disk/image GC, logs journald, NSG + iptables interaction.",
      "**Node triage order:**\n1. Node Ready? `kubectl describe node`\n2. Disk/memory pressure?\n3. kubelet/containerd healthy? `systemctl status`\n4. Image pull errors? `crictl pull`\n\n**Architect:** Platform SRE owns nodes; app teams need basic fluency for joint incidents.",
      "Container host is still Linux — abstractions leak.",
      "cgroup v2? — Unified hierarchy — .NET respects limits.",
      "Node problem detector? — AKS addon — auto drain unhealthy nodes.",
      "Treat AKS nodes as black boxes always",
      "crictl vs docker CLI confusion",
      "No runbook for NodeNotReady"),
    q("AKS Node Troubleshooting", "Linux", "Very Common",
      "Systematic AKS node NotReady troubleshooting?",
      "Check node conditions (MemoryPressure, DiskPressure, PIDPressure), kubelet logs, recent upgrades, subnet/IP exhaustion, taints.",
      "**Steps:**\n1. `kubectl describe node` — Conditions and Events\n2. `kubectl get pods -A -o wide | grep NodeName`\n3. SSH/bastion: `journalctl -u kubelet`\n4. Azure Portal: node pool health, scale set\n5. Cordon/drain if unrecoverable — replace node\n\n**Architect:** Automate node auto-repair; alert on NotReady >5 min.",
      "Node issues affect all pods on host — prioritize resolution.",
      "Surge upgrade stuck? — PDB blocking — temporary scale.",
      "Subnet full? — Scale fails — expand VNet prefix.",
      "Delete node without drain",
      "Ignore DiskPressure until kubelet stops",
      "No monitoring on node conditions"),
    q("Linux Incident Triage Workflow", "Linux", "Very Common",
      "Linux incident triage workflow for platform architect?",
      "Stabilize → assess blast radius → gather evidence → hypothesize → mitigate → postmortem. Use runbooks; communicate status; avoid random changes.",
      "**First 15 minutes:**\n1. User impact? SLO breach?\n2. Recent deploy/change?\n3. Golden signals: latency, errors, traffic, saturation\n4. `kubectl get nodes,pods -A` — cluster healthy?\n5. Top: CPU/memory/disk/errors from dashboards\n\n**Architect:** Incident commander role; timeline doc; blameless postmortem with actions.",
      "Structured triage beats heroic debugging.",
      "Severity levels? — SEV1 customer down — escalation path.",
      "Rollback first? — If deploy correlated — roll back before deep debug.",
      "Random reboot without hypothesis",
      "No incident timeline",
      "Skip postmortem for 'quick fix'"),
]

ALL_WEEKS_25_28 = {
    25: WEEK_25,
    26: WEEK_26,
    27: WEEK_27,
    28: WEEK_28,
}
